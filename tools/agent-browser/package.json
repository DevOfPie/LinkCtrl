{
  "name": "linkctrl-agent-browser",
  "private": true,
  "version": "0.0.0",
  "description": "The browser an agent drives (@playwright/cli) and the kept spec that outlives the session (M46.5). Verification only — never a build or runtime dependency of LinkCtrl (Plan.md D25).",
  "type": "module",
  "license": "SEE LICENSE IN ../../LICENSE",
  "scripts": {
    "verify": "playwright test --reporter=json",
    "install-browsers": "playwright install chromium"
  },
  "dependencies": {
    "@playwright/cli": "0.1.18",
    "@playwright/test": "1.62.1"
  }
}
