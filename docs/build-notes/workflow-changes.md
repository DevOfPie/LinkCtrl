# Workflow changes

Changes to **how this project is built** — the operating contract, the loop, the
commands — as opposed to changes to the product. The process backlog, and the
record of what the process used to be.

It exists because process changes were the one kind of work with no tracker.
A product defect gets a row in [deferred-findings.md](deferred-findings.md); a
product feature gets a Plan.md row and a milestone file; a process change got a
commit and nothing else. Once `.queue.md` was drained the only surviving record
that a change had been *asked for* was a commit message, and `.queue.md` is
untracked, so it survives no clone.

| Not here | There |
| --- | --- |
| Why a change was made | [decisions.md](decisions.md) — append-only, and every row below points at its entry |
| A question with no answer yet | [upcoming-decisions.md](upcoming-decisions.md) |
| A defect in the product | [deferred-findings.md](deferred-findings.md) |
| Product scope | [Plan.md](../../Plan.md) |
| The rules themselves | [workflow.md](workflow.md), [phase-loop.md](phase-loop.md), [planning.md](planning.md) |

A row here is a change to make, or one already made. It is not the change: the
rule always lives in the file it governs, and a row that tries to state the rule
becomes a second copy that drifts.

**Nothing leaves this file silently.** A row is moved to *Made* with its commit,
or removed with the removal logged in decisions.md, per workflow.md's standing
rule. Abandoning a proposed change is a decision.

---

## Proposed

Asked for, or identified, and not yet made. Approval is the owner's, the same as
for a deferred finding — an unapproved row is a suggestion, not scheduled work.

**Emptied on 2026-08-06 and not empty since 2026-08-08.** Every row that has
ever been in this table was made; none was abandoned.

| # | Change | Why | Raised | Approved |
| --- | --- | --- | --- | --- |
| W37 | **The loop should say what it does when the harness it runs in forbids spawning a worker**, rather than leaving the actor to discover the conflict at the first ordinary build and stop | [phase-loop.md](phase-loop.md#three-actors) gives step 2 to a worker spawned fresh and forbids the orchestrator from editing code, tests or SQL. A session launched with a standing instruction not to spawn subagents can therefore run reviews, amendments and triage output — all of which are the orchestrator's — and then stop dead at the first milestone that is ordinary build work. **That is what happened on 2026-08-08**: M51.9, M46's reopening and F174 all landed, and M52 could not be taken either way. The conflict is silent until it fires, which is the part worth fixing: nothing in the loop's own text says the two-actor split has a hosting requirement, so a run can be planned as unattended and turn out not to be. Three shapes are available and the row does not pick one — say the requirement out loud at [step 0](phase-loop.md#0-resume) so a session finds out before it starts, or name the conditions under which the orchestrator may build and what it costs, or declare that such a session is a review-and-triage session by construction and stops before the first build | 2026-08-08, from M51.9's run | **No.** Raised by the actor that met it, which is the weakest possible evidence that it is worth the owner's time — one run, one harness, and a workaround (start a session without the instruction) that costs a restart and no knowledge |
| W42 | **Correct the release-notes extraction comment in `.github/workflows/release.yml`** — written and waiting at [`ci/proposed/release.yml`](../../ci/proposed/README.md) | The comment says *the newest version is always the last section in CHANGELOG.md*. It is the **first**: Keep a Changelog orders newest first, so the file runs 0.3.0, 0.2.0, 0.1.0 with the link-reference definitions at the bottom. The consequence is not the awk, which is correct and was re-run against the 0.3.0 changelog — it is that the comment tells the next reader the reference-block exit guards the release they are cutting, when it guards the **oldest** section. That exit is there because v0.1.0's published body really did sweep the definitions in. Getting it backwards means changing the wrong line under time pressure, during a release, which is the only time anybody reads it. Comment-only: the extraction is untouched, byte for byte | 2026-08-11, from [F196](deferred-findings.md#open) at M58's final review | **Not yet — the owner applies it.** Nothing here can push `.github/workflows/`; the token carries no `Workflows` permission and GitHub refuses the push, not the merge. `make workflow-proposals` reports it pending |
| W39 | **Decide whether a milestone file should cite `file:line` at all**, and if it should, gate it | A milestone file proves its claims by pointing at the tree, and a line number stops being a pointer the moment anybody inserts a line above it. M58 moved roughly forty of them in one commit — see [F194](deferred-findings.md#open) — including one its own step-1 amendment had corrected hours earlier, and `make check-links` cannot see any of it because a `file:line` is not a link. This has been silent since Phase 1. **Three shapes, and this row picks none.** A **test** resolving every citation in `phase-details/` against the tree makes it permanent, and also makes every future line insertion a build break somebody must repair by hand. **Symbol names instead of line numbers** — `EraseDeletedAccounts` rather than `accounts.sql:370` — rot far more slowly and are greppable, but say less about where inside a long statement to look. **Accept the rot and say so**: record that a shipped milestone's citations are true as of its own commit and nothing later, which costs nothing and is arguably what a historical record already means | 2026-08-10, from the third reviewer run at M58 | **No.** Raised at a stop, on a milestone already rejected twice, which is the worst moment to judge how much a process is worth |

## Made

Newest first. The commit is the change; this row is how somebody finds it
without knowing what to grep for.

| # | Change | Commit | Entry |
| --- | --- | --- | --- |
| W41 | **`check-links` is wired into `check` and `ci-build`, so the gate runs without anybody remembering it.** It was invoked by `make check-links` typed by hand and by `scripts/release-check.sh`, which runs only at a release — so the link gate, and the table-column gate [M58](phase-details/m58.md) had just added to it, were enforced nowhere automated. `Makefile:224` and `:282` now both carry it, which also makes `check`'s own description — *everything CI runs, short of integration tests* — true again. `release-check.sh`'s label said *every relative link and anchor resolves* and now names both halves. **Sabotage-verified**: a broken link appended to a tracked file took `make check` to rc=2, restored by counter-edit. This is the **wiring half of [F198](deferred-findings.md#open)**; the half where the gate cannot see a delimiter row narrower than its header stays a finding, and the row says so. Split at the owner's direction 2026-08-11 — which gates run is a process question, and what a gate can see is a defect | *(this commit)* | [A gate nobody runs](decisions.md#2026-08-11--a-gate-nobody-runs) |
| W40 | **The fifteen-milestone target binds planning, not production.** [planning.md](planning.md#the-size-target-a-phase-stays-under-sixteen-milestones) said any insertion past the target was a **phase-boundary conversation**, so a milestone the build turned out to need cost an owner decision for having pushed a count past fifteen. It now says a phase is *planned* at fifteen — a real ceiling on the plan — and a milestone the build needs is added when it is needed. **Owner-set 2026-08-11**, in these words: *the initial milestone limit in a phase is in place to restrict initial planning, adding a few needed milestones during production is acceptable.* *Needed* and *a few* are kept in the rule because they are the bound: this is not a licence for unbounded growth, it is a statement that the ceiling was aimed at optimistic planning rather than at honest discovery. What the target still removes — a phase growing one insertion at a time with nobody ever deciding it was large — moves to planning time, where the count is a prediction somebody is making. Phase 3's two recorded exceptions are left as they happened rather than reinterpreted; under this reading neither would have been a boundary conversation | *(this commit)* | [The target was aimed at the plan, not at the build](decisions.md#2026-08-11--the-target-was-aimed-at-the-plan-not-at-the-build) |
| W38 | **A third actor: an independent reviewer reads the tree between the worker's report and acceptance, on every milestone.** [phase-loop.md](phase-loop.md#reviewer--between-33-and-acceptance) is the contract. It is spawned fresh, run synchronously, given the milestone number, the branch and that file — **and nothing else**, because a reviewer handed the worker's report reviews the report. Two halves, and they are deliberately unequal: whether the tree satisfies `mN.md`, which the orchestrator was already doing, and **whether anything in the diff makes a *shipped* milestone's claim false**, which nobody in the loop was doing at all. The second is why it exists. It **edits nothing** — not code, not records, not a deferred row — and a reviewer that found nothing must say so, because silence and *did not look* are the same string. A finding is not a rejection: the orchestrator dispositions it like any other, and one that falsifies a shipped claim is a [reopening](workflow.md), which is scheduling and the owner's. **Asked for by the owner 2026-08-09**, at M58's acceptance, in those terms — *to catch these mistakes that keep making us re-open closed milestones* — and the evidence was in front of both of us: M58's own worker filed F187, a defect of M52 that had been in the erasure pass since it was written **It costs 2,691 bytes on every `/work phase` resume** — 2463 in phase-loop.md, 228 in workflow.md, measured rather than estimated — and one agent per milestone. Defended at [the 2,691 bytes W38 added to every resume](decisions.md#2026-08-10--the-2691-bytes-w38-added-to-every-resume-defended), not by this row. | *(this commit)* | [A third actor, for the claim nobody was checking](decisions.md#2026-08-09--a-third-actor-for-the-claim-nobody-was-checking) |
| W36 | **A plan is reviewed by somebody who did not write it, before the first milestone is built against it.** [planning.md §7](planning.md#7-review-it-before-anything-is-built-against-it) is the contract: a phase plan or a milestone-adding scope addition, never an annotation or a count fix. The reviewer gets the plan, this repository's standards named as files, and **none of the author's conclusions** — no summary of intent and no hint of which parts are thought weak, which is the fastest way to get a review that confirms rather than checks. **A different model from the writer's, and a more capable one where the choice exists**, because a reviewer sharing the writer's model shares its blind spots; where only one is available, a fresh context that has never seen the reasoning. Seven dimensions, including that the reviewer **checks the plan's `file:line` citations against the tree** and proves absence claims with its own searches. Findings are **verified before being acted on** — a review is evidence, not a verdict — with documentation corrections made under the existing standing approval and anything touching a definition of done or a count raised as a prompt. **The phase already budgets two adversarial reviews for the code and budgeted none for the plan**, which is what every one of those milestones validates against | *(this commit)* | [Reviewing the specification, not only the thing built from it](decisions.md#2026-08-07--reviewing-the-specification-not-only-the-thing-built-from-it) |
| W8 | [`/preview-decisions`](../../.claude/commands/preview-decisions.md) asks its questions instead of only filing them, in the order the owner specified: **the file first, then the prompts.** A run interrupted halfway through asking has still recorded everything it found. The prompt is the entry's own content — options, costs, recommendation, assumptions, all four already required by step 2 — because a question phrased differently in conversation than in the file is two questions. An answer moves to [decisions.md](decisions.md) **immediately**, carrying the date it was given, with the `D` number assigned when the milestone that uses it lands; *you decide* means the entry's stated default, recorded as such; silence leaves the entry untouched and the report names it. Only what this run wrote is asked — re-asking older entries every run is how a read-ahead becomes noise. **The rule was already written**: upcoming-decisions.md's header says *write it here first, then answer it*, and the command simply never did the second half | *(this commit)* | [The command catches up with the file it writes](decisions.md#2026-08-06--the-command-catches-up-with-the-file-it-writes) |
| W35 | A finished phase's status rows move to their own file. Phase 2's 33 rows are now [phase-2.md](phase-details/phase-2.md), matching what Phase 1 already did, and [phase-details/README.md](phase-details/README.md) carries the **live** phase alone — today an empty Phase 3 table, because Phase 3 is not planned. **The saving is small and the growth avoided is not**: README falls 10044 → 8838 bytes, but appending Phase 3 beneath Phase 2 would have reached forty-eight rows and kept climbing every phase, on a file read at a 0.88 ratio on every resume. The inherited-rules table **stayed** — its anchor is linked from three places, and most of its rules are product invariants that outlast a phase; which ones Phase 3 inherits is now stated as *confirmed at planning* rather than assumed from the table having been left in place | *(this commit)* | [What the resume path reads is the live phase](decisions.md#2026-08-06--what-the-resume-path-reads-is-the-live-phase) |
| W34 | `scripts/doc-cost.sh` charges a file by **what the contract instructs**, not by its size. An entry may now be `path::regex`, charged at the longest matching line, and `Plan.md` is charged at its longest ordering row — 185 bytes — because [step 1](phase-loop.md#1-validate) reads one row of it. The `/work phase` resume floor falls from **229189 bytes to 59420** (≈57297 → ≈14855 tokens), and no read got cheaper: the old number was three-quarters one file nobody reads whole. **Made at the phase boundary on purpose** — [W24](#made) refused this mid-phase because changing what a measure measures makes phases incomparable, and the generated header now carries that discontinuity permanently. A pattern matching nothing falls back to the whole file and says so loudly in the table, which is not a contrived test: the first pattern was broken and the fallback is how it was caught | *(this commit)* | [Measuring what the contract reads](decisions.md#2026-08-06--measuring-what-the-contract-reads-not-what-it-names) |
| W14 | The harness form, not the by-hand statement. [`tools/render-verify/`](../../tools/render-verify/README.md) re-checks [M26.5](phase-details/m26.5.md)'s popover geometry in Blink, Gecko and WebKit behind an opt-in `make verify-render`, with the Taskfile mirror beside it. **Its page under test is neither a running instance nor a committed fixture**: every geometry-bearing class is read out of `layout.html` and `partials/nav.html` at run time and styled by the built `app.css`, because a committed fixture is a second copy free to stay green while the template drifts, and the :8081 instance would make a geometry check depend on Docker, Postgres, Redis, a schema and a session. Nine claims, measured against what the page renders rather than against numbers retyped from the template. Node is permitted by D25 and appears nowhere else; `verify-render` is reachable from no gate, verified by walking the prerequisite closure of all ten | *(this commit)* | [A check that outlives the session that ran it](decisions.md#2026-08-06--a-check-that-outlives-the-session-that-ran-it) |
| W33 | **The fallback half only.** [Step 1](phase-loop.md#1-validate) takes the first *independent* un-`done` row when the next one is blocked, instead of stopping — independent meaning its `Depends on` rows are all `done` and it shares no work area with the blocked one. Table order still decides and nothing is skipped that was not blocked. The blocked row is **parked**: its question stays owed, verbatim on the note's new `Parked:` line, re-reported at every step boundary and at the run's stop, and the loop never answers it. [§4](phase-loop.md#4-repeat-or-stop)'s *a prompt is unanswered* row gains *and no un-`done` row is independent of it* — **a real weakening of the only stop the loop imposes on itself**, chosen over the narrower dependency-blocks-only form because the owner judged that an unanswered prompt is the block that actually happens. The concurrency half is **not** made: one worker still holds exactly one milestone, and the note still names one | *(this commit)* | [Working past a question without losing it](decisions.md#2026-08-06--working-past-a-question-without-losing-it) |
| W23 | `/work` reaches this repository from any directory. The contract is [work-loop.md](work-loop.md#dispatch-from-outside-this-repository)'s new *Dispatch from outside this repository*: a dispatcher parses by the shared rule, resolves the **outermost** target against a registry of checkouts, **verifies before entering** — checkout present, contract present, kind declared, work outstanding — and hands everything else to that repository's own contract. It holds no repository logic, and an unresolvable target is the existing unknown-target prompt one level up rather than a near-match. **The half this repository does not version is named rather than implied**: the dispatcher and its registry are home configuration and survive no clone, so a fresh checkout gets every local loop and no cross-repository dispatch. The versioning question the row demanded an answer to is answered that way — the same shape as [`ci/proposed/`](../../ci/proposed/README.md), where the tracked half is the description | *(this commit)* | [Dispatch is a resolution step, not a cd](decisions.md#2026-08-06--dispatch-is-a-resolution-step-not-a-cd) |
| W16 | `/note --discuss` marks a row the owner wants to think about **before** it acquires a type. It changes nothing about `/note`, which still classifies nothing and asks nothing — the marker is a request written down. The teeth are in [`/process-queue`](../../.claude/commands/process-queue.md): a `discuss` row is **not classified at step 1**, it gets the tree read for it and then a prompt, and the owner's answer is what types it. *Being obvious is not an exemption.* Unlike `blocking?` the marker is never inferred and never dropped on the orchestrator's judgement, because the two answer different questions — `blocking?` asks whether the work in flight is wrong, `discuss` asks for the owner's thinking. Also in [workflow.md](workflow.md#something-is-noticed-and-now-is-not-the-time)'s queue trigger and [commands.md](commands.md#note) | *(this commit)* | [The marker that buys back a decision](decisions.md#2026-08-06--the-marker-that-buys-back-a-decision) |
| W1 | The documentation pass **judges** the always-read contract's growth, not merely regenerates the number: [workflow.md](workflow.md#documentation-pass--after-validation-before-the-pr-exists)'s `doc-cost.md` row now says defend or trim, and points at [W13](#made)'s definition rather than carrying a second copy of it. Two obligations, one definition — the review where growth is still cheap to reverse, and the close where the number is final. **The row was marked approved for five days with nothing left to make**, because it had been scheduled into [M45](phase-details/m45.md) as a bullet: the judgement ran once, the milestone landed, and the gate still read *regenerated* | *(this commit)* | [A bullet is spent; a rule recurs](decisions.md#2026-08-06--a-bullet-is-spent-a-rule-recurs) |
| W13 | An `X.9` review judges what the always-read contract costs — `make doc-cost`, then **defend the growth or trim to pay for it**. Regenerating and moving on is neither, which was the gap: the number existed and obliged nobody. It sits inside [phase-loop.md](phase-loop.md#two-milestones-that-do-not-end-like-the-others)'s non-delegated section, so *the orchestrator runs it* needs no new rule and the no-delegation rule gains no exception. **The one place the judgement is defined** — anything else obliged to make it points here. It also says to judge the *recurring* cost rather than the headline: `scripts/doc-cost.sh` charges Plan.md whole while step 1 reads one row of it, and [doc-cost.md](doc-cost.md) as generated puts Plan.md at 169954 of a 225579-byte resume charge against a realized read ratio of 0.01 | *(this commit)* | [Six process rows approved](decisions.md#2026-08-06--six-process-rows-approved-and-the-two-answers-that-cost-something), and [W13, and the bytes it spends to judge bytes](decisions.md#2026-08-06--w13-and-the-bytes-it-spends-to-judge-bytes) |
| W32 | [planning.md](planning.md) carries a **size target: a phase stays under sixteen milestones**, insertions counted. Past it is a phase-boundary conversation, not arithmetic — and the rule states its own trap, that the cheapest way to satisfy a count is fatter milestones | *this commit* | [The size target is fifteen, and the rule says what it costs](decisions.md) |
| W31 | The `# PROPOSED` header [W28](#made)'s apply left on the live `ci.yml` is gone — four lines of deletion and nothing else, verified by diff before proposing and by `make workflow-proposals` reporting a byte-for-byte match after applying. Everything that runs on push — triggers, `permissions:`, `concurrency:`, `GO_VERSION`, service images, action SHA pins — is untouched. The same defect W30 closed on `release.yml`, on the other workflow: the header predates the rule that now forbids it | `94990ea` | [F159](deferred-findings.md), the Fable 5 review finding that caught it |
| W30 | The `# PROPOSAL` header [W29](#made) left on the live `release.yml` is gone — twenty-five lines of deletion and nothing else, so `release.yml` no longer opens by telling a reader to apply a file they are already reading. **The route's own lesson, learned by using it**: applying a proposal is `cp`, so anything a whole-file copy says about itself lands on the workflow that runs. `ci/proposed/README.md`'s *Writing one* section now says a proposal carries no header addressed to the reviewer — the diff is the description, and `make workflow-proposals` prints it. Verified after applying: F1's second `awk` exit and its `printf` survive, and both released sections still extract with zero stray reference definitions | `64db850` | [F1's fix takes W27's route](decisions.md#2026-08-06--f1s-fix-takes-w27s-route-because-the-token-cannot-push-it) |
| W29 | `release.yml`'s changelog extraction stops at the link-reference block, closing [F1](deferred-findings.md). **The first proposal to go through W27's route end to end**, and it found one flaw in the route: a proposal written as a whole-file copy carries its `# PROPOSAL` header onto the live workflow when applied, so the file that runs opens by telling a reader to apply it. [W30](#proposed) removes it and `ci/proposed/README.md` gains the rule, because the fix for a bad proposal is another proposal — the token cannot touch the file either way | `447095c` | [F1's fix takes W27's route](decisions.md#2026-08-06--f1s-fix-takes-w27s-route-because-the-token-cannot-push-it) |
| W28 | `.github/workflows/ci.yml` is the shim W27 proposed: every step is now a `make ci-*` call, and the file holds only what CI *is*. **Applied by the owner**, which is the whole point of the route — the branch that proposed it could not have pushed it. Verified after the fact rather than assumed: `make workflow-proposals` compared the applied file against the proposal and reported `applied`, and all four jobs passed on the applied commit, with `Lint` falling from ~60s to ~20s because `ci-lint` folds its steps into one. The proposal file is deleted here, per the rule that a matched proposal left in place becomes a second copy of the workflow free to drift. **One defect found by applying it**, and fixed in the same commit: the applied copy had lost its trailing newline, and a byte-for-byte comparison called that a pending change — a report that cries pending on an applied proposal is one people stop reading, which is this repository's link-gate failure exactly. The final newline is now normalised and nothing else is | *(this commit)* | [CI logic moves into the Makefile](decisions.md#2026-08-06--ci-logic-moves-into-the-makefile-because-the-token-cannot-write-the-workflow) |
| W27 | CI's logic moves out of YAML and into the Makefile: `ci-build`, `ci-lint`, `ci-integration`, `ci-test`, `ci-image-smoke`, built from new `vet`, `check-tidy`, `check-generate` and `check-version-stamp` targets and three new scripts. The sqlc pin becomes `SQLC_VERSION` in the Makefile instead of a literal in a workflow step. [`ci/proposed/`](../../ci/proposed/README.md) is created as the route for the changes that still need the owner, `make workflow-proposals` reports what is waiting there, and [workflow.md](workflow.md) gains the standing rule. The Taskfile mirror gained all ten, read-verified against their Makefile counterparts; the tasks were not run. **What it buys:** a new CI check is now a Makefile edit that reaches the next push, where before it was a workflow edit that no token here can make. **What it costs:** GitHub shows one step per `run:`, so a folded job loses the per-check timing and the tick beside each name — `make` names the failing recipe in the log, and that is the trade | *(this commit)* | [CI logic moves into the Makefile](decisions.md#2026-08-06--ci-logic-moves-into-the-makefile-because-the-token-cannot-write-the-workflow) |
| W26 | A **milestone** is a target: `/work M45 phase`. It bounds the run rather than selecting the work — the loop resumes as it always does, takes the next un-`done` row in the status table's order, and gains one [§4](phase-loop.md#4-repeat-or-stop) stop condition. It can only stop a run **sooner**: never skips a row ordered before the named one, never starts one after it, weakens nothing. Skipping is refused because the status table's order and *Depends on* column are what decide what is next. Three resolutions happen before the loop is entered — already `done` enters nothing, another phase prompts, absent is the existing unknown-target prompt. The bound rides the note's existing `Stop:` line rather than a second mechanism. **+3091 bytes**, no deletion offsetting it, of which only 340 are on the resume path | *(this commit)* | [A target that says where the run stops](decisions.md#2026-08-05--a-target-that-says-where-the-run-stops-not-what-it-builds) |
| W25 | The note's resumability becomes a claim that can be shown false — *a fresh session reading this note and step 0's inputs reaches step 1 for the next milestone without asking the owner anything* — checked at [3.9](phase-loop.md#3-land) by the actor that just wrote it. 3.9 is named as the **checkpoint** and reported as the safe point to compact or restart, because the loop has no compaction lever of its own: `--autocompact` is fixed at session launch and `/compact` is the owner's. The *Context is long* row now names compaction, so a due one cannot read as an ending. **Partly paid for** by deleting two rationale passages duplicated in [decisions.md](decisions.md); net **+890 bytes** on the most-read file, and the shortfall is stated rather than absorbed | *(this commit)* | [The loop cannot compact itself](decisions.md#2026-08-05--the-loop-cannot-compact-itself-so-the-checkpoint-says-when-it-is-safe-to) |
| W24 | `scripts/doc-cost.sh` labels the resume cost `/work phase`, not `/work-on-phase`. The generated [doc-cost.md](doc-cost.md) is **not** regenerated here: it is a measurement taken at the commit that generated it, its header says every diff in it is real growth, and regenerating now would fold a phase's accumulated drift — `Plan.md` 64169 → 162696 bytes, `decisions.md` 437566 → 995314 — into a workflow commit and take [M45](phase-details/m45.md)'s *judge the growth* bullet with it. The rendered file therefore still names the old command until M45 regenerates it, which is a stale measurement rather than a false claim | *(this commit)* | — (the row carries its own reasoning) |
| W12 | [commands.md](commands.md) — every command as a contract rather than as a harness invocation: what it is given, what it may decide, what it must not do, where its authority lives, what it produces. Plus the three things an agent reached some other way needs, and what is deliberately **not** a command. It is the authority on nothing — the command file wins, and a disagreement is a defect in the page | *(this commit)* | [Describing the commands to a reader who is not here](decisions.md#2026-08-05--describing-the-commands-to-a-reader-who-is-not-here) |
| W10 | `/stop --checkpoint` — the deferred stop folded into the same command as a flag, rather than a second command. The checkpoint needed no redefining: it is still the end of the landing step and nothing else, now stated once for both loops. The no-loop case is where the two stops stop agreeing, and the command says so | *(this commit)* | [The second stop becomes a flag](decisions.md#2026-08-05--the-second-stop-becomes-a-flag-and-the-one-case-where-the-two-stops-disagree) |
| W9 | `/stop` exists, so stopping the loop is a command and not only a phrase. Immediate only — it takes no arguments, and given any it stops **nothing** and says so. Stopping at the checkpoint stays a phrase until W10 | *(this commit)* | [Two stops, one of which was only ever a sentence](decisions.md#2026-08-05--two-stops-one-of-which-was-only-ever-a-sentence) |
| W11 | `/work-on-phase` is gone. `/work [target …] <kind>` resolves a route and enters that route's loop — `/work phase` is the old command, `/work workflow` is a new loop over this file's approved *Proposed* rows, and `/work` alone reports both backlogs and enters nothing. [work-loop.md](work-loop.md) is the routing contract and the workflow loop. **Wider than the row asked for**, at the owner's direction on 2026-08-05: targets are positional and nest, an unknown target or kind is a prompt rather than a near-match, and `--revalidate` re-derives the route and prompts before entering | *(this commit)* | [A command that chooses a loop](decisions.md#2026-08-05--a-command-that-chooses-a-loop-and-the-backlog-that-had-nothing-consuming-it) |
| W22 | The Taskfile mirror gained the nine Makefile recipes it lacked — `clean`, `cover`, `doc-cost`, `vuln`, `verify-assets`, `css-watch`, `docker-build`, `dist`, `release-check` — so a contributor without `make` can build, check and release. Sync is **read-verified**: each recipe was compared to its Makefile counterpart line for line, and `task --list` parses; the tasks were not run | *(this commit)* | [The Taskfile mirror catches up](decisions.md#2026-08-02--the-taskfile-mirror-catches-up-and-what-verified-means-for-a-mirror) |
| W21 | The carry-a-volume-over warning names `LINKCTRL_API_KEY_PEPPER` beside `POSTGRES_PASSWORD`, in both [environment.md](../dev-notes/environment.md) and [instances.md](../dev-notes/instances.md) | *(this commit)* | — (the warning carries its own reasoning) |
| W19 | *Verify, do not assume* now says a cached result is not a measurement, and names `GOFLAGS=-count=1` | *(this commit)* | [Two gate rules that lived only in an untracked file](decisions.md#2026-08-02--two-gate-rules-that-lived-only-in-an-untracked-file) |
| W20 | [dev-notes/instances.md](../dev-notes/instances.md) records how to run a single integration test — `make -n test-integration` prints the command with its DSNs filled in | *(this commit)* | [Two gate rules that lived only in an untracked file](decisions.md#2026-08-02--two-gate-rules-that-lived-only-in-an-untracked-file) |
| W17 | The loop stops rather than entering an `X.9` review it did not start the session with, and asks for a fresh one | *(this commit)* | [Two rules the last run earned](decisions.md#2026-08-01--two-rules-the-last-run-earned) |
| W18 | A milestone that adds a visible feature extends the demo seeder — an inherited rule, and a row in the commit gate | *(this commit)* | [Two rules the last run earned](decisions.md#2026-08-01--two-rules-the-last-run-earned) |
| W15 | [dev-notes/instances.md](../dev-notes/instances.md) now records how to claim a freshly migrated instance through `/setup`, the test instance's current credential, and a warning that signing in against the wrong port fails as *the email or password is incorrect* | *(this commit)* | [Draining the queue](decisions.md#2026-08-01--draining-the-queue-and-the-four-rows-that-could-not-be-verified) and its correction |
| W7 | Trackers gained a no-silent-removal rule, upcoming-decisions gained a section for questions no milestone forces, and this file was created | *this commit* | [Nothing leaves a tracker silently](decisions.md) |
| W6 | The per-commit Docs gate now names `README.md` and `docs/SECURITY.md`, not only Plan.md and decisions.md | `b068b73` | [The gate that never asked about README](decisions.md) |
| W5 | A workflow summary for an outside reader, at [README.md](README.md) in this directory | `035a9a0` | — (the file explains itself) |
| W4 | `stop at the checkpoint` — a second stop that lands the milestone in flight instead of abandoning it | `70ef4ac` | — (the rule carries its own reasoning) |
| W3 | Amending a bullet: the orchestrator corrects a **fact** and logs it; changing what a bullet **asserts** still prompts | `a21bdc3` | [Plan drift is allowed; silent plan drift is not](decisions.md) |
| W2 | The M24.5 amendment backfilled into the decision log, in the format W3 established | `87339bd` | [M24.5, amendment: the eight pages were nine](decisions.md) |

Phase 1's process history is not backfilled here. It is in decisions.md, in the
order it happened, and rewriting it into this table would be a second copy of a
record that is already append-only.
