# Deferred findings

Where out-of-spec issues go the moment they are found, instead of being fixed on
the spot or forgotten. Anything discovered while a milestone is in flight that is
not required by that milestone lands here as one row: what, where, the evidence
it is real, and a suspected severity.

**Nothing here is scheduled work.** The owner reviews each row and approves it
individually; approved rows become the phase's final milestone. An unreviewed row
is a report, not a commitment — which is what keeps "I noticed something" from
turning into unplanned scope.

Issues that make the *current* milestone's own claim false are in spec by
definition and get fixed immediately, whatever subsystem they turn up in. The
test is the claim, not the file.

**A row never just disappears.** It moves to *Closed* naming what closed it, or
it acquires a milestone number in [Plan.md](../../Plan.md), or its removal is an
entry in [decisions.md](decisions.md) saying it was dropped and why. Deciding a
finding does not matter is a decision, and one made silently is indistinguishable
from a row somebody deleted by accident. This is workflow.md's standing rule; it
is repeated here because this is the file most tempting to tidy.

The process around this — when to defer, what counts as work, what has to pass
before a phase PR — is in [workflow.md](workflow.md).

---

## Open

**Found in** names the milestone that was under way when the row was written —
not necessarily the milestone that caused the defect, which the evidence names
when it is known and different. It is written by whoever appends the row, at the
time, because it is unrecoverable afterwards.

| # | Found in | Finding | Where | Evidence | Severity | Reviewed |
| --- | --- | --- | --- | --- | --- | --- |
| F21 | M25 | Picking a workspace in the header dropdown does nothing until **Switch** is pressed, and any navigation silently discards the choice | `internal/ui/templates/partials/nav.html:23`–`:35` — a bare `<select>` and a separate submit button, with no change handler | Found by adversarial verification of two owner reports that a selection *resets* between pages and that the links page *does not update*. Both are this: the store-level switch is sound (`POST /workspace/switch` persists `users.last_workspace_id` and survives new sessions, 500 pages, API calls and short-link hits), but the UI never sends that POST unless the button is pressed, so the next render re-emits `selected` at the workspace the user is actually in and their pick vanishes. The template's own comment records what it believed was the cause — an auto-submitting select needs a change handler and the CSP forbids inline script — so this reads as a deliberate design whose affordance misleads, not an oversight. It is what the owner actually experienced. **That justification is false, and the tree falsifies it** (checked 2026-08-02, draining the queue): the CSP forbids *inline* script and always did, but htmx has been served from `internal/ui/static/js/htmx.min.js` since M11 (`13fb0f6`), is loaded by `layout.html:9` on every page, and runs under `script-src 'self'` — `internal/httpx/middleware.go:266-270` says so in as many words. `internal/ui/templates/pages/links.html:56` already ships **exactly** the control this finding says was impossible: `<select name="status" hx-get="/links" hx-trigger="change">`, needing no inline script and no CSP waiver. So the two-step was never forced by the CSP; it was a change handler nobody wired | Moderate. Nothing is broken or lost, and D22's persistence holds — but a claim *is* false, which is new: `nav.html`'s comment asserts a constraint the same codebase disproves one directory over, and that comment is what would stop the next reader from fixing this. The cost is that the control looks like it changed something and did not, which is indistinguishable from a bug to the person using it | No — but the **owner has directed the shape of the fix** (2026-08-02, via `/note`): the dropdown performs the switch on change, and *"a separate button to switch can't stay"*. The one thing that leaves open — what a browser without JavaScript gets once the button is gone — is in [upcoming-decisions.md](upcoming-decisions.md). Approval to schedule the work is still the owner's and has not been given |
| F22 | M25 | Switching workspace from a link detail page lands on a 404 | `internal/httpx/web.go:105` puts `shell.Path` into the switcher's `next`, so the POST redirects back to a link id that does not exist in the target workspace | Reproduced: from `/links/<id>` while acting in Marketing, switching to Sales 303s back to that same URL and renders the Not-found page. A links surface that visibly fails to follow a switch, where falling back to `/links` would have worked | Minor. One dead end, recoverable by navigating; no data is exposed or lost | No |
| F23 | M25 | Aliases are global across workspaces, so the create form leaks the existence of another workspace's aliases | `internal/store/query/links.sql`, `GetWorkspaceDefaultDomain` — every workspace shares the one default domain, and uniqueness is per domain | Reproduced: creating a link in Sales with an alias already used in Marketing answers `409 alias "vmk2" is already in use`. The query's own comment concedes the shape (*Phase 2 gives a workspace its own and this gains a filter*). Two costs: the same alias cannot be used in two workspaces, and a 409 tells a member of one workspace that an alias exists in another they may not be able to see | Minor to moderate, depending on whether workspaces are meant to be isolated from each other. Related to [M39](phase-details/m39.md), which gives a workspace its own domain | No |
| F24 | M32.9 | The dashboard shows two different numbers for the same link's clicks, side by side | Recent-links row reads `links.click_count`, which counts bot hits; the Clicks tile reads `workspace_click_daily`, which counts them separately | Found while adversarially verifying that the dashboard scopes by workspace (it does). With 7 human and 2 bot clicks on one link, the row read 9 and the tile read 7, on the same page for the same workspace. Not a scoping defect and not wrong data — two defensible definitions of *clicks* rendered adjacently with nothing distinguishing them | Minor. A plausible source of somebody reporting that the dashboard numbers look wrong | No |
| F25 | M32.9 | `ResolveWorkspaceForUser` does not check the organization's `deleted_at` while `ListWorkspacesForUser` does | `internal/store/query/auth.sql:26`–`:33` filters `w.deleted_at IS NULL` only; `internal/store/query/workspaces.sql:19`–`:25` joins `organizations` and filters both `w.deleted_at` and `o.deleted_at` | Verified by reading both queries. A workspace under a soft-deleted organization could be resolved into but never listed, so the switcher would mark nothing selected and a browser would show the first entry while the session acted somewhere else. **Latent today**: `DeleteOrganization` (`organizations.sql:71`) is a hard `DELETE` and nothing in the tree ever sets `organizations.deleted_at`, so the two queries agree in practice. It becomes live the moment anything soft-deletes an organization | Minor, latent. Filed because the asymmetry is invisible from either query alone | No |
| F17 | M32.9 | `docs/SECURITY.md`'s enumeration of what the audit log records is stale, and the file contradicts itself | `docs/SECURITY.md`, the paragraph beginning "**The audit log works, but plenty still does not write to it.**" (around line 255) | Found by M32.9's documentation-truth dimension. The paragraph says "The events recorded today are" — present tense, no as-of scoping — and lists the root redirect, the invitation lifecycle and M28's membership, workspace and organization changes. It omits five actions that have shipped since: `destination.blocked` (M30), `dispute.allowed` and `dispute.upheld` (M31), and `link.bot_blocking_changed` and `domain.bot_blocking_changed` (M32.5) — 5 of the 17 that exist. Adversarial check: the same file names `destination.blocked` at line 207, so SECURITY.md contradicts itself rather than merely lagging. No milestone's own definition of done is falsified, which is why this is a row and not an in-spec fix. | Minor. Documentation only, but it is the file a security-conscious reader trusts most, and it under-reports coverage rather than over-reporting it. | Reviewed 2026-08-01: owner agreed the M32.9 triage — deferred, not scheduled. Agreeing a row is deferred is not approving it as work; it stays here until a milestone claims it. F17 and F18 are the natural pair for M45's documentation pass. |
| F18 | M32.9 | The audit action vocabulary is split across two packages, so anything enumerating it from `internal/audit` silently misses two actions | `internal/dispute/dispute.go:113`–`:114` define `ActionDisputeAllowed` and `ActionDisputeUpheld`; the other fifteen are in `internal/audit/audit.go` | Found by M32.9. Adversarial check confirmed both actions are emitted and written correctly, so no behaviour is wrong today and this is structural only. It is the mechanical cause of F17: a reader listing the vocabulary from `audit.go` gets fifteen and believes that is all of them. The next milestone to add an audit surface has a coin-flip precedent to follow. | Minor. No incorrect behaviour; the cost is that the vocabulary has no single home, and every consumer that enumerates it is wrong by two. | Reviewed 2026-08-01: owner agreed the M32.9 triage — deferred, not scheduled. Agreeing a row is deferred is not approving it as work; it stays here until a milestone claims it. F17 and F18 are the natural pair for M45's documentation pass. |
| F19 | M32.9 | "May an invitation create an account" is derived independently in two places, and only one of them is the enforcement | `internal/httpx/web_invitations.go:152` re-derives it as `Config.Auth.SignupMode != config.SignupClosed`; enforcement is `internal/invite/invite.go:572` reading `cfg.NewAccounts`, set at `cmd/linkctrl/main.go:413` from `signupSvc.Effective().AdmitsNewAccounts()` | Found by M32.9. **The refutation partly succeeded and the finding is narrowed accordingly**: the two derivations agree in all four reachable states today, because `Effective()`'s only derivation is open-with-no-mailer becoming `invite`, which still admits accounts. So this is NOT a live defect and no user is currently misled. What remains is that D7's rule has two implementations, and the page's copy bypasses the effective-mode logic entirely — if `Effective()` ever gains a case yielding `closed`, the invitation page would invite somebody to register while redemption refuses them. | Minor, latent. Nothing is wrong today; the cost is a second implementation of a rule the project treats as absolute. | Reviewed 2026-08-01: owner agreed the M32.9 triage — deferred, not scheduled. Agreeing a row is deferred is not approving it as work; it stays here until a milestone claims it. F17 and F18 are the natural pair for M45's documentation pass. |
| F16 | M32 | Most dashboard pages are counted as redirects in the HTTP metrics, because the surface classifier's prefix list stopped being updated after Phase 1 | `internal/observability/metrics.go` — `webPrefixes`, and `ClassifySurface`'s fall-through, which returns `SurfaceRedirect` for anything it does not recognise | Noticed while adding `/feeds` and finding there was no list to add it to that anybody had been maintaining. `webPrefixes` holds eight entries — `/login`, `/logout`, `/setup`, `/dashboard`, `/docs`, `/links`, `/keys`, `/account` — and `ClassifySurface` labels everything else that is not `/api/`, `/static/` or an ops endpoint as `redirect`. Every dashboard route added since Phase 1 is therefore mislabelled: `/notifications` (M22), `/workspaces` and `/members` (M25, M28), `/invites` and `/invite/` (M27), `/organizations` (M28), `/signup` and `/verify/` (M29), `/disputes` (M31), `/theme` and `/workspace/` — and `/feeds` would be the twelfth. `dashboardPatterns` in `internal/httpx/router.go` is the list that *is* maintained, and the two have silently diverged. | Minor, and observability-only — no request is served differently. The cost lands on the numbers an operator reads: `linkctrl_http_requests_total{surface="redirect"}` and `linkctrl_http_request_duration_seconds{surface="redirect"}` mix short-link traffic with dashboard page loads whose budget is 250ms rather than 20ms, so the redirect surface's own figures are inflated by whatever the dashboard is doing. The SLO itself is unaffected — that is measured by `linkctrl_redirect_duration_seconds`, which the redirect handler records directly and which this does not touch — so the claim in docs/slo.md stands. Not fixed here because adding `/feeds` alone would make the list wrong in a new way, and the honest fix is to derive the classifier from `dashboardPatterns` rather than to extend a second hand-written copy of it, which is a change to a shipped metric's labels. | No |
| F15 | M31 | Every organization owner on an instance can read every dispute filed on it, and lift a blocklist entry for everybody | `internal/store/migrations/01600_destination_disputes.sql` — the grant of `destinations.review` to the owner role; `internal/dispute/dispute.go` — `List` and `decide`, both scoped by the permission and by nothing else; `internal/store/query/disputes.sql` — `ListDestinationDisputes`, which carries no organization predicate | Built into M31 by the shape of what it governs, and confirmed by `TestTheQueueIsGatedOnTheNewPermission`: the queue query has no `organization_id` filter, and `DeleteBlockedDestination` removes a row from a table [01500](../../internal/store/migrations/01500_destination_blocking.sql) makes instance-wide on purpose. So on an instance with two organizations, A's owner sees that somebody in B tried to link to a given host, sees their address in `filed_by`, and can delete the entry that refuses it for both. With `LINKCTRL_SIGNUP_MODE=open` (M29) that reach is one registration away, because registering provisions an organization and makes the registrant its owner. The queue *cannot* honestly be narrower than the decision — a per-organization view over an instance-wide effect would hide rows the same reader is deciding for — so this is the tenancy model showing through rather than a scoping mistake. | Moderate, and the same shape as something already shipped rather than new: `domains.write` has been granted to owner and admin since 00800 and changes where the link domain's root sends every workspace's stray visitors. This is one degree wider because it decides what every workspace may link to, and because M31 adds a cross-organization *read* that `domains.write` does not have. The root cause is D38's finding that this product has no instance-level principal, so "the instance owner" is not a thing the permission system can name; every fix — an instance-owner flag, an environment-named moderator, or scoping the blocklist per organization and giving up 01500's reasoning — is a design decision rather than a correction. Bounded meanwhile by the shipped signup default (`closed`) and by admins deliberately not holding the permission. | No |
| F14 | M30 | Anyone who can create a link can now write audit rows at will, because every refused destination is recorded | `internal/link/blocking.go` — `Service.recordBlocked`, reached from `checkDestination` on every tiered refusal, on all three destination-writing surfaces | Built into M30 by its own bullet: *every blocked attempt writes an audit event with `ip_prefix` and the attempted URL stored as evidence*. Confirmed by `TestEveryDestinationSurfaceRunsTheFullTierCheck`, which asserts exactly one `destination.blocked` row per refusal. The caller needs `links.create` or `links.update`, so this is not reachable anonymously — but on an instance with open signup (M29) or many members, a holder of an ordinary editor role can loop `POST /api/v1/links` with `http://127.0.0.1/` and add a row per request, each carrying a metadata blob and up to 2 KiB of defanged URL. Nothing rate-limits the write beyond `LINKCTRL_API_RATE_PER_MIN` (600/min by default), and unlike the other eleven audit actions this one records something that did *not* happen, so there is no successful state change bounding how often it can be provoked. | Minor, and bounded rather than unbounded: M21 shipped `LINKCTRL_AUDIT_RETENTION_DAYS` and `LINKCTRL_AUDIT_SIZE_WARN_BYTES`, so growth is trimmed and an operator is warned — but retention defaults to `0` (keep forever, D5), which is the configuration where this costs disk indefinitely. The trail is worth more than the risk, which is why the bullet is right and this is a row rather than a design change; the candidate fixes — a per-actor rate on the record, or collapsing repeated identical refusals into one row with a count — are both behaviour changes to a shipped audit contract and belong to whoever schedules them. | No |
| F13 | M29 | Registration answers whether an address already has an account, which on an open instance is a public enumeration oracle | `internal/signup/signup.go` — `Register`'s `CountUsersByEmail` check, surfaced as `409` by `WriteError`'s `auth.ErrEmailTaken` case in `internal/httpx/problem.go:137` and as "That email address is already registered." by `SignupSubmit` | Reachable without any credential once the effective mode is `open`: `POST /api/v1/auth/register` with a known address answers `409 conflict`, an unknown one answers `202 verification_sent`, and the browser form makes the same distinction in words. Rate limiting slows a sweep but does not remove the signal. The behaviour is not new — the endpoint has answered `409` for a taken address since Phase 1 — but until M29 the endpoint was API-only and there was no browser form; opening self-serve signup is what makes it a surface a stranger reaches. M27 deliberately spent argon2 work to stop redemption answering the same question (D27), so the product already treats this as something worth not disclosing, and the two surfaces now disagree. | Minor, and bounded by the mode: on a `closed` or `invite` instance the endpoint refuses before it looks anything up, and the shipped default is `closed`. On a deliberately open instance it lets somebody test a leaked address list for membership. The honest fix is to answer `202` either way and mail "you already have an account" to a taken address, which is a behaviour change to a documented status code and a second mail template — scope, not a detail, which is why it is deferred rather than taken inside M29. | No |
| F12 | M28.5 | D18's two limbs do not cover the map they govern, so applied literally they would make an irreversible permission delegable | [Plan.md](../../Plan.md#phase-2-decisions-taken-after-the-plan-was-finalised)'s D18 row, against `auth.NonDelegableScopes` in `internal/auth/apikey.go:150` | Found while recording `org.delete`'s delegability, which m28.5.md required be named against D18. D18 says a permission is non-delegable "when reading it exposes an actor's identity tied to network data, **or** when holding it lets a key widen its own reach. Everything else is delegable." `org.delete` does neither — it discloses nothing and removes tenancy rather than granting any — yet it has been in `NonDelegableScopes` since Phase 1's `00700_seed.sql`, on irreversibility. The map's own comment states the wider rule explicitly: "the rule this map encodes is now 'escalating, irreversible, or disclosing' rather than only the first two." So the code and the decision text disagree about what the rule *is*, and the code is the one that has been right since before D18 was written (2026-07-31). | Minor today, and latent rather than active: no permission is mis-classified in the tree, and `org.delete` is correctly non-delegable. The cost is the next milestone that adds an irreversible permission — M44's rotation scopes are the near candidate — applying D18's two limbs as written, finding neither matches, and concluding the scope is delegable. Correcting it means rewording a decision, which is the owner's, and D18 is referenced by name from six milestone files. | No |
| F11 | M28 | `TestInvalidationReachesAnotherReplica` is flaky | `test/integration/invalidation_test.go:276` — "the second replica did not cache the alias in process" | Observed 2026-08-01 while running M28's gates. Two consecutive uncached full runs (`GOFLAGS=-count=1 make test-integration`) of the same tree: the first failed on this assertion, the second passed, and three earlier full runs of the same tree during the milestone passed. Nothing in M28 touches the redirect path, the resolver, Redis or the invalidation subscriber — the milestone's diff is `internal/team`, `internal/httpx`, `internal/ui`, `internal/auth`'s provisioning helper and one migration — so the change under test is not a plausible cause. The assertion is that a *second* replica has populated its in-process tier, which is reached by a prior request whose caching is asynchronous relative to the assertion; that is the shape a timing-dependent assertion has. | Minor, and test-only — no product behaviour is implicated. The cost is a gate that fails for the wrong reason, which is expensive in a loop that treats a failing gate as a stop condition: a flake here reads identically to a real regression in whatever milestone happens to be in flight, and the next person will spend the same run re-running it to find out which. | No |
| F9 | M26.6 | An uncached redirect pays `REDIS_READ_TIMEOUT` twice while Redis is stalled, not once | `internal/redirect/resolver.go` — `fromRedis` (the failed `Get`) and `store` (the `Set` that repopulates), both reached synchronously from `Resolve` | Measured for M26.6 on 2026-07-31, against `redisProxy.blackholed(true)` with a client built the way `internal/platform/redis.Open` builds one (`DialTimeout` 1s, `ReadTimeout` 50ms). A cold `Resolve` took **108ms**; the two following resolves took 0s, because the in-process tier was populated. The 108ms is 50ms of failed lookup, plus the Postgres query, plus 50ms of `Set` against a server that will not answer it either. Both calls are bounded and neither retries, so this is not the compounding M26.6 was scheduled for — that was the invalidation path's three-attempt loop, now bounded — but the `redis.go` comment claiming a stalled Redis "costs a little latency" was written about one call and there are two. | Minor. Nothing is wrong, only slow, and only while Redis is stalled: the redirect is still served from Postgres. The documented uncached target is 100ms and a stalled cache puts a cold redirect just past it, so the cost is an SLO the operator cannot meet during a Redis stall rather than a failure. The obvious fix — skip the repopulating `Set` when the lookup just timed out — is a change to the redirect path and therefore owes the k6 re-run, which is why it is not taken inside a milestone that deliberately left that path alone. | No |
| F8 | M26.6 | The rate limiter's comment blames a stalled Redis on connection establishment, which measurement contradicts | `internal/ratelimit/shared.go:143`–`:144`, in `take`'s doc comment: "does not honour the context deadline go-redis is handed **when it has to establish a connection**" | Measured for M26.6 on 2026-07-31. The context is not honoured on a stalled read whether or not a connection has to be established: a client with `ReadTimeout` 400ms took 401ms to fail a command carrying a 50ms context both against a server that never answered the handshake *and* down a connection established beforehand and then left to go quiet mid-command. What bounds the wait is the client's own `ReadTimeout`; connection establishment has nothing to do with it, and a dial in isolation does honour the deadline (8s dial timeout, 50ms context, 50ms elapsed). The comment's conclusion — enforce the deadline from outside the call — is correct and the code is correct; only the stated reason is wrong, which makes it the kind of comment that misleads the next person deciding whether the same defence is needed somewhere else. | Cosmetic, and comment-only. No behaviour is affected and M24's mechanism is the right one. It is recorded rather than corrected in place because M26.6's bullets reach the invalidation path, the redirect path and F2's row, and none of them reach `internal/ratelimit` — a comment correction there is out of spec, and this repository's rule is that a shipped milestone's claim is reopened by the owner's scheduling, not by the worker who noticed. | No |
| F1 | 0.1.0 release | Release-notes extraction sweeps up the changelog's link-reference block | `.github/workflows/release.yml`, the "Extract this version's changelog section" step | The `awk` runs from `## [<version>]` to the next `## [` heading. The newest version is always the last section in `CHANGELOG.md`, so the trailing reference definitions are inside that range: the published v0.1.0 body carries `[Unreleased]: …` and `[0.1.0]: …` immediately before the `### Container image` section. Confirmed against the live release body, not inferred. | Cosmetic. Markdown renders link *definitions* as nothing, so a reader sees no artefact; the cruft is in the raw body only. It will recur on every release, because the newest section is last every time. | Yes — owner approved 2026-07-31, scheduled as [M45](phase-details/m45.md), which is the only milestone it can sit in: the extraction runs solely inside the release workflow, so the fix has to be checked against CHANGELOG.md as it will be at 0.2.0 before the tag exists rather than confirmed by publishing. |
| F31 | M32.9 | A workspace-scoped admin reads the **entire organization's** audit log, including workspaces where they hold no membership at all | `internal/store/query/audit.sql:33` (`WHERE organization_id = @organization_id`) and `internal/audit/audit.go:313` (`org := actor.OrgID`) | Verified against the live migrated database inside `BEGIN … ROLLBACK`: a user whose only membership is `role=admin, workspace_id=A` resolves `audit.read` and `members.write` in A and **0 permissions** in C, yet `ListAuditLogs` scoped to the organization returns C's rows — a planted `member.added` carrying `{"email":"carol@example.com"}` and a `destination.blocked` carrying `url_defanged`, each with `ip_prefix` and `actor_label`. The stronger form also holds: after the org-wide membership is removed she holds zero permissions in B and C, still resolves into A, and still reads their rows. The verifier corrected the severity argument — member emails are *already* org-wide readable by design, so what is new is `ip_prefix` bound to a named actor, the per-action timeline, and `url_defanged`, for workspaces they do not administer. `audit.sql:26-28` argues the org scope deliberately, but that reasoning is M21's, from when `audit.read` could only be held organization-wide; D31 enumerates the union's costs and does not name this one | **Moderate.** An unexamined composition of two intended decisions rather than an accepted trade-off. Read-only, leaves no trace (reads are not audited), and needs a direct JSON call since no UI page exists. Same root as F27 — the fix for one may not fix the other, since this is a read scope and F27 is a write scope | No |
| F32 | M32.9 | Invitation and email-verification tokens are stored **in clear** in `mail_outbox.body`, so a read of the database yields redeemable credentials | `internal/mail/mail.go:139` (`Body: body`, rendered at enqueue time), reached from `internal/invite/invite.go:749` and `internal/signup/signup.go:288` | Reproduced against a real database: a token pulled out of `mail_outbox.body` **by SQL alone** hashed to `invitations.token_hash` and redeemed into an **admin** membership; the body still contains the token after send, and `PurgeFinishedMail` is age-only (30 days), never scrubbing. Four narrowings the finder missed. (a) It needs `SMTP_HOST` set **and** `SIGNUP_MODE != closed`; under the shipped default the leaked token is **refused** — verified. (b) The signup half is near-harmless: verification ends at the sign-in form and the plaintext password never survived the signup request, so the attacker force-completes a registration they cannot sign in as. (c) `docs/SECURITY.md:45` is an **over-read** — it sits in the Invitations row and describes how the invitation *record* stores its token, which is true. (d) The sentence that actually convicts is `docs/SECURITY.md:241`, which already discloses that the outbox stores each message rendered — so the design is accepted; what is false is its closing clause *"the first mail this ships, the audit-growth warning, contains no secret"*, written in M26 (`13df367`) and broken by M27's `invitation.txt` (`0a5b1fd`) and M29's `verification.txt` (`24b3c89`) without revisiting the paragraph | **Moderate.** Genuinely falsifies `01200_invitations.sql:34-35` — *"A database leak hands over no redeemable invites"* — and literally falsifies `01400_signup.sql:43-44`. Real window is 7 days per unredeemed invitation (`INVITE_TTL`), 24h for verification. On a mailer-configured instance a **read-only** database compromise becomes an authenticated foothold at up to admin. Scrubbing `body` on `MarkMailSent` would collapse it to minutes, at the cost of the operator-inspectability the outbox exists for — a trade worth recording as a decision rather than making silently | No |
| F33 | M32.9 | A dispute stores the host that was **typed**, not the blocklist row that refused it, so the one-open-dispute-per-host bound is defeated by any subdomain — and the queue shows the owner one host while **Allow** acts on another | `internal/dispute/dispute.go:302` (`Host: verdict.Host`), against the unique index at `01600_destination_disputes.sql:77` | Reproduced against a real database: 19 shortener hosts are seeded by default, the rule matches on **suffix**, and `ValidateDestination` performs no DNS lookup — so `a.bit.ly`, `b.bit.ly`, `c.bit.ly` each judge as `low_confidence.shortener_chain`, each open a distinct dispute, and each notifies **every instance owner**. One blocked row for `evil.example` produced two open disputes from `login.` and `mail.` children. The dashboard `POST /disputes` (`router.go:373`) carries **no rate limiter at all**. The verifier found a sharper consequence than the flood: `entryToLift` re-runs `HostCandidates(d.Host)`, so an owner clicking Allow on a dispute rendered `a[.]bit[.]ly` deletes **`bit.ly`** from the instance-wide blocklist, un-blocking it for every workspace — and `Dispute` carries no field naming the row that would be lifted | **Moderate**, top of band. Falsifies `01600_destination_disputes.sql:26-30` in both clauses — *"it is the host the refusal actually matched rather than the one that was typed"* — and its stated bound at `:67-73`. Needs only `links.create`. The decision-integrity half (the string shown is not the string the button acts on) is worth more than the flood | No |
| F34 | M32.9 | The full `FEED_URL` — which the tree elsewhere assumes carries an API key in its query string — is written to the application log at WARN on every transport-level feed failure | `internal/link/blocking.go:444`, via the `*url.Error` wrapped at `internal/feed/feed.go:254` | Reproduced through the real `feed.Client` and the real `observability.NewLogger`: `error="feed AcmeRep: Post \"https://feed.example.com/v1/check?apikey=SUPERSECRET123&mode=strict\": …"`. All four escape routes checked and closed: `slog.Any` renders errors via `Error()` (confirmed in `json_handler.go`); `redactAttrs` (`logging.go:65`) matches on the attribute **key**, which here is `"error"`; and `FEED_URL` is a plain `string`, not `config.Secret` (`config.go:318`). Fires on DNS failure, refused connection, TLS error and timeout — the common one at `FEED_TIMEOUT=2s`. One narrowing: `FEED_METHOD` defaults to **POST**, so the user's destination is only in the URL under `FEED_METHOD=GET`. The killer fact the finder missed is that the boot log already does it right — `cmd/linkctrl/main.go:461` logs `feedClient.Endpoint()`, deliberately query-stripped — and `internal/platform/postgres/postgres.go:62` refuses to wrap for exactly this reason: *"Do not wrap: the error may echo the DSN, which contains the password"* | **Moderate.** A live third-party credential reaching a lower-trust sink on an ordinary error path, precisely when logs get shipped and pasted into tickets. By the project's own DSN standard it is major-class; by absolute impact the secret belongs to a service the operator opted into and grants no access to this instance | No |
| F35 | M32.9 | The feed disclosure strips a credential from the query string but not from the URL's userinfo, so `https://key:secret@feed.example/` is rendered verbatim to **every signed-in user** | `internal/feed/feed.go:185-193` (`Endpoint`), rendered at `internal/ui/templates/pages/feeds.html:57` and returned by `GET /api/v1/feeds` | Reproduced: `Endpoint("https://apikey:SECRET@api.feed.example/v1/check")` returns the string unchanged, while the query-string form is stripped correctly. Config validation (`config.go:828-843`) parses the URL and checks only `u.Scheme == "https"`; `u.User` is never examined, so a userinfo URL validates clean. The counter — *userinfo is operator error, not a defect* — fails four ways: Go's client actually sends Basic auth from `req.URL.User`, so it **works** and the operator gets no warning; `Endpoint`'s own doc comment concedes the premise (*"a feed URL can carry an API key … the disclosure page is shown to every signed-in user"*), making this an incomplete redaction rather than an absent one; `FEED_AUTH_TOKEN` is typed `Secret`, `,unset` and in `FileSecretVars`, so a credential smuggled through `FEED_URL` bypasses the whole discipline; and the template prints the endpoint two lines above the sentence calling it *"the operator's own credential"*. `FeedsPage` is ungated by design (D40) and correctly so | **Moderate.** Converts an operator slip into full third-party-credential disclosure to any authenticated principal, including any stranger who registers under open signup. The clean fix is to **reject** userinfo in `FEED_URL` validation beside the existing https check, leaving `Endpoint`'s redaction as defence in depth | No |
| F36 | M32.9 | An instance-wide administrative act is recorded in exactly one tenant's audit log and is invisible to every tenant it changed | `internal/audit/audit.go:247` (stamps `actor.OrgID`) and `internal/store/query/audit.sql:33` (reads the same) | Verified against live data: the `domains` table holds one row with `organization_id` NULL, and `SetDefaultDomainBotBlocking` / `SetDefaultDomainRootRedirect` both target `WHERE is_default`, so both actions are unambiguously instance-wide. Five `domain.bot_blocking_changed` rows are logged under `Tenant B` and `AdvVerify-…`; the same query run as *Test Owner* — whose links those settings govern, and which has a `link.bot_blocking_changed` row of its own — returns none of them. `block_bots_enforced` was `t` at the time: every link in that organization was being forced to block bots by a change its owner's audit log does not contain. The D38/F15 defence (*there is no instance principal*) does not stick: `audit_logs.organization_id` is **nullable** and the product already uses a NULL org to mean instance-wide for the `domains` row, so writing these with `organization_id = NULL` and widening the read to `… OR organization_id IS NULL` is one predicate and one branch, naming no principal and granting nobody anything | **Moderate**, for accountability rather than confidentiality — nothing leaks in either direction. `docs/SECURITY.md:55` tells the reader *"Both changes are audited"*, and a reader holding `audit.read` acts on that, looks, and finds nothing. Latent until an instance has two organizations. The `dispute.allowed`/`upheld` limb overlaps F15; the domain-settings limb does not | No |
| F37 | M32.9 | `README.md` still says Phase 1 / 0.1.0 and instructs the reader to pin the tag, on the same page that lists twelve Phase 2 milestones' features in the present tense; `Plan.md` says Phase 2 is unstarted | `README.md:9-15` and `:64-66`; `Plan.md:435-437`, `:523-528`, `:701-703` | `git tag -l` returns only `v0.1.0`; every feature row in README's *What you get* table for Audit log, Notifications, Mail, Invitations, Sign-ups, Members, Workspaces, Organizations and Bot blocking sits under `## [Unreleased]`. The *README describes HEAD* defence is killed by a fact the finder never cited: the 0.1.0 CHANGELOG section — the document the status line points at — states its own absences at `CHANGELOG.md:682` and `:687` (*"No audit log behaviour…"*, *"No signup page. … Invitations, and a signup form worth having, are Phase 2"*). So the page names a version, names the changelog as its authority, tells you to pin it, and then lists features that version denies. Also: *"What is deferred to Phase 2 is listed plainly in Not built yet"* is false — 2 of 16 bullets are deferrals, 13 are limitations of shipped Phase 2 features. `Plan.md:435`'s *"As of 2026-07-31"* does not rescue it: nine Phase 2 milestones landed on that same date, so the snapshot was overtaken within its own stamp. `Plan.md:523` says five insertions and lists six. `Plan.md:703`'s caption *"Deliberately accepted in Phase 1"* heads a table where **fifteen** of 26 rows rest on Phase 2 decisions. `docs/configuration.md:177`,`:201` and `docs/usage.md:310` say "Since 0.2.0" for behaviour in no released version | **Moderate to major.** The front door, and it fails on contact: a reader who follows the Quick start and pins `0.1.0` as instructed gets an instance with none of what the page just sold them. Everything here is documentation; nothing in the product misbehaves | No **Widened by [M44.9](phase-details/m44.9.md), 2026-08-04:** add `Plan.md:504-506` — *"One open finding, cosmetic, unreviewed"* — against a queue that held sixty-two. It was filed by the review as a critical release-blocker and refuted down to this line: no gate reads the sentence, the clause before it names deferred-findings.md as the authority for review state, and it sits inside the same Phase-1-captioned section this row already convicts at `Plan.md:703`. |
| F38 | M32.9 | Three operator-facing documents state that all rate limits are per instance, which M24 made false for the credential and API limits — and `docs/configuration.md` contradicts itself 37 lines apart | `docs/SECURITY.md:85`, `docs/deployment.md:408`, `docs/configuration.md:300` and `:320`, against `internal/httpx/limits.go:50-56` | `limits.go` gives `Login` and `API` a `ratelimit.Shared`; only `NotFound` is built plain, with the comment *"Deliberately not shared."* `configuration.md:320` says *"**Per instance.** The limiter is in memory… With N replicas the effective limit is N times the number"*; `:337` says *"**The credential and API limits are shared across replicas** through Redis."* The rescue that `:320` is scoped to the 404 limiter fails: `:309` introduces *"Four properties are worth knowing"*, `:320` is the third, and the fourth is explicitly titled *"The 404 limit charges misses only"* — the document names that limiter whenever it means only that limiter. False rather than incomplete: the project demonstrably knows the degradation phrasing and uses it correctly at `README.md:159`, `Plan.md:709` and `docs/operations.md:292`. `CHANGELOG.md:673` says the same thing inside the `[0.1.0]` section, where it is historically accurate and must not be touched. `internal/ratelimit/ratelimit.go:5-11`'s package doc also still says *"not backed by Redis"*, in the package `shared.go` lives in | **Moderate.** M24's own bullets are satisfied — `Plan.md:140` and `:709` do state the split — so no milestone claim is falsified; these are other texts that lagged. `docs/SECURITY.md` is where a reviewer goes to learn what is guaranteed, and `deployment.md:408` is the paragraph read when deciding to run replicas | No |
| F40 | M32.9 | The theme-token guard scans `templates/**` only, so it cannot see `funcs.go` — the one file `input.css` explicitly names as a class source — and raw palette utilities written there ship unflagged | `internal/ui/theme_test.go:37` (`fs.WalkDir(files, "templates", …)`) against `internal/ui/static/css/input.css:13` (`@source "../../funcs.go";`) and the embed at `internal/ui/ui.go:35` | Demonstrated rather than argued. Rebuilt with the repo's own pinned `bin/tailwindcss` v4.1.14 — output byte-identical to the committed `app.css` (22760 bytes), so the harness is sound. Sabotaged `funcs.go`'s status-badge map to `bg-emerald-50 text-emerald-700 ring-emerald-200`: **both theme tests PASS** while Tailwind emits `.bg-emerald-50{`, `.text-emerald-700{`, `.ring-emerald-200{`, and `statusBadge` renders at four template sites, so the sabotage reaches a browser. Also proved by experiment that `input.css:6-8` — *"The vendored htmx build is deliberately NOT scanned"* — is false as a mechanism: appending `/* "z-[9987]" */` to `htmx.min.js` produced `.z-\[9987\]{` in the output. Removing `static/vendor/` shrinks `app.css` by 798 bytes and drops six utilities that appear nowhere else, so auto-detection is scanning the vendored bundles too | **Minor**, upper end. No live defect — `funcs.go` currently uses tokens only. But m24.5.md stakes the milestone on this test: *"That test, not review vigilance, is what holds every later milestone's UI to both themes"*, naming M31, M37, M38, M41, M43 and M44. The design identified `funcs.go` as the special case and then built the enforcement so it cannot see it | No |
| F41 | M32.9 | `CacheKeyVersion` was not bumped when M32.5 added bot-policy fields, so a snapshot written by a pre-M32.5 binary reads as "do not block" for up to `REDIRECT_TTL` | `internal/redirect/snapshot.go:44-50` and `internal/redirect/resolver.go:24` | Reproduced at the Redis layer: with `{"block_bots":true}` in Postgres, dropping the in-process copy and overwriting the shared entry with the payload a pre-M32.5 binary would write (identical minus the `omitempty` `bb`/`db`) yielded **three 302s to a bot**, and the app never revalidates or rewrites the entry. Half the finding is dead, though: `SetBotBlocking` calls `InvalidateDomain` unconditionally, which sweeps `lc:a:v1:<domain>:*`, so entries written **before** blocking is switched on have a zero stale window. What survives needs an old binary writing entries **while blocking is already on** — reachable because `docs/releasing.md:102` runs migrations at boot before the listener opens, so a rolling restart has old and new containers serving concurrently, and `kindDomain` is new in `fa0a079`, so an old replica hits `apply`'s `default:` branch and neither clears its tier nor stops writing bot-less entries. `docs/releasing.md:120-124` calls in-series rollback *"safe"* and cross-series residue *"harmless"* | **Low.** Fail-open on an opt-in control whose failure mode is the pre-M32.5 status quo — no disclosure, no authz bypass — but silent, bounded only by a 24h TTL, on a sequence the release documentation calls safe. The comment's justification (*"nobody can have switched blocking on yet"*) is what is actually false | No |
| F42 | M32.9 | `Dispute.Liftable` conflates "a blocklist row caused this" with "an allow can succeed", so the review queue draws an **Allow** button on exactly the disputes the code already knows will 409 | `internal/dispute/dispute.go:236` (`liftableRules` admits `operator_blocklist`), `:545` (`entryToLift` refuses `SourceEnv`), `:712`, and `internal/ui/templates/pages/disputes.html:82-89` | Reproduced: `SeedBlocklist(["envblocked.example"])`, judge, file, Allow → `conflict: "envblocked.example" comes from LINKCTRL_DESTINATION_BLOCKLIST …`, dispute still `open`. A source census after a boot seed on a fully migrated database is `map[env:2 shortener:19]` — **no `review` row exists or can exist**, because the only runtime insert hardcodes `'env'` and the seed hardcodes `'shortener'`. The template renders the Allow form on `{{if .Liftable}}` and renders the *"nothing to remove"* warning only when `not .Liftable`, so an env-sourced dispute gets the button and not the warning. The 409 itself is deliberate, tested (`dispute_test.go:487`) and documented — it is the button that is wrong. M30's tier table is **not** falsified: a shortener row lifts cleanly, verified. Related and folded in: `01500:45` says `'review'` is *"what M31's owner review **will** write"* and `created_by` is written by no statement in the tree, which is the mechanical reason nobody noticed | **Minor to moderate.** Contradicts `dispute.go:140-145` — *"The page draws its buttons from this rather than making the owner discover it by clicking"* — for the most likely dispute an operator-configured instance will see, and `webError` turns the 409 into an error page rather than in-context guidance. Same family as F21: a control that looks like it does something and does not | No |
| F43 | M32.9 | An API key whose owner lost their membership still authenticates, still carries that organization's tenancy, and reaches one state-changing endpoint outside its own scopes | `internal/auth/apikey.go:426-451` — the `workspace_id != nil` branch checks no membership, while the `else` branch that does is unreachable because `Create` always sets it | Reproduced in a rolled-back transaction: after `DeleteMembership`, `GetAPIKeyByPrefix` still resolves the key, `GetUserPermissions` returns 0, and `resolveWorkspace` — the dead branch — would have refused. The finder's stated contradiction is **wrong**: `apikey.go:396-399` promises only that *permissions* are recomputed, and they are, to empty. The real contradiction is `apikey.go:439-444`, the comment on the dead branch, which names this exact state and prescribes *"Answering 'invalid' is honest for both"* — applied only where it cannot be reached — and `team_test.go:1004`, which asserts an orphaned identity must carry neither org nor workspace, on the session path. The impact bound the finder gave is also wrong: sweeping every route found `POST /api/v1/organizations` **reachable and writing**, because `CreateOrganization` bypasses `orgs.create` when `CountUserMemberships` is 0 (the D36 first-organization path), carries no `requireSessionActor`, and `orgs.create` is not in `NonDelegableScopes` — so a key scoped to `links.read` alone can create an organization and own it. One-shot | **Minor.** No tenant data reachable in either direction; the org-creation door is a scope bypass rather than a privilege escalation, since the same human can do it with a session by design. Kept above cosmetic because a tested invariant is violated on a path the test does not cover, and an administrator who removes somebody reasonably believes their credentials into the tenant are inert | No |
| F44 | M32.9 | No account deletion or GDPR erasure exists anywhere, while the schema and four other sites describe both as behaviour that exists — and two tables holding email addresses have no deletion path of any kind | `internal/store/migrations/00200_identity.sql:35-37` (`anonymized_at`, *"Set by the GDPR erasure routine"*), and `users` / `destination_disputes` | `anonymized_at` has no writer: grep returns the column, the sqlc model field and three `SELECT *` scan lists. `users` appears in none of the 14 `DELETE FROM` statements and nothing writes `users.deleted_at`. Four further sites assert deletion as existing: `docs/SECURITY.md:51`, `internal/audit/audit.go:277`, `01200_invitations.sql:38` and `01600_destination_disputes.sql:56`. The same migration marks `mfa_secret` *"Phase 3"* four lines earlier, so the file knows how to mark something forward-looking and did not here. Two corrections to the finder's enumeration: `audit_logs` **is** purgeable — it is in `PartitionedTables` and governed by `NewRetentionPolicy`, just defaulted to 0 and documented that way in three places — and `invitations` and `notifications` **do** cascade from `organizations`/`users`. What is genuinely undeletable is `users`, and `destination_disputes`, which by explicit design has *no foreign keys at all* (`01600:49-52`) and no purge. `reserved_aliases` also lacks a purge but holds no personal data | **Low to moderate.** Nothing exploitable. Its cost is that the project's own convention — every deferral is written down — has a hole exactly where a compliance reader would look: erasure appears in neither Plan.md's scope rows nor *Not in Phase 2*. `Plan.md:426`'s framing (*"the largest table … is out of scope for subject-access and erasure requests"*) reads as though the other tables are in scope | No |
| F45 | M32.9 | Nine documentation and comment enumerations have drifted from the code they describe, each in a list that presents itself as complete | `docs/SECURITY.md:50`; `docs/deployment.md:89` and `docs/configuration.md:41`; `docs/configuration.md:520`; `docs/operations.md:52`; `README.md:88`; `docs/usage.md:12-21`; `docs/releasing.md:68`; `docs/operations.md:234`; `api/openapi.yaml:770`; `internal/audit/audit.go:57`; `internal/mail/mail.go:56` and `docs/operations.md:150`; `docs/operations.md:145-153` | Each verified both ways. **Non-delegable scopes**: SECURITY.md names four of five (`apikeys.*` is a glob covering two, so the shortfall is one — `destinations.review`), and the same file asserts it at `:158`; `api/openapi.yaml:2523` carries the identical omission. Note F10 was closed as fixed while two of the three files its evidence cited went stale. **`_FILE` secrets**: two documented, `FileSecretVars` holds four (`SMTP_PASSWORD`, `FEED_AUTH_TOKEN` missing), and configuration.md documents them in their own rows while its summary line does not. **Removed variables**: the table's preamble presents itself as the set startup warns about; `RemovedInUse` warns on five, the table lists three, and `SECRET_KEY` appears in no markdown file in the repo. **`cache` label**: documented as five values, the domain is six — `"none"` is emitted at `redirect.go:170`, the *only* site producing `outcome="error"`, which is the series `operations.md:105` tells you to alert on. **README audit list**: omits `dispute.allowed`/`dispute.upheld` (distinct from F17, which is scoped to SECURITY.md). **usage.md page table**: eight rows, omits `/members`, `/workspaces`, `/invites`, which share the identity menu with three pages that are listed. **Release archive**: three files listed, `Makefile:410` copies four. **Prometheus recipe**: `> 5e9` against a `5368709120` default — same variable feeds both, so the comparison is real; written in M21 as a forward reference before `SizeWarnBytes` existed. **`actor_id`**: openapi says *"Present only while the acting user still exists"*; nothing ever clears it. **Audit comment**: cites *"a filter on `action` [that] the read API already supports"*, written a day after the read API shipped with no such filter and no index on `action`. **Mail backoff**: documented 1m–16m; `Backoff(5)` is unreachable because the terminal check precedes it, so the real span is 15 minutes. **`signup-purge`**: runs, exports `linkctrl_job_runs_total`, and is the only `withLeadership` job with no row in operations.md's table | **Minor**, individually and collectively — documentation and comments only, nothing misbehaves. Filed as one row because they share a cause and a fix: lists that claim completeness and are maintained by hand. The `cache` label and the `_FILE` entries are the two with operational teeth | No **Widened by [M44.9](phase-details/m44.9.md), 2026-08-04:** two of the enumerations grew. The README audit list is now short by sixteen actions rather than two — the true count is thirty-one, twenty-nine in `internal/audit/audit.go` plus two in `internal/dispute/dispute.go:113-114` — and `README.md:97` still says the rest *"arrive with the Phase 2 features that produce them"* while `:88`, `:90` and `:96` of the same table say they are already audited. The never-grantable-scope lists are short by M42's `webhooks.write` and M43's `automation.write`: `docs/usage.md:379-380` names five of seven, `api/openapi.yaml:5215-5219` fewer. Also `docs/cli.md:284-288`'s `demo --reset` inventory omits that it deletes every API key in the organization, and `internal/audit/audit.go:78-81` still cites an action filter the read API has never had. |
| F46 | M32.9 | In the dark theme, white text on the two `*-hover` surface tokens fails WCAG AA — `accent-hover` 4.47:1 and `danger-hover` 3.67:1 — and neither pair is in the recorded contrast figures for either theme | `internal/ui/static/css/input.css:179` and `:193`, used with `--t-on-accent` on 21 buttons | Luminance recomputed independently. The resting states **pass** in dark (`accent` 6.29:1, `danger` 4.70:1) and are recorded at `input.css:93` and `:97`, so this is not the headline claim failing — it is two pairs that exist only in a transient state and were never measured. Both misses clear AA-large 3:1, and hover is unreachable by keyboard. WCAG 2.1 SC 1.4.3 has no hover exemption, and D21 restates the bar without a state carve-out | **Minor**, low end. Falsifies m24.5.md's *"Every token pair meets WCAG AA contrast (4.5:1 for body text) in both themes, recorded next to the token definitions"* — an unqualified *every* that two pairs escape. `#f43f5e` is rose-500, exactly what the milestone's own risk section predicted would drift on dark surfaces | No |
| F47 | M32.9 | For an account that belongs to nothing, the notification bell's **View all** link is a dead end — `/notifications` sits behind `RequireOrganization` and 303s straight back | `internal/ui/templates/partials/nav.html:224`, against `internal/httpx/router.go:344` + `:287` and `internal/httpx/web_team.go:469` | Rendered the orphan page rather than reasoning about it: the only href not gated on anything is `/notifications`. `layout.html:33` invokes the bell unconditionally while the identity menu beside it gates Account and Feeds on `HasOrganization`. The finder's premise that organization deletion is soft is **false** (`organizations.sql:71-83` is a hard delete that cascades) — the conclusion survives for a better reason: `notifications.workspace_id` is nullable and all four producers leave it nil, so the workspaces cascade never reaches a notification row, and the count/preview queries are user-scoped. The orphan is also the *likeliest* holder: deleting an organization needs the owner role, and owners are the recipient class for audit-growth, invite-accepted and dispute-filed. `TestTheHeaderOffersNothingToAnAccountThatBelongsToNothing` cuts the nav *before* the bell, so despite its name it cannot see this | **Minor.** No authorization consequence — `RequireOrganization` is explicitly *"an affordance and not the authorization boundary"*. One dead control in a state whose design premise is that a nav made of redirects reads as broken, and the test written to enforce that premise stops one `div` short | No |
| F48 | M32.9 | The root-redirect cache refills on the **application** pool with no timeout and no singleflight, from a mux to which no `RequestTimeout` is applied | `internal/httpx/rootredirect.go:57-71`, with `cmd/linkctrl/main.go:468`/`:506` and `internal/httpx/router.go:438`/`:522` | All three absences verified. Bounded, which changes the emphasis: the path exists **only on split-host deployments** (`registerRoot` is called solely inside the `SplitHosts()` branch), the query is a unique-partial-index lookup on a table holding one row, and `WriteTimeout` (30s) does bound it. So the singleflight half is near-costless — a few duplicate index lookups per TTL, not a herd. The half that matters is the pool crossing: `main.go:278-280` says the redirect pool exists *"so a slow analytics query on the application pool cannot leave a redirect waiting to acquire a connection"*, and this is a redirect-tree path acquiring from `pools.App`. M23 makes the miss fleet-simultaneous, since `Subscriber.flush()` clears every replica's root cache on every re-establishment including the first | **Minor.** A stated architectural guarantee with a real hole, biting only on split-host under app-pool exhaustion, blast radius one URL rather than the alias traffic carrying the SLO | No |
| F49 | M32.9 | The audit-growth warning mails and notifies the owners of **every organization on the instance** with the shared `audit_logs` size, repeating weekly, to people who cannot act on it | `internal/notify/notify.go:558-610`, via `ListOrganizationIDs` → `OwnersOf` | Fan-out verified end to end; the size comes from `partitions.go:349`, which has no organization predicate. Under `SIGNUP_MODE=open` every registrant is an owner (`signup.go:374`, D6), so the recipient set is every account. `EveryOwner`'s justifying comment (`notify.go:212-217`) rests entirely on *"the people who can act"* — correct for disputes, and false here: `AUDIT_RETENTION_DAYS` is an environment variable with no non-config consumer, no dashboard control, no API. The codebase argues against itself twice: `notify_test.go:322` and D19 both say *"An editor cannot change the retention setting, so telling them is noise in the inbox of somebody who cannot act on it"* — a rule that, applied honestly, excludes owners too. It was true when written; M28 made owner ≠ operator and M29 made owner = anybody who registered, and the recipient list was never revisited | **Minor.** An aggregate byte count is not tenant data, and `SECURITY.md:245` correctly says the mail carries no secret. The cost is a repeating emailed operational alert fanned out to N−1 parties who cannot act, plus a decision-log entry and a passing test asserting a rule the code no longer follows. Three fixes name no instance principal, so this is separable from D38/F15 | No |
| F50 | M32.9 | With bot blocking on, a blocked bot writes a click event for **expired and archived** links, which recorded nothing before M32.5 | `internal/httpx/redirect.go:188-198` (the gate calls `h.record`) against `:208-216` (`OutcomeGone` returns without recording) | Reproduced both directions on one link pair. Blocking off: expired 410, archived 404, `clicks=0`, `click_count=0`. Blocking on: both 403, `expired clicks=6 archived clicks=3`, and `click_count` moved to match. Analytics are unaffected — every recorded row carries `is_bot`, and `link_click_daily` non-bot clicks stayed 0 — but `links.click_count` is rendered raw at `links_table.html:29` and `dashboard.html:45` and is the `sort=clicks` key, so an archived link can accrue a visible count from crawler traffic and outrank active links. No milestone claim is falsified: m32.5.md:64 states *"A blocked attempt is counted, not audited"* unconditionally. The direction is safe — `click_count > 0` gates both `ReserveAlias` and `PurgeExpiredLinks`, so both alias-protection paths become more protective | **Minor.** An unstated consequence of a stated rule rather than a contradiction. Identical traffic is recorded or not depending on a setting about *responses*; the honest fix is a decision — record for every state or none — not a correction. F24's family, extended to links that previously counted nothing | No |
| F51 | M32.9 | `invite.Redeem` verifies a password with no per-account lockout and no failed-login record | `internal/invite/invite.go:588-601`, against `internal/auth/service.go:299-327` | The headline is a conflation and dies: the per-IP login limiter **does** cover redemption — `router.go:204` wraps it in the *same* `"login"` bucket as `/auth/login`, deliberately, so alternating cannot double the budget. Verified live: 8 wrong guesses returned 404 with `failed_login_count=0` and `locked_until=NULL`; a burst of 20 returned 429 from the sixth. What survives is narrow: redeem lacks the per-**account** lockout that login also carries, so a *distributed* attacker holding a live, address-bound, single-use 32-byte token can guess that one account's password at per-IP-limited speed without tripping the cross-source lock. D27 weighs enumeration, not brute force, so this is genuinely unweighed | **Low.** Heavily gated by possession of a scarce token, and a successful guess yields nothing beyond what the same guess at `/auth/login` yields. Worth parity — record failures and honour the lock — not urgency | No |
| F52 | M32.9 | Clearing `SMTP_HOST` on an instance that had one strands every queued message forever: the drain and the purge are gated on the same nil mailer | `cmd/linkctrl/jobs.go:189-191` and `:370-376` | Both guards confirmed. The package comment at `internal/mail/mail.go:9-11` — *"an instance with no `SMTP_HOST` … the outbox stays empty"* — is the actual defect: true only if SMTP was never configured, false across a configured-then-removed transition. The finder's *stronger half* is **not** an independent finding: `PurgeFinishedMail`'s `status <> 'pending'` is correct by design, because with a mailer no row stays pending — `Drain` claims and leases each one and transitions it to `sent` or, after 5 attempts, `failed`, and the lease recovers crash-interrupted rows. So the whole finding reduces to the SMTP-removal transition | **Low.** The relay is gone, so nothing deliverable is lost; the result is orphaned invisible rows (`Service.Pending` has no non-test caller) bounded by what was enqueued before removal, produced only by a deliberate uncommon operator action. Best fixed as an invariant correction or a one-time drain-then-purge on teardown | No |
| F53 | M32.9 | An address that passes `auth.ValidateEmail` but fails `net/mail.ParseAddress` commits a `pending_registrations` row and then answers HTTP 500 — a status the spec does not declare | `internal/signup/signup.go:279-293` via `internal/mail/mail.go:119-124` | Divergence measured: `a<b@c.de`, `a,b@c.de`, `user@exa(mple.com` and six more pass the regex and fail `ParseAddress`. The transaction commits *before* the enqueue, and neither `WriteError` nor `webError` maps the wrapped error, so both fall to `default` → 500 plus an Error-level log per attempt. `api/openapi.yaml:146-164` declares 202/403/409/422/429 and no 5xx. Full precondition: `SIGNUP_MODE=open` **and** a configured mailer — on a default instance `Register` refuses with 403 before validating. The orphan row is not a leak: it expires after `VerificationTTL` (24h) and the hourly `signup-purge` removes it | **Minor.** A genuine contract break plus log noise, self-limited to the attacker's own malformed input. Clean fix is to run the address through `ParseAddress` before the transaction commits, or map the enqueue failure to 422 | No |
| F54 | M32.9 | The invitation email tells every recipient *"If you do not have an account yet, that form creates one"* — false on a `closed` instance, and the one surface in the flow that cannot derive the answer | `internal/ui/templates/mail/invitation.txt:12` | Unconditional, no `{{if}}` in the file. `Create` never reads `s.cfg.NewAccounts` and mails whenever a relay exists; `RenderMail` is passed six keys, none of them the mode, so the template could not branch. The redemption page says the opposite for the same instance (`invite.html:41`). A closed instance with a relay is an intended, documented state (`docs/configuration.md:285`), and the enforcement lives in redemption, not issuance, so `Create` is fully reachable there. Folded in: `invites.html:14-15` renders *"No mail relay is configured on this instance"* whenever `Emailed` is false, which is also the case when the outbox insert fails on a perfectly healthy mail-configured instance — the page already carries `MailConfigured` and the else-branch does not use it | **Minor.** A third copy of the D7 question, adjacent to F19 which covers the page-vs-service pair. Nobody is wrongly admitted — the landing page tells the truth — so the cost is one contradicted sentence to a stranger, and one misdirected operator | No |
| F55 | M32.9 | `internal/analytics/visitor.go` carries a dead duplicate of `AnonymizeIP`, statement-for-statement identical to the live `auth` copy, with its own near-duplicate test | `internal/analytics/visitor.go:82-101` against `internal/auth/session.go:167-186` | `grep -rn "analytics\.AnonymizeIP"` returns nothing; within the package it is referenced only by `visitor_test.go`. Both live callers use the `auth` copy (`internal/audit/audit.go:264`, `internal/auth/service.go:365`). The two functions differ only in comment wording. Found while verifying the 6to4 truncation gap — which is the exact kind of one-line fix that would land in one copy and leave the other passing its own green tests | **Minor.** No current exposure; it is a fix-divergence trap sitting on the privacy boundary. Deleting the dead copy is strictly better than syncing it | No |
| F56 | M32.9 | `NonDelegableScopes` is enforced only at mint time, so adding an already-granted permission to it would not disarm keys already issued — and D18 says otherwise | `internal/auth/apikey.go:513` (the only enforcement, in `resolveScopes`) and `:457` (`Authenticate` re-filters against the owner's role and nothing else) | Six non-test references, all accounted for; `web_keys.go:55` is a UI filter on the create form, not a request-time check. Never exploitable: `git log -S` confirms all five entries landed in the same commit as the migration creating their permission (`a0f23ff` for `audit.read`/00900, `e8a6b1c` for `destinations.review`/01600, Phase-1 seed for the rest), so no key has ever held a scope that later became non-delegable. The finder's cited claim is an over-read — `apikey.go:147-149` is explicitly about the *deleting* direction and is true there. The sentence that is inaccurate is D18's: *"Flipping a permission in **either direction** stays a one-line map edit"* | **Note / informational.** No defect in shipped behaviour. Worth an accurate one-line correction to D18, or an `isNonDelegable` filter in `restrictTo` as belt-and-braces. The near candidate that would trip it is `domains.write` | No |
| F57 | M32.9 | The shared rate limiter writes the client's full IPv4 address into a Redis key, and nothing tells an operator pointing at a managed Redis that they are now storing addresses | `internal/ratelimit/ratelimit.go:299-310` (`Key`), composed at `internal/ratelimit/shared.go:175` | Confirmed live, read-only, on the running instance: `redis-cli --scan --pattern 'lc:rl:*'` → `lc:rl:api:172.18.0.1`, TTL 48s of a computed 120s. Two counters land and reframe it: a Redis key is not a column, so the stance (*"No IP column anywhere"*) is not literally violated — every corroborating doc scopes itself to storage; and the `/64` for IPv6 is evasion resistance, not privacy (`Key`'s own doc: *"Handing out /64s to single hosts is normal"*), so anonymising the v4 side to /24 is **not** an available fix — it would let one host throttle 255 neighbours. Keyed hashing is the only coherent fix. What survives: the shipped compose is memory-only, but `LINKCTRL_REDIS_URL` is operator-configurable and managed Redis commonly snapshots by default | **Low.** Bounded to 120 seconds in a store the design treats as disposable. The actionable half is documentation: the privacy stance should say what it does about Redis, and `docs/deployment.md`'s managed-Redis guidance should tell operators to disable persistence | No |
| F58 | M32.9 | `SeedBlocklist` accepts malformed `LINKCTRL_DESTINATION_BLOCKLIST` entries silently, inserting rows that can never match | `internal/link/blocking.go:688-708` — lowercase, trim, insert; `checkListEntry` is never called | Reproduced: `HostCandidates` only ever produces dot-delimited fragments of a real hostname, so a stored `https://evil.example` or `*.evil.example` row is unreachable by `MatchBlockedDestination`. Two narrowings. The panic-on-malformed promise at `blocking.go:170-173` is scoped, in both places it appears, to the **embedded** list — generalising it to the env list is inference. And it is not a regression: the pre-M30 in-memory implementation validated nothing either, which is what m30.md's only claim here (*"the env list keeps working"*) promises. The implied fix is also wrong: calling `checkListEntry` would reject IP-literal entries that currently work | **Low.** Operator-authored and self-inflicted; it cannot make the instance refuse less than before M30. The cost is silence — the operator sees their entry in the table and it refuses nothing — one function call away from the boot-time rigour `main.go:508-517` spends on exactly this failure mode | No |
| F59 | M32.9 | Two narrow privacy truncation gaps: `/48` preserves the entire client IPv4 for 6to4 addresses, and the in-memory salt cache retains salts past the database deletion described as the de-identification step | `internal/auth/session.go:167-186`; `internal/analytics/salt.go:86-96` and `:138-153` | Arithmetic reproduced: `2002:cb00:712a::1` → `2002:cb00:712a::/48`, whose bytes 2–5 are `203.0.113.42`. Every other v4-in-v6 scheme was tested and is safe — Teredo, NAT64 and ISATAP all embed in the low bits that `/48` discards, and `::ffff:` is folded explicitly with a comment naming the bug class. 6to4 is a single blind spot against a population RFC 7526 deprecated in 2015. The salt half was corrected in the **worse** direction: `Purge` evicts to `today−2` in the same call that deleted `today−2`'s row, so the leader is affected too and it reproduces on the single-replica default; and the eviction inside `For` runs only on a cache miss, so a replica that stops receiving clicks never trims. But the category is over-read: `README.md:126` and `salt.go:135` are claims about what a **database** holds — an erasure request, a backup, a dump — and a heap copy in a live process is none of those | **Trivial**, both. Filed together because both are one-line corrections on the privacy boundary that cost nothing to take, and because the salt one is the kind of off-by-one that makes a proud sentence not quite true. Neither is at risk from any attacker who does not already have more | No |
| F60 | M27 | An API key holding `members.write` can promote an existing member to `admin` — F29's escalation through role assignment rather than through an invitation, and D43 bounds only the invitation door | `internal/team/member.go:107` (`ChangeRole`) and `:261` (`Grant`), reachable over the bearer path at `PATCH /api/v1/members/{id}` and `POST /api/v1/members` (`internal/httpx/router.go:212-213`). The claim it falsifies is `internal/httpx/api_team.go:23`, *"none lets a key widen its own reach, because a key's permissions are its scopes intersected with its owner's role on every request"* — true of the key, and the point of F29 is that the principal it produces is not the key | Read from the code while building M27's fix, not reproduced live. Both call sites gate on `members.write` alone and bound the role only by the actor's rank, which a key inherits from its creator: `resolveRole` caps the granted role at the actor's own rank, `manageable` requires the target strictly below it. So a key an owner minted with `members.write` alone can re-role an existing editor or viewer membership to `admin`, and that person's next interactive session holds `apikeys.read`, `apikeys.write` and `audit.read` — scopes the key may not hold. Neither call site branches on credential type; `requireSessionActor` appears only in `internal/auth/apikey.go` and `internal/auth/workspace.go` (whole tree swept for `IsAPIKey`, 2026-08-02) | **Moderate**, below F29. It needs an account that already exists **and** is already a member of the organization, so it elevates somebody rather than manufacturing them; the change is audited as `member.role_changed` with the key's identity, where an invitation-minted account arrived with only its own redemption record. Same precondition as F29: an owner- or admin-issued key | No |
| F61 | M27 | The API-keys claim in `docs/SECURITY.md` enumerates four of the five never-delegable scopes, reading as exhaustive | `docs/SECURITY.md:50` — "`apikeys.*`, `org.delete` and `audit.read` are **not delegable**", with the reasons given as "the first two … the third …" | `auth.NonDelegableScopes` (`internal/auth/apikey.go:157`) holds five entries; `destinations.review` is the one missing. It is not undocumented — `docs/SECURITY.md:156` states it plainly in the disputes section, and `docs/usage.md:178` lists all five — so this is one row in the claims table that has not kept up with M31, not a gap in the document. Same class as F10, one file over | **Minor.** Nothing is reachable that should not be: the map is what enforces this and it is complete. The cost is a reader taking the claims table as the list | No **Widened by [M44.9](phase-details/m44.9.md), 2026-08-04:** `auth.NonDelegableScopes` now holds seven, not four — M42 added `webhooks.write` and M43 added `automation.write`, both on the durability-of-reach limb recorded in D80 and D85 — while `docs/SECURITY.md:51` still names four. The same file states both new scopes correctly at `:60` and `:61`, so it contradicts itself rather than merely lagging. |
| F62 | M27 | The invite form's role list mirrors D28's rank ceiling and not D43's cap, so a caller authenticated with an API key is offered `owner` and `admin` and gets a `403` on submit | `internal/invite/invite.go:450` (`Roles`, which filters on `r.Rank < actor.RoleRank` and nothing else), rendered by `internal/httpx/web_invitations.go:55`. A key reaches that page because `signedIn` (`internal/httpx/router.go:286`) is `RequireWebAuth`+`RequireOrganization`, `RequireWebAuth` (`internal/httpx/web.go:290`) tests only that an identity exists, and `BearerAuth` is applied outside `Session` (`router.go:413`) so a bearer token attaches one | Read from the code at M27's step 3.4, not reproduced live — it was raised as the worker's stated reason for not filtering `Roles`, *"the role form is signedIn-only so no key can reach it"*, and that reason is false. Bounded, and the bound is why this is not in spec: `POST /invites` and `POST /api/v1/invitations` both call `invite.Service.Create`, where the D43 cap sits (`invite.go:293`), so the only reachable outcome is a refusal at submit. `Roles`'s own doc — *"a form cannot offer something the ceiling check will then refuse"* — names **the ceiling check**, which `Roles` does apply, so D43 leaves it incomplete rather than false | **Trivial.** A form offering an option that is then refused, to a caller class that has no form. `web_keys.go:55` is the same shape done the other way — a UI filter mirroring `NonDelegableScopes` — which is what makes the absent mirror here worth one row rather than none | No |
| F63 | M28 | `CreateWorkspace` gates on the identity's `workspace.write`, so a **workspace-scoped** admin adds workspaces to the whole organization — the one member-adjacent write M28's reopening left on the identity's union rather than on the membership that carried the permission (D44) | `internal/team/workspace.go:88` — `if !actor.Can(PermWorkspaceWrite)`, against `RenameWorkspace` and `DeleteWorkspace` immediately below it, which both resolve the permission against the *target* workspace through `writableWorkspace` | Read against the tree while building D44's bound, and the function's own doc comment states the mismatch: *"the authority being exercised is over the organization the caller is currently in"* — which is exactly the authority a workspace-scoped membership does not carry. `internal/store/query/members.sql` says a workspace-scoped membership grants authority over one workspace, and `00700_seed.sql` grants `workspace.write` to `owner` and `admin`, so any workspace-scoped admin reaches it. Not reproduced end to end: the D44 tests added in M28 cover the membership writes and stop where this begins | **Low.** It creates rather than reads or destroys, the new workspace is one the actor holds no membership in and therefore cannot act in, and no existing data is reachable through it — the cost is organization-wide clutter an owner must clean up, not access. Worth closing for consistency: after D44 it is the one write in the package that answers the old question | No |
| F64 | M33 | `ServeMux`'s own path-cleaning redirect is the one response on the redirect tree that carries none of the tree's headers — no `nosniff`, no `X-Robots-Tag`, no `Cache-Control` — and it serves an HTML body | `net/http`, `ServeMux.findHandler`; the rule it sits outside is stated at `internal/httpx/redirect.go`, `errorPage` — *"the redirect tree is outside the security-header chain, so every response it writes sets nosniff itself"* | Reproduced on the built image: `GET /ld1//deep` answers `307 Temporary Redirect`, `Content-Type: text/html; charset=utf-8`, `Location: /ld1/deep`, 45 bytes of HTML, and **none** of the three headers every other response on that tree sets. Same for `/ld1/../ld2` → `/ld2`. `ServeMux` cleans the escaped path before dispatching and answers the redirect itself, so no handler in this repository is involved and nothing the project writes can add headers to it. **Pre-existing rather than caused by M33** — `cleanPath` applies to every request whatever patterns are registered, so `/foo/../bar` behaved this way before the deep-link route existed. What M33 changes is how many paths reach it and end somewhere real: before, a cleaned two-segment path redirected and then 404ed. Verified **not** a 301, which would have broken the absolute *redirects are 302, never 301* rule: it is 307. The body is Go-escaped, so the reflected path is not an injection vector | **Minor.** No alias or destination is disclosed that the request did not already contain, and the missing header with any bite — `nosniff` on a `text/html` body — protects against sniffing content that is already HTML and already escaped. The cost is that the tree's stated invariant is not actually true of every response it produces, and the honest fix is either to wrap the mux so the headers are set on the way out or to amend the claim | No |
| F65 | M35 | `RedirectHandler.Logger` is dereferenced without a nil check on the redirect path's failure branches, so a handler constructed without one panics on the request it was trying to report | `internal/httpx/redirect.go:206` — `h.Logger.Error("redirect resolution failed", …)`, reached when `Resolver.Resolve` returns an error | Found while wiring M35's gate code, which logs on branches a fixture actually takes. The gate code is nil-tolerant through a `log()` accessor added for this reason (`internal/httpx/redirect_gates.go:52-59`); the pre-existing call above it is not, and neither is the sampled-success line at `:334`. A nil `*slog.Logger` panics inside `slog.(*Logger).Enabled`, which is what the M35 signature test hit before the accessor existed — the connection was closed mid-response and the client saw `EOF`. Production always sets `Logger`, so the reachable case is a fixture or an embedder of `httpx`; the panic is nonetheless on the hot path's 503 branch, i.e. the branch that runs when Postgres is already unwell | Minor. Unreachable in `cmd/linkctrl`, which always sets the field, and every failure branch is one a healthy instance does not take. It is a latent crash in the one place that must degrade rather than fail, and the fix is three lines — use the accessor that now exists | No |
| F66 | M35 | [M34](phase-details/m34.md)'s four decisions are in [decisions.md](decisions.md) and **absent from Plan.md's decision table**, which the phase-details README calls the place they are *"recorded in full"* | `Plan.md`'s post-finalisation decision table runs D44…D48 then jumps to D53; `docs/build-notes/decisions.md:8738`, `:8761`, `:8801`, `:8827` define D49, D50, D51 and D52 under the M34 entry | Found while repairing a numbering collision at M35's step 3.4. The orchestrator minted D49 and D50 for M35 at validation, having checked Plan.md's table — which was the only place that looked authoritative and which M34's worker had not written to. decisions.md is append-only and M34's entry is committed at `5409e94`, so the collision was resolved by renumbering M35's pair to **D53/D54** across Plan.md, decisions.md, `docs/SECURITY.md`, six Go files, two embedded HTML pages and `test/integration/gates_test.go`; M34's D49–D52 are untouched. The residue is the gap: four decisions the README says are recorded in full in Plan.md are not in it | **Minor**, and process rather than product — no code behaves differently. It cost one collision already, and it will cost another the next time somebody reads Plan.md's table to find the next free number, which is exactly what happened here | No |
| F67 | M36 | The per-link rule ceiling and the one-kind-per-split rule are both **read outside the transaction that writes**, so two concurrent creates can pass a check neither would pass alone | `internal/link/routing.go:144` — `CountRoutingRules` is called before `s.pool.Begin` in `CreateRule` (M34); `internal/link/split.go` — `CreateVariant` reads `CountRoutingRules` and `loadSplit` the same way, before its own `Begin` | Noticed while writing `CreateVariant`, which replicates M34's pattern deliberately so that the two writers behave identically. The window is real: two `POST /links/{id}/rules` at 20 rules each see 19, both insert, and the link ends with 21 — past the ceiling that exists because the whole list travels inside the cached snapshot and is walked on every uncached resolve. The split version is worse in kind rather than in size: two concurrent `POST /links/{id}/split`, one `weighted` and one `sequential`, both read a link with no arms, `ValidateSplitKind` passes for both, and the link ends with a **mixed split** — the exact state the validation exists to make impossible. The redirect path survives it (`Snapshot.Split` takes the first arm's kind and ignores the other), but the state should not be reachable. No `SELECT … FOR UPDATE`, no advisory lock and no unique constraint stands between them | **Minor.** Needs two writes to the same link inside a few milliseconds, which is a person double-submitting a form or a script, not ordinary use; and the worst outcome is a slightly over-long rule list or a split whose second kind is silently ignored rather than anything a visitor can observe as wrong. The fix is to move both reads inside the transaction — a `FOR UPDATE` on the link row would serialise every writer for that link — and it belongs to both milestones rather than to one, which is why it is a row rather than an in-spec fix here | No |
| F68 | M36 | `demoReset` scopes three of its statements to one workspace and the rest to the organization, so an actor resolving anywhere else commits a half-reset that reports success — M36 removed the reachable path, not the asymmetry | `cmd/lctl/demo_data.go:63`, `:68`, `:73` — `link_tags`, `destinations` and `links` are deleted `WHERE workspace_id = $1`, while the rollups (`:87`), the click events (`:80`) and everything in `demoResetPhase2` are deleted by organization or by address | This is the defect that failed `make demo-update` at M36, diagnosed from the demo database: twenty-one catalogue links, their destinations and M34's four routing rules still present, and no click events, no rollups, no invitations, no seeded accounts and no second workspace. The reset had committed. M36 fixed it by pinning the actor (`demoActor`, D61), which closes the two ways the workspace could differ — the M25 switcher, and a run that dies between the seeder entering the second workspace and leaving it. The asymmetry itself remains, and one path to it survives: soft-deleting the workspace the demo lives in moves the pin to the next-oldest while the catalogue's rows stay behind, and `links` has no `deleted_at` cascade from `workspaces`. Not reproduced — no surface deletes a workspace that holds links | **Minor**, and only because the reachable path is now closed. The failure mode is the worst shape a seeder has: the destructive half commits, the rebuilding half refuses, and the operator is left with a demo that has lost its accounts and its history. The fix is to scope the three statements to the organization like everything else, and to keep the alias list as the thing that bounds them | No |
| F69 | M38 | [phase-details/README.md](phase-details/README.md)'s inherited-rules table says `demoCoverage()`'s **"four trailing rows"** assert zero for milestones not yet built. There are three | `docs/build-notes/phase-details/README.md`, the *A new feature somebody can see* row — against `cmd/lctl/demo_coverage_test.go`, whose trailing `MaxIsZero` rows are M41, M42 and M43 | Counted while adding M38's coverage rows. `git show 3ffebb0:cmd/lctl/demo_coverage_test.go` has four trailing boundary rows — M34, M41, M42, M43 — so the sentence was true when M33.5 wrote it; M34 converted its own row when it landed, exactly as the rule instructs, and the count beside the rule was not moved with it. (A fourth `MaxIsZero` row exists at `:201`, but it is M32's "no reputation feed verdict anywhere" assertion rather than a boundary row, so a bare grep count still reads four and hides this) | **Cosmetic, with a small teeth.** Nothing fails and no milestone is misled about what it must do. What it costs is that the number is the only thing telling a reader how many milestones are still unbuilt-and-watched, so it will drift again at M41 and there is nothing that notices. The durable fix is to stop writing the count — say "its trailing rows" — rather than to correct it to three | No **Widened by [M44.9](phase-details/m44.9.md), 2026-08-04:** the count is now zero, not three. Every trailing zero-asserting boundary row was converted as its milestone landed — M39/M40 at `cmd/lctl/demo_coverage_test.go:429`, M41 at `:474`, M42 at `:518`, M43 at `:566`, M44 at `:638` — so `phase-details/README.md:80`'s *"Its four trailing rows"* now describes nothing that exists. Separately, M33 is the only shipped Phase 2 milestone with a demo-visible seeded feature and no coverage row at all; that half is F119. |
| F70 | M39 | `domains.write` is a **role** permission, so on an instance with more than one organization every organization's owner and admin can administer the **instance-wide** default domain — its root redirect and its bot policy — not only their own hostnames | `internal/store/migrations/00800_domain_root_redirect.sql:32-34` grants the slug to the owner and admin role ids with no organization scope; `internal/link/domains.go`, `canAdminister`'s third limb returns `domains.write` alone for a row with both owner columns NULL; `internal/link/domain_settings.go:77` and `:191` guard `SetRootRedirect` and `SetBotBlocking` on the bare permission | Named while writing M39's ownership check, which had to decide what the instance default's limb should be. m39.md says the default "**stays** administrable only by instance owner/admin", so preserving the existing guard is what the milestone asked for and M39 neither widens nor narrows it — registering a hostname is not a route to it, and the new collection refuses to rename or delete the default at all. The reach is nonetheless real and already documented from the other end: `Plan.md`'s known-limitation row for the dispute queue calls it "the shape `domains.write` already has", `docs/SECURITY.md:174` repeats it, and migration `01600_destination_disputes.sql:100-105` excluded admin from `destinations.review` for exactly this reason. Recorded rather than fixed because narrowing it is a change to who administers an instance — the missing instance-level principal (D38) — rather than a defect in per-domain ownership | **Minor**, and unreachable on a single-organization instance, which is every default one. It becomes real with `LINKCTRL_SIGNUP_MODE=open`, where one registration provisions an organization and makes the registrant its owner: they then hold `domains.write` and can repoint the root of the hostname every workspace's links are served on. The same registration already reaches `destination_disputes`' sibling gap (F15), so the two want one answer rather than two | No |
| F71 | M39 | `demoCoverage`'s idempotency assertion is **time-dependent**: today's clicks are generated at minute granularity and dropped when they land after the current instant, so two seeder runs seconds apart on either side of a minute boundary produce different click counts and the test fails | `cmd/lctl/demo_data.go:361` and `:387` — `if at.After(now) { continue }`, against `at` built from `rng.IntN(60) * time.Minute` at `:358` and `:384`; asserted against by `cmd/lctl/demo_coverage_test.go:534` | Hit once while running the full `make test-integration` for M39: *"Click history (M9) counted 34532 on the first run and 34540 after the switch"* — eight rows, the number of today's clicks whose random minute fell inside the minute the wall clock crossed between the first and third runs, which the logged runtimes put about ten seconds apart. Not reproducible on demand and it did not recur in four consecutive re-runs; the mechanism is nonetheless visible in the two lines above rather than inferred from the symptom, and it is independent of anything M39 changed — the seeder's click generation is untouched by this milestone. What M39 adds is two more service calls to each run, which lengthens a run by tens of milliseconds and therefore makes straddling a minute boundary marginally likelier | **Minor, and a flaky gate rather than a broken demo.** The demo it produces is correct either way; what fails is the assertion that two runs match, and it fails at random. The cost is that a red `make test-integration` has to be re-run to find out whether it meant anything, which is the property that makes a flake expensive. The fix is to truncate `now` — or to freeze it once per run and pass it to every generator — so the boundary is not crossed mid-comparison, rather than to relax the assertion. It belongs to [M33.5](phase-details/m33.5.md), whose claim is that this test is the thing stopping the demo rotting | No |
| F72 | M40 | `config.CanonicalHost` does not fold a **trailing dot** in the `Host` header, so `Host: lnk.example.com.` matches no configured hostname and gets the ops-only 404 — and since M40 the same spelling of a *verified custom* hostname is unserved too | `internal/config/config.go:609-623` — lowercases, splits the port and strips only `:80`/`:443`; nothing trims a trailing `.`. `internal/redirect/hosts.go` keys the verified set through the same function deliberately, so the two agree and both miss | Reproduced by reading: `CanonicalHost("lnk.links.test.")` returns `"lnk.links.test."`, which equals neither `appHost` nor `linkHost` in `hostRouter.ServeHTTP`, so the request lands on the ops mux. Storage already folds the dot — `domain.ValidateHostname` drops it (M39) and the unique index is on `lower(hostname)` — so the *stored* name can never carry one and the mismatch is entirely on the request side. Commit `6f95079` ("Fold the dot before any tier reads the host") folded it for **destinations**, not for the `Host` header. Pre-existing since the split-host router; M40 inherits it rather than introducing it | Minor. A browser normalizes the trailing dot before sending, so this is reachable by a hand-written client or a misconfigured proxy rather than by ordinary traffic — and it fails closed (a 404, never a wrong host's links). The fix is one `strings.TrimSuffix` in `CanonicalHost`, which would reach the split-host router as well and therefore is not a change to make inside a milestone that did not ask for it | No |
| F73 | M40 | The verified-hostname cache has **no periodic reload**, so a replica that misses a pub/sub message serves a stale set until it restarts or hears another one | `internal/redirect/hosts.go` — `Reload` is called at boot, on `kindHost`, and by the subscriber's `flush` on every (re-)establishment; nothing is on a timer. The alias tier does not need one because every entry carries `REDIRECT_TTL`; this set has no TTL by construction, which is what makes it exact | Reproduced by reading. The gap is narrower than it looks: `Subscriber.establish` reloads on the first connection and on every reconnection, and `Subscriber.Run` bounds silence with `REDIS_SUBSCRIBER_READ_TIMEOUT` and probes rather than trusting it (F30's fix), so a replica that stops hearing gets a reload when it notices. What has no backstop is a **published message that is simply lost** while the subscription stays healthy — Redis pub/sub is at-most-once — and on an instance with no Redis at all, where a second replica would never reload | Moderate on a multi-replica deployment with Redis unavailable, minor otherwise; it fails in the direction of continuing to serve a hostname that has been unverified, which is exactly the staleness M40 exists to bound. A ticker calling `Reload` at `DOMAIN_VERIFY_INTERVAL` would close it for a query against a table of tens of rows. Out of spec here because m40.md asks for invalidation *wired into M23's pub/sub*, which is what shipped | No |
| F74 | M41 | `demoCoverage`'s idempotency assertion failed once with a **252-click** delta, which is two orders of magnitude larger than [F71](#open)'s recorded mechanism can produce | `cmd/lctl/demo_coverage_test.go:570` — *"Click history (M9) counted 34042 on the first run and 33790 on the second"*, and the same pair again at `:604` for the third run. F71 attributes this assertion's flakiness to `cmd/lctl/demo_data.go:361` and `:387` (`if at.After(now) { continue }`), where two runs seconds apart drop a different handful of today's clicks | Observed once, on the first `GOFLAGS=-count=1 make test-integration` of M41's tree. **Not reproducible**: six subsequent runs of the same code passed — one full suite plus five runs of `-run TestDemoSeederShowsEveryFeature` — as did two intermediate runs with parts of M41's seeder disabled. The seeder's runtimes were logged at 3.308s and 3.356s, so the two runs were seconds apart, and F71's mechanism accounts for roughly the number of today's clicks whose random minute falls inside that window — F71 itself observed **eight**. 252 is a whole low-weight link's history, not a few seconds of one. So either F71's stated cause is incomplete or there is a second one; what is *not* plausible is that M41 caused it, because the seeder's click generation runs before `demoSeeder.run` and is untouched by this milestone, and disabling M41's seeding entirely was one of the runs that passed | **Minor, and test-only** — the demo it produces is correct either way; what fails is the claim that two runs match. Its cost is F71's cost: a red `make test-integration` that has to be re-run to find out whether it meant anything, in a loop that treats a failing gate as a stop condition. Filed separately from F71 rather than folded into it because the magnitude contradicts F71's stated mechanism, and a row that quietly absorbed a contradicting observation would make the recorded cause look confirmed. The fix F71 proposes — freeze `now` once per run and pass it to every generator — is still the right first move, and this row is the reason to check afterwards that it actually closed it | No |
| F75 | M44 | Revoking an API key is scoped to the key's id and its **owner**, with no organization check, while listing keys is scoped to the owner *and* the organization — so the two disagree about which keys an actor acting in one organization can reach | `internal/store/query/apikeys.sql`, `RevokeAPIKey` (`WHERE id = $1 AND user_id = $2`) against `ListAPIKeysForUser` (`WHERE user_id = $1 AND organization_id = $2`); the caller is `internal/auth/apikey.go` `Revoke`, which passes only `actor.UserID` | Read from the two statements, not reproduced. A user belonging to two organizations sees only the current organization's keys in the list, but a `DELETE /api/v1/api-keys/{id}` naming a key issued in the other one answers `204` and revokes it. Both keys are that user's own, so nothing crosses a tenancy boundary and no key is destroyed that its owner could not have destroyed by switching organization first. What it does give is a weak existence oracle — `204` versus `404` distinguishes *my key, elsewhere* from *not mine* — against ids the caller would have to guess. Not touched by M44: rotation adds no revoke path, and the milestone's org-wide keys are still revoked by their own owner | **Low.** An inconsistency between two statements of the same scoping rule, with a negligible oracle attached. The fix is one `AND organization_id = $3`, and the argument against it is that it would make a key unrevokable from the organization somebody is signed into, which is worse | No |
| F77 | M44.9 | A host spelled with Unicode label separators or fullwidth characters walks past every destination tier, and is stored and served | `internal/link/destination.go:147` is the whole of host canonicalisation (`strings.TrimRight(strings.ToLower(u.Hostname()), ".")`); `looksNumeric` at `:219` uses `strings.LastIndexByte(host, '.')`; `internal/link/punycode.go:37` returns early unless a label starts `xn--` | **Reproduced live against the current build.** `POST /api/v1/links` accepted and stored `http://169。254。169。254/latest/meta-data/`, `http://１６９.２５４.１６９.２５４/`, `http://ｌｏｃａｌｈｏｓｔ/`, `http://metadata。google。internal/computeMetadata/v1/` and `http://127。0。0。1/admin`, each percent-escaped into storage, while the control `http://169.254.169.254/` was refused 422. No `destination.blocked` row is written for the accepted ones. There is no IDNA/UTS-46 step anywhere and `golang.org/x/net` is deliberately absent from go.mod (decisions.md ~5847). A browser's WHATWG host parser percent-decodes the host, then runs UTS-46 ToASCII, which maps U+3002/U+FF0E/U+FF61 to `.` and the fullwidth ranges to ASCII. **The finder's belief that Go's client is immune was refuted by measurement**: `net/http`'s `canonicalAddr` calls the standard library's vendored `golang.org/x/net/idna`, and a dial probe showed the stored form connecting to `169.254.169.254:80` | **High**, the tier F26 was given, over a wider spelling space. Not critical: nothing reads the response back, and the webhook dialer's `guardDial` judges the *resolved address*, so the private-address line holds for the one server-initiated fetch. But `guardDial` judges addresses only, so the Postgres blocklist, the shortener list and the homograph tier — all name-based — are bypassed, and this server can be made to POST signed workspace payloads to a host the operator blocked. Falsifies `docs/SECURITY.md:40` (*"a name cannot be spelled past a check that would otherwise refuse it"*) and m30.md's central claim, so it **reopens M30** rather than F26, whose `TrimRight` fix is correct for what it names. Fix note: canonicalise, do not reject — refusing non-ASCII kills `müller.de`, which a recorded decision protects — and a hand-rolled map of the three separators misses the fullwidth spellings, which carry no separator at all. Only full UTS-46 catches both, the ToASCII form must be what is **stored**, and it needs `golang.org/x/net/idna` in go.mod, which the punycode decision refused. **That dependency is a decision for the owner, not a correction** | No |
| F78 | M44.9 | An exhausted one-time or max-click link serves its destination to `HEAD`, repeatably and forever | `internal/httpx/redirect_gates.go:229-235` — `if !gated \|\| r.Method == http.MethodHead { return gatePassed }`, which skips the budget rather than merely not consuming it; `gatePassed` is `answered:false`, so `redirect.go:405` writes `Location` and 302 | **Reproduced live.** A `one_time` link answered 302 with the destination to GET #1, 410 to GET #2, and then 302 with `Location: https://example.com/secret-destination` to three successive `HEAD` requests. `record()` skips HEAD (`redirect.go:454`), so the disclosure leaves no click row. The budget is never *read*, only never *written* — and `gate.Service.Budget` (`internal/gate/gate.go:205`) is an existing non-consuming read that would let HEAD answer 410 without spending | **High.** Falsifies m35.md's *"exceeded serves 410"*, which carries no method carve-out. It does **not** falsify `docs/SECURITY.md`'s *"HEAD never spends a click"*, which is satisfied — the recorded decision bought non-consumption and the code delivered non-checking, which is strictly more. Narrowed by verification: signature and password gates both run first and both refuse HEAD, so the disclosure needs a link whose only gate is the budget — which is the ordinary one-time link, the shape the feature exists for. Untested: no `MethodHead` case exists in the gate tests. Fix note: making HEAD consume destroys the feature and is the outcome D53 names as unacceptable; the fix is the non-consuming read plus a 410, and it must not become a per-request query for links that are not exhausted | No |
| F79 | M44.9 | Signed links are verified against the instance's default domain rather than the link's, so they are wholly broken on custom domains and a signature crosses between same-alias links | `internal/httpx/redirect_gates.go:125` passes `h.DomainID`, documented at `internal/httpx/redirect.go:51-53` as *"resolved once at boot"*, while `internal/link/gates.go:132` signs with `row.DomainID`. `redirect.go:176-179` computes the request's real domain into a local that `passGates` (`:361`) never receives | `grep -n 'h\.DomainID' internal/httpx/` returns exactly two sites, both in the gates, and the field is never reassigned. `internal/gate/signature.go:169` mixes the domain id into the HMAC, so signer and verifier disagree whenever `links.domain_id` is not the boot default — which D71 makes the norm, since a workspace's verified hostname becomes the default for its new links. `redirect.Snapshot` carries `LinkID` and `WorkspaceID` and no domain, so no second path supplies it. No test covers a custom-domain gate: every gate fixture wires `DomainID` from `ResolveDefaultDomain` | **High.** Two failures from one omission. Signature verification on a custom domain fails closed (403) for the legitimate holder — a shipped feature that is 100% non-functional on another shipped feature. And a signature minted for alias `X` on the default domain verifies against a *different* link with alias `X` on a custom host in the same workspace, which is exactly the property the domain id is in the MAC to prevent. Falsifies `internal/gate/signature.go:42-45`, `docs/SECURITY.md:64` and the M35 decisions entry, which calls the case *"not hypothetical"* and names M40 as the milestone that creates it. The same boot constant keys the per-alias password bucket at `:211`, falsifying its comment that *"the same alias on two domains is two links"* — one defect, two consequences, and the second one fails safe. Fix note: threading the request domain invalidates every signature already minted, and does not fix F80 | No |
| F80 | M44.9 | `POST /links/{id}/sign` returns a signed URL on the wrong hostname, which may resolve another workspace's link | `internal/link/gates.go:139` — `signed := s.baseURL + "/" + row.Alias + "?" + params.Encode()`, with a comment claiming it is *"built from the same base URL the short link is published under"* | `internal/link/service.go:1225-1230` already holds the domain-aware `shortURL` helper, called at `service.go:1122` and `internal/link/automation.go:322` and skipped only here; its own doc names this failure — *"a link created on go.acme.com whose short_url named the instance's own host would be a link the product told you to publish at the wrong address."* Alias uniqueness is `(domain_id, alias)`, so the emitted URL resolves against the default domain. Adversarial verification sharpened the consequence: the default domain is shared across workspaces (F23), so if that alias exists there the URL does not merely 404 — **it resolves a different workspace's link and redirects the recipient to a stranger's destination** | **High**, and independent of F79: fixing the verifier alone still leaves the minted URL pointing at the wrong host. Fix note: `shortURL` falls back to `s.baseURL` when its cached hostname lookup returns empty, which at this call site is the bug again — an empty result must be an error here, not a fallback | No |
| F81 | M44.9 | On a `REDIRECT_DEFAULT_STATUS=307` instance, a correct link password is re-POSTed by the browser to the link's third-party destination | `internal/httpx/redirect.go:405` answers a successful password POST with `h.status()` (`:529-534`), fed from `cfg.Redirect.DefaultStatus` at `cmd/linkctrl/main.go:749`; `internal/config/config.go:776-785` permits 307 and `docs/configuration.md:174` advertises it | Nothing forces 302 on the gate-pass branch — it is the only redirect write in the tree and it takes the configured status. `internal/httpx/static/password.html:45-46` is `<form method="post">` with no `action` and `name="password"`, so the body is `password=<secret>`, and RFC 9110 §15.4.8 forbids a user agent changing the method on a 307. No test covers it: every integration fixture pins `http.StatusFound` | **High on any instance set to 307** — plaintext credential disclosure to a host the link owner chose and the operator does not control, for every password-gated link, and the feature is functionally broken because the destination will answer 405. Bounded by being a non-default setting, but a documented and validator-blessed one. Fix note: special-casing 307 leaves any future method-preserving status; the branch should answer **303**, which mandates a GET — and 303 is not in `config.go:776`'s allowed set, so the validator and `docs/configuration.md:174` need the same edit, and D53's *"answers the **302** itself"* has to be amended rather than silently contradicted | No |
| F82 | M44.9 | One workspace's webhook backlog stalls every scheduled job on the instance — mail, automation, rollups and domain verification | `internal/webhook/webhook.go:213-215` delivers a claimed batch sequentially; `DrainBatch = 20` at `:72`; per-attempt timeout is `WEBHOOK_TIMEOUT`, default 10s with a 1m ceiling (`internal/config/config.go:963`); `cmd/linkctrl/jobs.go:194-219` is one `select` in one goroutine, and `case <-outbox.C` runs `runMail` then `runWebhooks` inline | No goroutine and no errgroup exist anywhere in `internal/webhook` — the drain is inline on the shared scheduler goroutine, and Go tickers hold a cap-1 channel, so every other job's tick is silently dropped for the duration. Twenty deliveries at the **default** 10s is 200 seconds, which falsifies the code's own claim at `webhook.go:69-72` that a backlog *"drains over several runs instead of holding the job for minutes"* — with no misconfiguration at all. Registration refuses only *private* addresses, so a webhook aimed at a blackholed public address passes | **High.** Tenant-triggerable degradation of subsystems with nothing to do with webhooks: invitation and verification mail sit in the outbox, automation stops firing on its advertised clock, rollups go stale. Needs only `webhooks.write` from a signed-in member — never an API key, since the scope is non-delegable — and on open signup, anybody. Fix note: delivering concurrently inside `Drain` breaks D77, because `withLeadership` releases the advisory lock when its function returns and goroutines outliving it would deliver unlocked; and giving webhooks their own ticker fixes nothing, because the stall is the shared goroutine rather than the shared clock | No |
| F83 | M44.9 | An automation rule past the hundredth by watermark is never evaluated, on any instance, permanently | `internal/automation/automation.go:217-223` returns before `ClaimAutomationRule` whenever `len(subjects) < config.Threshold()`; `ClaimAutomationRule` (`internal/store/query/automation.sql:122-126`) is the only writer of `last_fired_at` outside a disabled→enabled re-arm; `ListDueAutomationRules` (`:88-94`) orders `last_fired_at NULLS FIRST, id LIMIT $row_limit` with `AutomationRulesPerRun = 100` | There is no `last_checked_at` column — `02900_automation.sql` and `00600_phase2_dormant.sql:109` define one timestamp — and the query predicates only on `r.enabled` and the workspace not being deleted, so nothing excludes a rule whose trigger cannot match. The default threshold is 1, so **every zero-match run leaves the watermark fixed**, and the hundred oldest-watermark enabled rules are permanent front-of-queue. `internal/domain/automation.go:211-213` and `automation.sql:80-83` both assert the opposite — *"the cap starves nobody: the rules a capped run skipped hold the oldest watermarks on the next run and go first"* — and `automation.go:150-157` logs the cap with the same reassurance | **High.** A rule the customer created, sees listed as enabled and is charged for does nothing, with no error, no log line naming it, and nothing on the page distinguishing it from a rule whose trigger has not matched. One hundred rules is five workspaces at the documented per-workspace cap of twenty. Fix note: advancing `last_fired_at` on a no-match run is the obvious fix and is wrong — it is the threshold accumulator, deliberately held so sub-threshold matches accumulate, and moving it discards subjects already inside the window. The ordering needs its own column | No |
| F85 | M44.9 | M42's and M43's entire browser surface is unreachable — eleven registered routes answer 404 on every deployment shape | `internal/httpx/router.go:889-899` — `dashboardPatterns` omits `/webhooks`, `/webhooks/`, `/automation` and `/automation/`, while `router.go:536-551` registers `GET /webhooks`, `GET /automation` and nine POST forms; `router.go:699-701` mounts that slice and nothing else | **Reproduced live** with a valid owner session: `GET /webhooks` and `GET /automation` return 404 rendering the *redirect handler's* "Link not found" page — they fall through to the alias catch-all — and `POST /webhooks` returns 404 too, while `/links`, `/domains`, `/campaigns`, `/folders`, `/keys`, `/feeds`, `/disputes`, `/members`, `/workspaces`, `/invites`, `/notifications` and `/account` all answer 200 on the same cookie. Verification established `registerApp` is the only mount and **both** deployment shapes call it, so split-host is broken too, differing only in returning a bare mux 404. `internal/ui/templates/partials/nav.html:177` and `:188` render links to both pages, and `docs/usage.md:1205`, `:1331`, `:1361` and `README.md:87` document them. Both names *are* in `internal/alias/reserved.txt:100-101`, so somebody reserved them and never mounted them | **High.** The guard that exists to prevent exactly this cannot see it: `RegisteredTopLevelPaths()` (`router.go:925-941`) is built **from** `dashboardPatterns`, so `TestReservedListCoversRegisteredRoutes` can only catch "registered but not reserved", never "handler registered but not mounted". `router.go:695-698` claims the opposite in as many words — *"Mounted from the same slice the reserved-list guard reads… Two lists that had to agree by hand is what the guard existed to prevent"* — and there are still two lists that must agree by hand. Filed as one row rather than two: it is a routing defect, and fixing the slice makes every documentation and nav reference true. Not an in-spec reopening — neither m42.md nor m43.md requires a page — but the demo seeds both features and `demoCoverage`'s M42 row describes *"the webhooks page with both states on it"* | No |
| F86 | M44.9 | Three documents promise that no destination leaves the instance without `LINKCTRL_FEED_URL`; since M42 any workspace member can send every destination to a host of their choosing | `Plan.md:215-217`, `docs/SECURITY.md:207-208` and `:215-216`, `README.md:151`, against `internal/link/webhook.go:379-386` (`emitLink`) and `:403-409` (`emitBlocked`) | `emitLink` puts `"url": l.URL` — the raw, undefanged destination — in all five link-lifecycle payloads, and `emitBlocked` sends `"url_defanged"` on `destination.blocked`, which is precisely the tier refusal `SECURITY.md:215-216` says *"is never sent anywhere"*. `internal/config/config.go:952-955` states there is no operator gate at all: *"Not inside an Enabled guard, because there is no switch: webhooks are a workspace feature rather than an operator one."* `README.md:151` is contradicted by `README.md:87` on the same page. m42.md carries no bullet updating the promise, and m32.md's own definition of done required that it be *"updated to state the opt-in exception precisely, rather than being quietly falsified"* | **High**, and documentary rather than hidden exfiltration — `docs/usage.md:1253` and `:1261` do publish both payload fields and `SECURITY.md:60` documents webhooks as user-chosen egress, so the capability is disclosed; four summary claims are what is false. It is the privacy claim the project leads with, on the front page and in the file a security-conscious reader trusts most, and it over-promises. Fix note: this is not a code fix — `internal/link/webhook.go:366-372` declares that payload a published interface. All four sites need amending plus the `url_defanged` leg, and amending Plan.md and README while leaving `SECURITY.md:215-216` is the likely half-fix, since that line is the strongest of the four | No |
| F87 | M44.9 | A sequential split on a password-gated link advances the rotation twice per visit, so at any even arm count half the arms are served to nobody | `internal/httpx/redirect.go:327` runs `h.split` before `passGates` at `:360`; `internal/httpx/redirect_gates.go:153-156` answers any non-POST with the challenge; `internal/httpx/redirect_split.go:136` calls `Rotate` unconditionally | The challenge GET reaches `:327` and consumes a position, then is answered with the form; the verifying POST is not refused (`redirect.go:274-275` exempts `HasPassword`), reaches `:327` again and consumes a second. The arm served is therefore always index `(2k-1) mod N` — **for N=2 that is `arms[1]` on every visit, forever, and `arms[0]` is never served to anyone**. D57 states the opposite at `decisions.md:9408`, repeated verbatim at `Plan.md:810` and `redirect_split.go:117`: *"the distribution is unaffected — whether a gate passes is independent of which arm came up."* That is true of a refusal and false here, because the password gate is the one gate that manufactures a guaranteed second request. `test/integration/split_test.go:215-236` pairs sequential with a one-time gate and uses three arms, an odd count that would mask it | **Moderate.** A shipped A/B feature silently reports zero traffic to half its arms whenever the link is also password-gated, and `click_events.destination_id` faithfully records the arm that was served, so the analytics confirm the skew rather than expose it. Fix note: moving `h.split` after `passGates` is the obvious move and breaks M35's load-bearing ordering (`decisions.md:9224-9231`) — the budget gate would then run before the destination exists, so an unforwardable deep link on a one-time link would spend its single click on a request about to 404. The fix belongs in the challenge branch | No |
| F88 | M44.9 | On a single-host deployment, a `Host` spelling that does not fold serves a customer's verified hostname the dashboard, the API and the default domain's aliases | `internal/config/config.go:643-657` (`CanonicalHost`) folds case and strips only `:80`/`:443`, never a trailing dot; `internal/redirect/hosts.go:117` keys the cache through it; `internal/httpx/router.go:821-827` makes the combined mux the fall-through arm; `internal/httpx/redirect_hosts.go:66` calls it | `Host: go.customer.example.` or `go.customer.example:8080` misses the host cache, falls through, and is served by `registerOps + registerApp + registerRedirect` — and `redirect.go:176-179` then resolves against `h.DomainID`, the instance default, so another workspace's aliases answer on the customer's name. This contradicts `router.go:786-790` and `redirect_hosts.go:52-56` (*"a customer's hostname must not serve the management UI, and a session cookie must never be offered a second origin"*) and `docs/deployment.md:239-243`. Reachable over HTTPS: SNI carries no trailing dot per RFC 6066, so the certificate handshake completes and Go passes `r.Host` through unnormalized. The whole M40 suite pins `SplitHosts: true`, the one configuration where the fall-through is the ops mux | **Moderate**, and above F72's stated tier. F72 records the same normalization gap but asserts the consequence is benign — *"it fails closed (a 404, never a wrong host's links)"* — which holds only for split-host; on single-host the failure is open, and serving the default domain's aliases is exactly "a wrong host's links". F72 also names only the trailing dot, not the port spelling. Fix note: `TrimSuffix` closes half — the non-default-port limb still misses — and stripping the port unconditionally breaks any deployment where the app and link hosts differ only by port, which is what `SplitHosts()` compares | No |
| F89 | M44.9 | Bot blocking cannot be set on a custom domain at all, and the link page reads the default domain's policy for every link | `internal/store/query/links.sql:422-431` (`SetDefaultDomainBotBlocking`) is `WHERE is_default`; `internal/link/domain_settings.go:46` and `:219` both resolve `defaultDomainID`; `internal/httpx/web_links.go:591-595` calls `DomainSettings` regardless of the link's domain | Enforcement reads the right row — `internal/store/query/redirect.sql:81-85` joins on `l.domain_id` — and the API guard reads the right row too (`internal/link/service.go:602`), but there is no writer for a non-default domain's `block_bots` columns, so they can never leave their defaults. So an operator who turns blocking on, even *enforced*, gets no blocking on any custom-domain link. Meanwhile the page renders the select disabled and prints *"Blocking is enforced for every link on {{.BotHost}}"* naming the **default** hostname for a link served elsewhere, while `PATCH /api/v1/links/{id}` with `bot_blocking: off` would be accepted — so the two surfaces m32.5.md says are *"asserted by test at both surfaces"* now disagree. `Plan.md:800` still says *"the domain's setting is instance-wide"*, which M40 made false | **Moderate.** A safety control an operator believes is instance-wide has a per-tenant hole any workspace can open by registering a hostname, and it happens without anyone choosing it, because D71 makes a workspace's verified hostname the default for its new links. Fix note: widening the setter to an arbitrary `domain_id` hands per-hostname bot policy to `domains.write`, which F70 already records as over-broad, letting a workspace switch off an enforcement the operator set; and `01800`'s CHECK refuses "enforced without blocking" per row, so a naive single-column `UPDATE` can write the combination `SetDefaultDomainBotBlocking` sets both columns at once to avoid | No |
| F90 | M44.9 | The webhook delivery queue is 40 rows a minute for the whole instance, strictly arrival-ordered, with no per-tenant bound | `internal/store/query/webhooks.sql:110-117` — `WHERE status='pending' AND next_attempt_at <= now() ORDER BY next_attempt_at LIMIT $batch_size`, no workspace term; `DrainBatch = 20` on a 30-second tick; `MaxWebhooksPerWorkspace = 20` (`internal/domain/webhook.go:113`) | One link write in a workspace holding the permitted twenty subscriptions produces twenty rows, so two link writes a minute from one workspace saturate instance-wide capacity. `EnqueueWebhookDeliveries` applies no cap on pending rows and `WEBHOOK_RETENTION_DAYS` prunes only *finished* ones, so a backlog is unbounded in size and duration; every row a tenant queues has an earlier `next_attempt_at` than everything queued after it. The query file's own header (`:5-9`) argues the tenant term was omitted deliberately — *"a drainer that filtered by tenant would deliver in tenant order, and the queue is fair or it is a queue somebody's backlog can starve"* — and removing it produced arrival-ordered delivery, which starves the same way | **Moderate**, rising on a multi-tenant instance where one customer's ordinary traffic silently delays another's integration. No loss and no disclosure — rows arrive late. The headline capacity is low enough to be a design fault rather than only an abuse case: one customer creating links through the API at 40/min with a single subscription reaches the ceiling with no attacker involved. Compounds F82. Fix note: `DISTINCT ON (workspace_id)` or a round-robin term reintroduces the tenant-ordered delivery the file refuses **and** cannot sit under `FOR UPDATE SKIP LOCKED`; a per-workspace pending cap silently drops events, which Plan.md already says nothing recovers | No |
| F91 | M44.9 | Webhook deliveries can spend every attempt without a socket ever being opened, and the row is then re-claimed forever rather than abandoned | `internal/store/query/webhooks.sql:105-107` spends `attempts + 1` and leases all claimed rows before anything is sent; `internal/webhook/webhook.go:212-215` passes the same `runCtx` into every `deliver`; `cmd/linkctrl/jobs.go:357` bounds the run at five minutes | Adversarial verification **corrected the finder's account, and the truth is worse**: on an expired context `MarkWebhookAbandoned` fails on the same dead context as `MarkWebhookRetry` (`webhook.go:250-256`), so the row is never marked abandoned — it stays `pending`, is re-claimed every 60 seconds, and `attempts` climbs without bound while permanently occupying batch slots, which feeds F90. Not reachable at defaults: 20 × 10s fits inside 300s. The real defect is that `internal/config/config.go:957-970` validates `WEBHOOK_TIMEOUT` against the 30-second drain interval and never against the five-minute job bound, so it documents up to `1m` as safe when twenty rows at 60s is 1,200s and roughly fifteen of them burn an attempt with no dial | **Moderate**, latent behind an operator setting the validator explicitly permits. Fix note: lowering the ceiling to 15s leaves the arithmetic coupling undeclared, since `DrainBatch` is prose in the config file and deliberately not imported; and reusing `context.WithoutCancel` for the mark calls converts an unbounded re-claim into unbounded abandonment writes. The bound has to be checked before the row is claimed, not after the dial fails | No |
| F92 | M44.9 | Sign-in tells an unauthenticated caller whether an address has an account, on every instance including `closed` | `internal/auth/service.go:294-296` exits an unknown address with `DummyVerify` and `ErrInvalidCredentials`; `:319-326` returns `ErrAccountLocked` only after a real row was found; `internal/httpx/problem.go:154-159` gives it its own type and a 429 | The fifth wrong password against a registered address answers `429 …/problems/account-locked` where five against an unregistered one answer `401 …/problems/invalid-credentials`. Five requests fit inside `LOGIN_RATE_PER_MIN` (10), so the per-address limiter does not mask it, and its own refusal carries a different type (`…/problems/rate-limited`), so the three states are distinguishable. The browser form reproduces the split in prose at `internal/httpx/web.go:389-393`. The file contradicts itself: `problem.go:141-146` collapses `ErrAccountInactive` into invalid-credentials *because* "telling an unauthenticated caller 'this account exists but is suspended' gives back exactly what that refusal withholds", twelve lines above the branch that does it, and `service.go:282-284` asserts *"Every failure returns ErrInvalidCredentials"* | **Moderate**, and higher-reaching than F13, which the tree already treats as worth deferring: F13 is refused before any lookup on a `closed` or `invite` instance, and this fires on the shipped default. It also costs the target a lockout, so it doubles as a cheap denial of service against a known address. Fix note: mapping the error to 401 fixes the API and leaves the browser copy leaking, and a real user then gets "wrong password" during their own lockout with no explanation. Timing is a second leak — the locked branch returns before any verification, so it is fast where the unknown-address path pays a dummy argon2 | No |
| F93 | M44.9 | A workspace-scoped admin lists and revokes the organization's invitations, including an owner's invitation of a co-owner | `internal/invite/invite.go:434-438` (`List`) and `:499-507` (`Revoke`) gate on `actor.Can(PermWrite)` and scope by `actor.OrgID`, with no `LoadMembershipAuthority`; the sibling `Create` at `:259-269` requires `authority.In(nil).Granted` | `members.write` is granted to `admin` wholesale (`00700_seed.sql:61`), so a workspace-scoped admin resolves it in their own workspace; `RevokeInvitation` (`invitations.sql:47-59`) predicates on id, organization and un-revoked/un-redeemed, so no SQL predicate saves it; `internal/httpx/web_invitations.go:47` renders whatever `List` returned and `invites.html:85-92` draws a Revoke button on every pending row with no `Manageable` filter. Both routes are bare `signedIn`/`RequireAuth`. No test drives a workspace-scoped actor at either. An invitation is D44's canonical organization-wide object — *"An organization-wide object — a membership with no `workspace_id`, **an invitation**, the organization itself — is reached only by an organization-wide membership"* — and the F27 fix reached one of its three verbs | **Moderate.** Discloses every pending invitee's address, role and inviter, and destroys an admission grant irreversibly — a revoked invitation cannot be un-revoked, and `Create` refuses this actor a replacement, so an owner can be prevented from staffing their own organization. Below F27 because it cannot mint authority. Reachable through the shipped UI and API with no forged request. Fix note: gating `Revoke` on `In(nil)` while leaving `List` produces a page of rows whose only button 403s, and `Roles` at `:474` carries the same identity-rank read | No |
| F94 | M44.9 | A workspace-scoped owner is notified about every other workspace in the organization, including hostnames and link aliases they hold no membership in | `internal/store/query/notifications.sql:81-88` (`ListUsersWithRoleInOrg`) filters organization and role slug only, with no `m.workspace_id IS NULL`; reached from `internal/notify/notify.go:294-297` (`OwnersOf`), used by `internal/notify/domains.go:93` and `internal/notify/automation.go:43` | `LockOrganizationOwners` (`members.sql:76-83`) asks the same question correctly and says why directly above it: *"Organization-wide only: a workspace-scoped owner membership grants ownership of one workspace, not of the organization."* The recipient receives M40's domain warnings carrying the hostname and M43's automation notifications carrying matched link aliases and defanged blocked destinations, for workspaces `ListWorkspacesForUser` would never list for them. Adversarial verification established this is **reachable, not latent**: an organization-wide owner may grant `owner` scoped to one workspace — `resolveRole` compares `role.Rank(10) < here.Rank(10)`, which is false, and `Roles` offers it — through `POST /api/v1/members` and the members page. The query also omits `r.organization_id IS NULL`, which the correct sibling carries | **Moderate.** Cross-workspace disclosure to a principal the schema's own comment says owns one workspace; push delivery, so the recipient does not have to go looking. No write and no authority gained. Fix note: adding `m.workspace_id IS NULL` alone silences a workspace-scoped owner about their **own** workspace, which is what both producers intend by passing a workspace id — the correct recipient set is organization-wide owners together with owners scoped to *that* workspace, which needs a second parameter rather than a second predicate | No |
| F95 | M44.9 | Any link-creator can flood the instance-wide dispute queue and every organization owner's inbox, one character per host | `internal/link/blocking.go:257-262` — the `url_credentials` heuristic matches on `u.User != nil` with the host ignored; `internal/dispute/dispute.go:344-368` (`disputable`) admits every low-confidence verdict; `PermFile = link.PermCreate` at `:85`; `internal/httpx/router.go:583` mounts `POST /disputes` behind `signedIn` alone | `01600_destination_disputes.sql:67-75` calls the one-open-dispute-per-host index *"the cheapest bound on somebody filling the queue — a caller who wants a thousand rows in front of the owner needs a thousand distinct blocked hosts"*. Because `url_credentials` blocks *any* host, a filer needs no blocked host at all, only distinct strings with an `a@` prefix. Each accepted filing calls `tellReviewers` → `notify.EveryOwner`, which walks `ListOrganizationIDs` with no predicate, so one POST writes one notification per owner on the instance. `router.go:693` confines the rate limiter to the API prefix, so the browser route is unlimited | **Moderate.** Two narrowings from verification: `tellReviewers` writes inbox rows only and sends no mail, and F33 already records the flood, the missing limiter and the instance-wide fan-out. What is new here is that `url_credentials` removes F33's precondition of a blocked parent, widening the input space from hosts under a listed suffix to every string. Fix note: the multiplier is `EveryOwner`, not the filer, so rate-limiting the route or capping the filer both leave the amplification; and narrowing `url_credentials` changes what M30 refuses, which is a tier change rather than a queue fix. This should land with F33 rather than beside it | No |
| F96 | M44.9 | The redirect tree has no request deadline, so every database call M35 and M36 added to it is unbounded | `internal/httpx/router.go:647` applies `RequestTimeout` to `appHandler` only; `registerRedirect` (`:766-773`) mounts the handler bare; the gate calls at `internal/httpx/redirect_gates.go:118`, `:179`, `:236` and `internal/httpx/redirect_split.go:136` all take `r.Context()` | `internal/gate/gate.go:193-196` passes the context through untouched. Nothing else bounds them: there is no `statement_timeout` anywhere in the tree, and `internal/platform/postgres.go:60-83` sets only pool sizes, lifetimes and a connect timeout. The contrast is exact and deliberate one file over — `internal/redirect/resolver.go:185` wraps its own query in `WithTimeout(WithoutCancel(ctx), DBTimeout)`, defaulting to a second even when unset, and explains at `:92-95` that a query still running after a second *"is going to hold a connection from the small redirect pool while more requests queue behind it"*. `HTTP_WRITE_TIMEOUT` sets a socket deadline and does not cancel the handler | **Moderate.** Unbounded is not merely slow: the goroutine holds its connection, and because M36's rotation runs on the **application** pool (see F97) a gated or split redirect can drain the connections the dashboard needs — the inverse of the two-pool separation decisions.md:277-279 exists to provide. Fix note: wrapping `registerRedirect` in `RequestTimeout` applies a dashboard budget to a 20ms path and `http.TimeoutHandler` buffers the response, which would break the `Location` write and swallow the gate pages. The bound belongs inside `internal/gate` per call, mirroring the resolver — and must **not** use `WithoutCancel` for the budget consume, or a client disconnect mid-consume burns a one-time link's only click | No |
| F97 | M44.9 | A key rotation performed by a key whose owner has been removed from the organization renews itself indefinitely, and no administrator can revoke it | `internal/auth/apikey_rotation.go:151-197` checks only `IsAPIKey`, revoked, expired, grace and successor under lock — never membership or `users.status`; `:196` and `:341-345` copy the predecessor row's scopes; `successorExpiry` at `:394-406` gives the successor *"the predecessor's lifetime, measured from now"*; `internal/store/query/apikeys.sql:85` (`RevokeAPIKey`) keys on `user_id` | `restrictTo` does not disarm the successor, because `narrowScopes` copies the **row's** stored scopes rather than the actor's live permissions, so full scopes are persisted and go live again if the membership ever returns. Each generation can rotate again, so the chain is unbounded. Two sub-claims were **refuted** and are not part of this row: `ListAPIKeysForUser` *is* organization-scoped (`apikeys.sql:37-38`), and `RecordAPIKeyRotation` writes an organization-scoped `apikey.rotated` audit row, so an admin holding `audit.read` can *see* a rotation. What they cannot do is revoke it — there is no organization-scoped revoke statement, and the vocabulary has no `apikey.created` or `apikey.revoked` | **Moderate**, and it strictly extends F43, whose severity argument rests on the orphaned key being *"One-shot"*. Rotation makes it repeatable and self-renewing. Bounded to workspace-bound keys: an organization-wide key dies correctly, because `apikey.go:568-577` answers `ErrAPIKeyInvalid` on `ErrNoWorkspace`. Fix note: adding a membership check to `Rotate` alone leaves F43's own door — `CreateOrganization`'s zero-membership bypass — open, and neither gives an administrator a revoke path, which needs a new organization-scoped statement | No |
| F98 | M44.9 | A cached redirect blocks on Redis for a returning-visitor rule, while two operator documents say a memory-answered redirect costs nothing | `internal/httpx/redirect_rules.go:188-198` → `internal/analytics/returning.go:150-154`, reached from `redirect.go:323` on every `OutcomeRedirect` regardless of cache tier | `internal/platform/redis/redis.go:52-54` and `docs/configuration.md:151-153` both state that *"A redirect already answered from memory costs nothing"*. Since M34 that is false for a link carrying a `returning` condition: the `SISMEMBER` runs on a memory hit, and with Redis stalled it costs `REDIS_READ_TIMEOUT` (50ms) on the path the 20ms SLO is stated over. The framing that the SLO was never measured on this path is **refuted** — `docs/slo.md:230-243` records all 240,002 cache-hit requests performing the lookup and matching, at 100% under 20ms — so what is wrong is the stall case and the two sentences, not the measurement | Minor, and the same class and tier as F9, which covers only the two uncached calls and predates M34. Fix note: making it asynchronous is wrong, because the answer decides which destination is served; skipping the lookup after a Redis failure silently stops a returning-visitor rule matching, which is a behaviour change to a shipped rule rather than a latency fix | No |
| F99 | M44.9 | The comment justifying which pool the gate service uses describes a property M36 removed | `cmd/linkctrl/main.go:501-507` — *"every one of its queries is either a management read or a write that only a **gated** link performs"* — against `internal/redirect/snapshot.go:378-380`, where `Gated()` is `HasPassword \|\| RequireSignature \|\| OneTime \|\| MaxClicks != nil` | M36 hung `Rotate` off the same service (`redirect_gates.go:301`) without revisiting the justification, and a sequential-split link is not gated, so the application pool is now on the hot path for a link class the comment excludes. The placement itself is defensible under the same reasoning — see the fix note | Minor. The false justification is the defect; the live cost only bites through F96's missing deadline. Fix note: moving `Rotate` to the redirect pool "for isolation" reintroduces exactly what the comment protected against — `docs/slo.md:453-465` measures 5,000 hot aliases serialising on one row lock at 3.1ms median with dropped iterations, which would exhaust the small pool and stall every redirect rather than only split ones. Correct the comment, or bound the call | No |
| F100 | M44.9 | `HEAD` advances a sequential split's durable rotation, re-phasing every subsequent visitor's arm with no click recorded | `internal/httpx/redirect_split.go:53` and `:127-136` never test `r.Method`, while `internal/httpx/redirect.go:454` (`record`) and `internal/httpx/redirect_gates.go:229-235` (budget) both do | Falsifies two absolutes in one decision entry — `decisions.md:9225-9231`, *"**The budget is the only gate that consumes something**"* and *"HEAD never consumes at all"* — because M36 put a second durable counter in the same table. A link checker or unfurler shifts the rotation on every probe, and because no click row is written the per-destination breakdown cannot show why the arms are uneven. The companion claim, that a deep link the alias cannot forward also spends its position, is **not** a finding: D57 records that deliberately at `redirect_split.go:114-121`, `decisions.md:9406-9413` and `Plan.md:810` | Minor. Skews a split test under crawler traffic; no visitor sees a wrong page and nothing is destroyed. Fix note: returning early on HEAD makes a HEAD answer the link's *own* destination while a GET answers an arm, so a checker validates a URL no visitor is sent to — HEAD must still choose an arm without advancing, which `NextVariantRotation` cannot do | No |
| F101 | M44.9 | One Redis call on the redirect path carries no deadline, falsifying the premise on which `MaxRetries` was left unpinned | `internal/redirect/resolver.go:257` — `_ = r.redis.Del(context.WithoutCancel(rctx), k).Err()` | `context.WithoutCancel` strips the deadline, not only cancellation — confirmed against the tree's toolchain. D26 and `internal/platform/redis/redis.go:58-61` both justify leaving go-redis's retry count at its default on the stated ground that *"every Redis call site in this tree carries one"*. Decisive beyond the original report: `resolver.go:407`, `:512` and `:590` all use `WithTimeout(WithoutCancel(ctx), …)`, so `:257` is the single site missing the re-wrap — an omission rather than a design | Minor and latent: the branch needs a `Get` that succeeds followed by a decode failure, which requires a snapshot format break — but then it fires on every key rather than one. Fix note: passing `rctx` re-introduces the cancellation `WithoutCancel` is there to strip, so a hung-up client would skip the cleanup and leave the poison key costing a decode per request; and the fix touches the redirect path, which owes a k6 re-run | No |
| F102 | M44.9 | For the three Redis-shared limiters, the tracked-keys gauge is pinned at zero while the feature works, and the runbook's alert for a degraded shared limit has no expression that can fire | `internal/ratelimit/ratelimit.go:213-218` returns before the local shard write and before the overflow counter; `internal/observability/collectors.go:153-159` declares only `Len()` and `Overflows()`; `internal/ratelimit/shared.go:221` (`Fallbacks`) is referenced only by tests; `docs/operations.md:116` | On a healthy Redis the local table for `login`, `api` and `link_password` is never written, so `linkctrl_rate_limit_tracked_keys{limit="login"}` reads 0 — indistinguishable from no traffic — and the gauge's documented meaning at `collectors.go:154` is false. `operations.md:116` prescribes watching a rate that stays *"unchanged"*, which is not an expression. Narrowed by verification: a fallback **does** write the local table, so a non-zero reading is a working, undocumented fallback detector, and the runbook row does also point at a log line | Minor to moderate, observability only; fail-open is the accepted posture. The cost is that an operator following the shipped runbook cannot tell a shared limit from a fallen-back one, on the two limiters whose entire justification for a Redis round trip is that per-replica multiplication is unacceptable. Fix note: writing the local bucket on shared success is explicitly rejected at `ratelimit.go:208-212`, because a permanently empty bucket destroys failover; and `Fallbacks()` is monotonic, so a naive gauge alert latches forever after one transient blip | No |
| F103 | M44.9 | `GET /api/v1/workspaces` answers an API key with every organization its owner belongs to | `internal/auth/workspace.go:102` calls `ListWorkspacesForUser(actor.UserID)`; `internal/store/query/workspaces.sql:26-31` filters `m.user_id = $1` with no organization predicate; the route is `RequireAuth` with no permission | Returns id, name, slug, organization id, organization name and personal flag for every workspace the owner belongs to, in any organization. M44 spent an `organization_id` parameter (`auth.sql:145-153`) specifically to stop a key **acting** in a tenant it was not issued for — *"The key would then act in a tenant it was never issued for"* — and the same bound is absent from this read. `workspace.go:95-97` concedes there is no permission *"because it exposes nothing but the caller's own memberships"*, which is true of a person and not of a credential | Minor. Metadata only, no ability to act there, and the gap is credential-versus-person rather than tenant-versus-tenant. Predates M44 — it arrived with M25 and org-wide keys are not required to reach it. Fix note: adding the predicate to the shared query breaks the browser switcher, whose entire job is crossing organizations; the filter belongs in the service on `actor.IsAPIKey()`, which collides with the rule F104 is about | No |
| F104 | M44.9 | The Permissions rule says nothing may branch on credential type outside two named mechanisms; seven sites do, every one of them correctly | `docs/build-notes/phase-details/README.md:75`; sites at `internal/auth/apikey_rotation.go:151`, `internal/auth/workspace.go:135` and `:178`, `internal/httpx/api_auth.go:178`, `internal/httpx/web.go:408`, `internal/httpx/web_keys.go:277`, `internal/invite/invite.go:318` | Each site is separately licensed — D87 for rotation (*"the key's own token is the authorization: there is no id to name and no permission to hold"*), the M25 entry for the workspace switch, and `docs/usage.md:345-349` for password change (*"a leaked key must not be able to lock its owner out"*). **This is F12's shape a second time**: the recorded rule under-describes the tree it governs, and the tree is the one that is right. The rule was written about scope maps and has already been amended once for this reason. What is genuinely false is `requireSessionActor`'s own comment at `internal/auth/apikey.go:682-685` — *"`apikeys.*` is already non-delegable, so no key holds the permission these operations check"* — which holds for its three callers in that file and is false for `workspace.go:135` and `:178`, which check no permission at all, where the function is the only enforcement rather than defence in depth | Minor, latent, and it lands on the next milestone that reads the rule literally and removes a session gate it does not understand. Fix note: deleting `requireSessionActor` from `workspace.go` to satisfy the rule hands a leaked key the ability to move its owner's browser — and, since M44, its own landing workspace. Fix the sentence, not the calls | No |
| F105 | M44.9 | `notifications.workspace_id` is written on every M40 and M43 notification and read by nothing, while two comments state it produces a per-workspace inbox | `internal/store/query/notifications.sql:16`, `:31` and `:52` all key on `user_id` alone; `internal/notify/notify.go:483-501` (`toNotification`) never copies the column and `Notification` at `:67-78` has no field for it | `internal/notify/domains.go:45-47` and `internal/notify/automation.go:32-33` both assert, verbatim, that *"The notification carries the owning workspace so it appears in that workspace's inbox rather than wherever the reader happens to be standing."* No read path consults it, so it cannot reach any surface. `internal/notify/automation.go` also writes a body reading *"matched … in **this workspace**"* that the reader cannot resolve | Minor. Nothing leaks and nothing fails; the cost is a column written on every notification with no reader, and two comments that would stop the next person adding the filter. It is also the stated mitigation for F94 — its authors believed cross-workspace notifications were contained by an inbox scope that does not exist. Fix note: a naive `AND workspace_id = @ws` hides every organization-level notification, since disputes and audit-growth write NULL; the predicate must be `IS NULL OR = @ws`, and it must be added identically to the count and the preview or `notifications_user_unread_idx` stops serving them, which `notifications.sql:26-28` warns about | No |
| F106 | M44.9 | `DeleteOrganization` says the audit trail is all that survives; three rollup tables have no deleter anywhere | `internal/team/organization.go:211-212` and `:228-229`; `internal/store/query/organizations.sql:101-107`; against `internal/store/migrations/00400_analytics.sql:87`, `:102`, `:116` | `link_click_daily`, `link_dimension_daily` and `workspace_click_daily` carry `workspace_id` with no foreign key and no delete statement in any query file, so they outlive the tenancy. `click_events` and `visitors` are reclaimed by partition drop at `ANALYTICS_RETENTION_DAYS` (395 by default, `0` meaning never). The dispute half of this finding was **refuted**: the instance-wide open-dispute slot *is* reclaimable, because `decide` loads and writes by id with no organization predicate, so any holder of `destinations.review` can free the host, and the absent foreign keys are a recorded choice at `01600:49-52` | Minor, correctness-of-claim. The rows are unreachable by any reader, since every consumer scopes to a live `workspace_id` — so this is stale aggregate data and two inaccurate sentences rather than a disclosure. Fix note: adding foreign keys puts a constraint on the partitioned write path that `00400` and `00600:134` deliberately keep clean; delete the three rollups inside the existing transaction and correct the two comments | No |
| F107 | M44.9 | The per-destination breakdown credits split-arm clicks to the link's own destination for up to fifteen minutes, and always reports zero visitors for that row | `internal/analytics/reader.go:245-253` computes the primary row as `totalClicks - attributed`, where the total comes from a 60-second rollup and the attribution from the 15-minute rollup M37 introduced; `primary` is built at `:215-219` without `UniqueVisitors` and only `.Clicks` is assigned at `:251` | The comment licensing the clamp says the two figures *"can leave the totals a few clicks apart"* and was written at M36, before M37 split the cadence; the gap is now a quarter-hour of traffic and the shortfall always lands on the primary row. `internal/ui/templates/pages/link_detail.html:667` presents the column as *"attributed to the destination that served them"*. Worst case is a split test launched today and viewed inside its first fifteen minutes: 100% to the link's own destination, 0% to the arms. `Plan.md:826` records that the breakdown *lags*; it does not record that the lag is rendered as positive attribution to a named row. The zero-visitors defect is permanent rather than transient | Minor, upper end — wrong numbers on the one panel a split test is read from, worst at the moment somebody is most likely to look | No |
| F108 | M44.9 | Two concurrent folder moves can produce the cycle M38 says can never exist | `internal/link/folder.go:197` reads the tree, `:208` judges `MoveRefusal`, `:212` issues the update, with no transaction, no `FOR UPDATE` and no cycle predicate in `internal/store/query/folders.sql:77-84` | With A and B at the root, `IsAncestor(A,B)` and `IsAncestor(B,A)` are both false against the pre-move snapshot, depth and sibling-name checks both pass, and both updates commit. Falsifies m38.md's *"A folder can never become its own descendant"*. The tree assembler survives it — `NewFolderTree`'s visited set re-adds the unreachable pair as roots — and moving either to the root skips the cycle branch, so it is recoverable | Minor. Needs two moves of two specific folders inside the same read-to-write window; nothing is lost and one move undoes it. Same shape as F67 at a third site | No |
| F109 | M44.9 | A recipient's email address reaches the process log unredacted, on the first bounce | `internal/mail/mail.go:181` appends `sendErr` to the returned error on **every** attempt, which `cmd/linkctrl/jobs.go:260` logs at ERROR as `slog.Any("error", err)`; also `mail.go:189-193`. `internal/mail/smtp.go:113` wraps the relay's response line, which routinely echoes the address | `internal/observability/logging.go:59-63` redacts by attribute **name** and `error` is not in the list, so `550 5.1.1 <alice@example.com>: User unknown` passes through. No `slog` attribute anywhere else in the tree carries an address, so this defeats a convention the package otherwise keeps — and `internal/webhook/client.go:288-296` unwraps `*url.Error` for exactly this reason one package over. Reachability is higher than first reported: it needs one bounce on the first attempt, not exhaustion of the retry budget. `mail_outbox.last_error` is **not** part of this — `recipient` is already a column on that row | Minor, and it needs SMTP configured. It moves an address from the database, which is access-controlled and retention-bounded, into the log stream, which is typically shipped elsewhere with no LinkCtrl-side bound. Same mechanism as F34 in a second package | No |
| F110 | M44.9 | The audit read API stores, indexes, selects and scans a workspace id it never returns | `internal/audit/audit.go:474-500` (`toEntry`) copies ten fields and not `workspace_id`; `Entry` at `:244-259` declares no such field, and `api/openapi.yaml`'s `AuditEntry` declares none | The write stores it (`audit.go:338-341`), the query selects it (`audit.sql:29`) and the generated row scans it. `audit.sql:25-27` justifies not **filtering** by workspace — *"an audit log that can be narrowed to the workspace the reader happens to be in would hide exactly the actions worth reviewing"* — and says nothing about not returning it, so this is an undocumented second choice riding on a documented first one. Compensation is partial: `workspace.*` actions carry the workspace as `target_id`, but a link-scoped action such as `link.bot_blocking_changed` names only the link | Minor. Read-only reporting gap with no security consequence, and there is no audit UI, so this API response is the entire reader-facing surface. It compounds F31: a workspace-scoped admin already reads the whole organization's rows and cannot tell which came from workspaces they do not administer | No |
| F111 | M44.9 | `docs/cli.md:83` tells operators to set `MIGRATE_ON_START=false`, which the loader never reads | `docs/cli.md:83`; `internal/config/config.go:34` (`EnvPrefix = "LINKCTRL_"`) and `:556` (`env.ParseWithOptions(…, Prefix: EnvPrefix)`) | The struct tags are bare and the prefix is applied by the loader, so the unprefixed name is never consulted and the variable is not in `config.Removed`, meaning startup does not flag it either. The same file uses the prefixed spelling correctly at `:34`. Narrowed by verification: the other three cites in that file describe a *state* rather than giving an instruction, and mirror the binaries' own error strings, which read the prefixed variable correctly — so this is one line, not four, and `releasing.md`, `deployment.md` and `configuration.md` all spell it right | Minor to moderate, and the failure mode is the expensive kind: a change-controlled deployer suppresses a schema change at boot, gets migrations anyway, and receives no warning | No |
| F112 | M44.9 | `docs/SECURITY.md` promises key minting and revocation will be audited by the milestone that owns them; the code has decided against it, and that milestone has shipped | `docs/SECURITY.md:284-286` against `internal/audit/audit.go:211-214` | The document says the two *"arrive with the Phase 2 milestone that owns it"*; the code says their absence *"is **not an oversight**: both require an interactive session, so the person is the record"*. M44 owned the key lifecycle, shipped, and added only `apikey.rotated`. No decision entry records either position, so the document is not describing a recorded choice — it promises a capability the code has refused | Minor to moderate, documentation only, in the file a security reviewer trusts most, and it over-promises rather than under-reports. A compliance reader defers building their own key-lifecycle logging waiting for something that already came and went. This is a **second limb of F17**, which names the same paragraph but scopes its evidence to the enumeration of what *is* recorded | No |
| F113 | M44.9 | Nothing reaps an unverified hostname claim, so a registration holds a name instance-wide forever | `internal/link/verification.go:339-343` skips rows with `VerifiedAt == nil`; `domains_hostname_key` (`00300_links.sql:23`) is instance-wide and unconditioned on verification | Narrowed sharply by verification: the squat itself is a **documented limitation** — `Plan.md:815` states *"a second registration of the same name is refused instance-wide, whoever asks… no way to hand one over"* — and the uninformative refusal is deliberate and reasoned in-tree. The index is partial on `deleted_at IS NULL`, so deleting the domain does free the name, but only the holder can delete it. What is genuinely undocumented is that an unverified claim is never polled out of existence, so the squat is free and permanent rather than merely unhandable | Low. No confidentiality or integrity loss; denial of a name on one instance, and the operator's own hostnames are configured statically rather than through this table. Fix note: a reaper on a timer breaks the case `verification.go:339-343` protects — a hostname registered before DNS cut-over — and must not treat a NULL `verification_checked_at` as evidence of abandonment, since `RenameDomain` writes that NULL on a live row | No |
| F114 | M44.9 | Filing a dispute answers whether an open dispute already exists for any host | `internal/dispute/dispute.go:323-329` maps the unique violation to a conflict returned to the filer; `internal/httpx/web_disputes.go:128-129` renders it as a distinct outcome; `01600_destination_disputes.sql:81-82` carries no tenancy column | Combined with F95's observation that `url_credentials` blocks any host, the probe works for arbitrary targets with only `links.create`, needing no `destinations.review`. Narrowed by verification: **the oracle destroys itself** — a negative probe *creates* the open dispute it was testing for, notifies every owner, and leaves a queue row carrying the filer's identity, so each host answers truthfully exactly once and never quietly | Low. Leaks existence rather than content — no filer identity, no attempted URL, no organization — and F15 already grants every owner the full queue. Loud, one-shot per host, self-poisoning. Fix note: collapsing the conflict into the success answer breaks the honest case, where a second filer needs to be told their appeal is already queued; adding a tenancy column to the index removes the bound `01600:67-73` claims and the instance-wide semantics the service argues for, so it is a tenancy decision rather than a de-duplication tweak | No |
| F115 | M44.9 | D54's per-alias password limiter has an availability cost nobody recorded, and a correct password spends a token | `internal/httpx/redirect_gates.go:161` calls `passwordAllowed` before `ParseForm` and before verification; `:204-216` keys the second limb on the alias with both limbs consumed on refusal; `LINK_PASSWORD_RATE_LIMIT` defaults to 20 refilling one per three seconds | The keying itself is D54's recorded choice and arguing it is re-litigation — the decision reasons explicitly that *"an attacker driving attempts through many visitors' browsers spreads them across addresses and walks past a per-address limit"*, and consuming both limbs on refusal is documented in the function's own doc. What is **not** recorded: D54 states the fail-open cost and no other, so nothing anywhere says a stranger can hold a password link's bucket empty and lock out its intended audience at roughly one request every three seconds. And verification found a limb nobody filed — because the limiter runs before the form is parsed, a link with more than twenty legitimate visitors in a burst throttles itself with no attacker present | Minor as a defect, since the mechanism was chosen deliberately; the row is for the missing risk note against D54 and the self-throttling limb. Fix note: dropping the alias limb or keying it on address-plus-alias reopens the distributed-guessing hole D54 exists to close, and D53's CSRF waiver is load-bearing on D54 — so any change here reopens D53. Refunding the alias token on a **correct** password touches neither decision | No |
| F116 | M44.9 | With `forward_path` on, the deep-link remainder is visitor-controlled, unbounded to 1 MiB, and walked or copied several times per request | `internal/httpx/redirect.go:721-761`; `internal/platform/httpserver/server.go:43-56` sets `Addr`, `Handler`, `ReadHeaderTimeout`, `WriteTimeout` and `ErrorLog` and never `MaxHeaderBytes` | No length check exists between the router and the joiner. `joinable` walks every `/`-separated segment calling `url.PathUnescape`, then the concatenation, the unescape of the join and `u.String()` each copy the whole remainder — roughly five passes over up to a megabyte, on a request that touches no database. A cache hit is never charged to the 404-probe limiter, and `docs/slo.md:913-917` records that as deliberate | Minor, corrected down from the tier it was filed at: it is CPU and allocation amplification rather than disclosure or corruption, it needs `forward_path` switched on per link, and 1 MiB is a real ceiling rather than none. Fix note: capping total length alone misses it, because `joinable`'s per-segment unescape is driven by segment **count** — a 40 KiB path of 20,000 empty segments is 20,000 allocations. Bound both, and refuse through the existing not-forwardable 404 rather than inventing a 414 on a tree whose header invariants F64 already shows are fragile | No |
| F117 | M44.9 | `canAdminister`'s organization limb authorizes an organization-owned domain from the identity's permission union rather than the covering membership | `internal/link/domains.go:131-143`, the `organizationID != nil` limb, which compares only `actor.OrgID` after a bare `actor.Can(PermDomainsWrite)` | This is D44's rule unapplied — *"a write is authorized by the membership whose scope covers the object being written, not by the identity's union"* — so a workspace-scoped admin's union would reach an organization-owned row. Latency verified on four independent checks: `CreateDomain`'s only caller writes both owner columns, no update statement touches `workspace_id`, the seeded default row has a NULL organization, and `domains.workspace_id` is `ON DELETE CASCADE`, so deleting a workspace removes the row rather than promoting it | Low and latent — the state is unconstructible through any shipped or migrated path, and the CHECK at `02500_domain_ownership.sql:48-52` is what legalises it. It becomes real the moment anything registers an organization-owned hostname, and whoever adds that surface has no reason to look here. Fix note: replacing all three limbs with the organization-wide check is wrong — only the middle limb is D44's, the workspace limb is correctly the union under D31, and the instance-default limb is F70 and D38's unanswered question about who administers an instance. Also `Domains()` calls this per row, so a per-call authority load is a query per row | No |
| F118 | M44.9 | Teardown writes alias reservations that the same statement's cascade deletes, for links on an organization- or workspace-owned domain | `internal/store/migrations/00300_links.sql:174` keys `reserved_aliases` to `domain_id` with `ON DELETE CASCADE`; `domains.organization_id` (`00300:12`) and `domains.workspace_id` (`02500:29`) both cascade from their parents; against `internal/team/organization.go:305` and `internal/team/workspace.go:243` | The reservation inserted immediately before the delete is removed by the cascade of that delete. Severity corrected sharply downward by verification: F28's exposure is the **shared default domain**, whose `organization_id` is NULL and which is untouched — `organizations.sql:104-107` says so and both regression tests use it — so the path that matters is intact. For a custom hostname the domain row is destroyed too, and re-serving that alias would require re-registering the hostname *and* passing M40's TXT check, at which point whoever does that controls the name anyway | Cosmetic. Self-defeating bookkeeping rather than self-defeating security: wasted inserts on every teardown that owns a custom domain, and an overstated sentence at `organization.go:216-225`. Fix note: `RESTRICT` makes organization deletion fail outright, `SET NULL` is impossible because `domain_id` is half the primary key, and re-keying reservations by hostname would reserve aliases on a name whose next owner proved control of it | No |
| F119 | M44.9 | M33 is the only shipped Phase 2 milestone with a demo-visible feature and no `demoCoverage` row | `cmd/lctl/demo_coverage_test.go` — the enumeration runs M32.5 → M34 with no M33 entry; the seeded feature is `cmd/lctl/demo.go:69-70`, `forwardPath: true`, carried to `demo_data.go:109` | `git log -S forwardPath -- cmd/lctl/` returns exactly one commit, M33's own. m33.5.md names M33 by name as the milestone that shipped in the window where the obligation existed and nothing enforced it, and M33.5's whole deliverable is the enforcement. Deleting `forwardPath: true` today leaves the build green. Every other absent milestone in the range seeds nothing visible. Verification also confirmed the more important half: **every trailing zero-asserting boundary row was converted as its milestone landed**, and no `MaxIsZero` row remains for an unbuilt milestone — with the consequence that `phase-details/README.md:80` still says *"Its four trailing rows"* when there are now none, which is F69's predicted drift, four off rather than one | Cosmetic. The demo is correct today; only the mechanism that keeps it correct has a hole, at the one milestone the file singles out. The trailing-row half belongs to **F69** rather than here | No |
| F120 | M44.9 | Both `Roles` functions filter on the identity's union rank, so a control offers a role the service then refuses | `internal/team/team.go:254` and `internal/invite/invite.go:485` both filter on `actor.RoleRank`, while the ceilings read `here.Rank` (`team.go:211`) and `orgWide.Rank` (`invite.go:302`) | An organization-wide viewer who also holds a workspace-scoped admin row has a union rank of the stronger membership, so `Roles` lists admin while `invite.Create` compares against the organization-wide rank and refuses. `invite.go:296-301` names the exact hazard it did not fix. Both doc comments claim the opposite — *"a control cannot offer something `resolveRole` will then refuse"* | Cosmetic. Nothing is reachable that the service permits; both ceilings hold and D44 is not weakened. The cost is a dropdown that 403s and three comments asserting it cannot. F62 covers the same function on the credential axis and its severity argument rests on `Roles` applying the ceiling, which this falsifies | No |
| F121 | M44.9 | Six redirect-tree pages carry a second, hand-written palette that M24.5's scan cannot see | `internal/httpx/static/404.html`, `410.html`, `403.html`, `403-signature.html`, `password.html`, `password_wrong.html`; the scan at `internal/ui/theme_test.go:37` walks `internal/ui`'s `templates` embed only | Four of the six were added **after** M24.5 — `403.html` by M32.5 and the other three by M35 — and both milestones inherit *"new UI colors use the theme tokens"*. Severity corrected down by verification: the live defect does not survive, because m24.5.md scopes the override to *"every **dashboard** page"* and already exempts a non-dashboard surface, these pages are embedded byte slices with no template, and since M40 the redirect tree can answer on a hostname the dashboard's theme cookie never reaches. They do honour `prefers-color-scheme`, which is more than the milestone promised them | Cosmetic — an inherited-rule breach with no behaviour it contradicts. Sibling of F40, which is the same blind spot in a different subtree and carries the same "no live defect" framing | No |
| F122 | M44.9 | Three code comments describe an organization-wide API key's reach as something the credential does not have | `internal/auth/apikey.go:557-558`, `internal/auth/workspace.go:130-133`, `docs/cli.md:131` | The **behaviour is licensed and is not a finding**: D90 records that an organization-wide key is *"resolved the same way a login is"* and *"still follows the person it acts as — pinned default included — within its own organization"*, fixing only the tenancy as a bound rather than a precedence rung, and `docs/SECURITY.md:51` and `docs/usage.md:365-368` say the same in product voice. What is false is the reasoning: `apikey.go:557-558` claims *"No session id: a key is not a browser, and a switch made in one must not move a key's requests"* — omitting the session id removes only the first precedence rung, and the switcher's second write lands on `users.last_workspace_id`, which decides for every account that has not pinned a default. `workspace.go:130-133`'s inference inverts for the NULL-workspace key M44 introduced. `cli.md:131`'s *"valid in every workspace of the organization"* is unqualified, and there is no per-request workspace selector anywhere in `internal/httpx` | Cosmetic, documentation and comments only. Filed because two independent readers reached the behaviour and filed it as a high-severity defect before D90 refuted it — which is what a misleading comment costs. Fix note: pinning the key's workspace at issue time reverses D90 and breaks the test that asserts it | No |
| F123 | M44.9 | The shared limiter's circuit breaker has no half-open state, so every concurrent request retries when the cooldown lapses | `internal/ratelimit/shared.go:250-257` — `allow()` reads under the mutex, sets nothing, and marks no probe in flight; `openUntil` is re-armed only in `recordFailure` at `:267`, after the failed call returns | The comment at `:228-233` promises *"an outage costs the timeout a few times and then nothing until the cooldown lets **one** request through to check"*, and `:61-62` repeats it. Severity corrected down by verification: the cost is `REDIS_READ_TIMEOUT` (50ms by default) once every five-second cooldown, a one per cent duty cycle rather than a stall, and it only becomes the herd the finding describes if an operator raises that timeout | Cosmetic to minor at defaults, moderate at a retuned read timeout. The comment is what the next person would trust when deciding whether the breaker needs work. Fix note: a compare-and-swap half-open must re-arm `openUntil` when the probe is **dispatched**, not on its result, or the probe's own timeout window is an open door; and a failed probe must re-open immediately rather than waiting for the failure threshold again | No |
| F124 | M44.9 | The IPv6 rate-limit key is documented in the code as covering a case it does not | `internal/ratelimit/ratelimit.go:13-14` and `:314-320` say *"A single host is routinely handed a /64 **or larger**"* while `Key` at `:321-332` folds to `/64`; `docs/configuration.md:384` drops the qualifier entirely | A delegated `/48` or `/56` yields many distinct buckets at no cost. Severity corrected down to documentation by verification: `/64` is the standard floor and coarsening commits the exact error F57 ruled out on the v4 side, where a wider key lets one abusive host throttle its neighbours. The threat named is separately covered by a control the finder did not check — per-account lockout at `internal/auth/session.go:188-199`, five attempts per fifteen minutes — and `internal/httpx/router.go:129-132` states the pairing outright | Minor, documentation only. The `/64` choice stands; the code comment overstates what it achieves and the operator document drops the qualifier that would let a reader notice | No |
| F125 | M44.9 | `disputes.sql` documents a scoping guarantee its only caller defeats | `internal/store/query/disputes.sql:93-95` against `internal/dispute/dispute.go:536` and `:551` | The comment says a decision about `login.evil.example` *"cannot take 'evil.example' off the list by accident"*; the caller passes `link.HostCandidates(d.Host)` — the host and every parent — to `MatchBlockedDestination`, which returns the longest match, and then deletes exactly that row. The first clause is accurate: the delete really is a literal equality on the row that matched, with no LIKE and no candidate walk in SQL. Only the trailing claim is false | Cosmetic. The behaviour is F33's and is recorded there with sharper evidence; this is the shipped file that states the opposite, in the one place a reader would check the scoping question. Fix note: deleting the comment is wrong, because the parent match is deliberate — an allow must lift the row that actually refused. F33's own repair, storing the matched row on the dispute at filing time, makes this comment true as written | No |
| F126 | M44.9 | A migration comment states a salting mechanism the schema does not implement | `internal/store/migrations/00400_analytics.sql:38-39` — *"Per-workspace salting means the same person is not correlatable across workspaces"* — against `:19-28`, where `analytics_salts` is `valid_on date PRIMARY KEY` with no workspace column | `decisions.md:326-329` already records the correction: *"The workspace is in the message, not the key… this corrects an earlier claim that the salts themselves were per-workspace."* The formula on the same line is right and `internal/analytics/visitor.go:46-48` states the mechanism correctly, so the privacy property the sentence claims does hold — only the stated mechanism is wrong | Trivial. Filed because this comment is where a reader auditing the privacy design starts, and it holds a reading a decision entry already superseded | No |
| F127 | M44.9 | `CreateOrganization`'s zero-membership guard claims a serialization it does not have | `internal/team/organization.go:92-93` claims the count and the writes that invalidate it *"cannot be separated by a concurrent redemption"*; `internal/store/query/organizations.sql:120` is a bare `SELECT count(*)` with no row lock | Running it inside the transaction gives read-committed visibility, not serialization, so a redemption committing after the read is invisible — which is the check-then-act the same file's preamble warns about at `:6-16` and which `LockOrganizations` avoids. The consequence is bounded by the guard's own reasoning at `:85-90`: an account with no memberships can reach exactly one operation whose whole effect is to give that account an owner membership, so losing the race yields one extra organization the account legitimately owns | Trivial. A comment promising serialization it does not have, guarding an operation where losing the race costs nothing. Same family as F67 | No |
| F128 | M44.9 | Three invitation-redemption outcomes spend twice the argon2 work of every other, against a claim stated three times | `internal/invite/invite.go:596-600` (`refuse` spends a dummy verification) reached after the real verification at `:655`, from `:673`, `:692` and `:707` | Undercounted when filed: three outcomes cost two runs, not one — "already a member", "membership already exists" and "invitation was spent concurrently". Wrong-password returns without `refuse` and costs one. That falsifies the literal wording at `:10` and `:563-565`, *"every refusal in Redeem… spends the same argon2 work"*. It does **not** falsify what the design protects: nothing answers *"does this address have an account"*, because the no-account branch creates a user, the unusable-account branch costs one dummy, and wrong-password costs one real — and reaching the doubled path requires a **correct** password | Trivial. The comment is false in letter with the stated property intact; the only thing the timing separates is already-a-member from wrong-password, for somebody who already holds the credential | No |
| F129 | M44.9 | `/tls-check` performs a database write with no deadline from any layer, on an unauthenticated route outside every application middleware | `internal/httpx/redirect_hosts.go:150-157` — `h.Activate(context.WithoutCancel(r.Context()), d)` runs synchronously in the request goroutine; `registerOps` (`internal/httpx/router.go:663-676`) mounts it on four muxes, none of which pass through `appHandler` | Sharper than filed, and the tree falsifies itself: every other detached context in the redirect tree pairs the two calls — `WithTimeout(WithoutCancel(ctx), …)` at `internal/redirect/resolver.go:185`, `:407`, `:512`, `:589` and `internal/redirect/hosts.go:133` — and `redirect_hosts.go:156` is the only `WithoutCancel` in the tree with no timeout beside it. `appHandler` is where `RequestTimeout` lives (`router.go:647`), and neither pool sets a `statement_timeout`, so the write is bounded by nothing. It runs on the application pool from a redirect-tree mux, which is F48's crossing. Mitigations check out and remove the amplification angle: the write is triple-guarded — cached pending status, the in-process mark flipped first, and `WHERE ssl_status = 'pending'` in the statement — so it is genuinely one write per verification, and `docs/SECURITY.md` both discloses that and mandates loopback | Minor, lower end. One unbounded goroutine per verified-pending domain, on an endpoint the deployment guidance tells operators to keep off the internet. Same class as F48 on a different route, and a write rather than a read | No |
| F130 | M44.9 | The guard that closed F39 has nothing holding it in place — the fix's own branches execute against zero inputs | `internal/httpx/web_shell_test.go:196-200` (recording an embedded field as declared) and `:111-140` (`declaredNames` walking non-shell promotions) | **Proved by performing the revert**: deleting the three lines that record an embedded field leaves `TestNoPageDataStructShadowsTheShell` and the whole `internal/httpx` package green. An AST scan of all 25 non-test files in the package confirms every page struct embeds exactly `shell` and nothing else, so the recursive walk never fires. One premise corrected — `embeddedName`'s package-qualifier stripping *does* run, via `internal/httpx/timing.go:67` and `web_keys.go:16`, though neither struct embeds the shell so the names are computed and discarded; the pointer branch has genuinely zero inputs anywhere. F39's sabotage was shown red and then deleted along with the file that carried it | Minor. No live defect — the guard is correct today — but it is F39's own finding one level up: a mechanism believed to work with nothing that fails if it stops. The comment at `:31-41` names three shapes it defends against and the package contains none of them. Sibling of F40, which is the analogous gap in the theme guard | No |
| F131 | M40 | `MarkDomainVerificationFailed` is still addressed by id alone, so a failed check can write its sentence and its watermark onto a row that was renamed while the lookup was in flight | `internal/link/verification.go:185-186` and `:388-389` call it with `{ID: row.ID}`; `internal/store/query/domains.sql` (`MarkDomainVerificationFailed`) is `WHERE id = $1 AND deleted_at IS NULL` | The same read-check-write gap F76 closed on the *success* path, left open on the failure path deliberately: M40's reopening scoped the conditional write to the two sites that start serving. Reproducible by the same means as F76 — the race apparatus is `stubZone.interrupt` in `test/integration/custom_domains_test.go` | **Low.** Grants nothing and stops nothing: the row a stale failure lands on is unverified either way, because a rename clears `verified_at`. What it produces is a `verification_error` sentence naming the old name on the new row, shown on the Domains page, and a `verification_checked_at` the new name did not earn — which moves it *out* of the head of the pending queue rather than into it. Fix is one predicate, matching `MarkDomainVerified` | No |

## Closed

A row leaves *Open* when the work that makes it untrue has landed, and the
milestone that closed it is named. The finding itself is not edited — the
evidence is what it was, and rewriting it would hide the shape of the defect
from anyone reading back.

| # | Found in | Finding | Where | Evidence | Severity | Reviewed | Closed by |
| --- | --- | --- | --- | --- | --- | --- | --- |
| F29 | M32.9 | An API key holding `members.write` can mint a full interactive account, reaching every scope `NonDelegableScopes` exists to keep off keys — and the resulting account survives revoking the key | `internal/invite/invite.go:227` (`Create` does not call `requireSessionActor`) with `internal/httpx/router.go:204` (`POST /invitations/redeem` needs no credential) | Every link is already proven by an in-tree test. `test/integration/invite_test.go:835` (`TestMembersWriteIsDelegableToAnAPIKey`) asserts on purpose that a key scoped to `members.write` alone gets 201 from `POST /api/v1/invitations` over the bearer path; `invite_test.go:225` shows an owner may invite at `owner`; `Created.URL` returns the raw token in the 201 body, so no mail interception is needed; `invite_test.go:500` shows redemption creating the account when `NewAccounts` is true. The resulting session is not a key, so `requireSessionActor` no longer refuses. Two corrections that widen it: `members.write` is held only by owner and admin (`00700_seed.sql:53-60`), so **there is no benign instance** — every key that can legally hold it escalates to at least admin, including `apikeys.write` and `audit.read`; and `SIGNUP_MODE=closed` is a weaker bound than it reads, because redemption under `closed` still succeeds for an address that already has an account, and `RemoveMember` deletes the membership rather than the account | **Major.** Falsifies the user-facing claim at `docs/SECURITY.md:45` — *"The role an invitation may carry is capped at its issuer's own rank, which is why `members.write` is safe to delegate to an API key"* — and D28's reasoning, which weighs only the rank axis while `NonDelegableScopes` governs the credential-type axis. `docs/SECURITY.md:238` recommends `closed` **or `invite`** as a safe posture, and `invite` is the vulnerable mode. Held below critical by the `closed` default, the audit trail, and the need to already hold an owner- or admin-issued key | Yes — owner approved 2026-08-01 on M32.9's triage; **reopens [M27](phase-details/m27.md)**. It is a decision before it is a fix: D28 reasons on rank, `NonDelegableScopes` governs credential type. | **M44.9**, which found the row itself rather than the defect. The fix landed at `524b793` with D43 — `KeyIssuableRoles` caps a key-issued invitation to `editor` or `viewer`, enforced at `internal/invite/invite.go:318-324`, the one chokepoint every invitation path funnels through, so the web surface is bounded as well as the API one. That commit edited this file to add F60, F61 and F62 and did not move this row, so it stayed in *Open* asserting a defect the tree refuses. M44.9's regression check re-confirmed the cap holds and `test/integration/invite_test.go:890-926` drives the chain. Approved for the move 2026-08-04. |
| F26 | M32.9 | A trailing dot on the hostname defeats the unappealable tier **and** the embedded high-confidence list — `http://169.254.169.254./`, `http://127.0.0.1./`, `http://localhost./` and `http://metadata.google.internal./` are all accepted and stored | `internal/link/destination.go:198` (`looksNumeric` returns false on the empty last label), `:169` (the `localhost` equality test), and `internal/link/blocking.go:218` (a bare `embeddedHosts[host]` lookup) | Reproduced live on :8081 through the real handler: `POST /api/v1/links` answered **201** for `http://169.254.169.254./latest/meta-data/` and **422 `unappealable.private_address`** for the dotless control; `PATCH` is affected identically; the stored value is 302'd verbatim at serve time and **no `destination.blocked` audit row is written** for any dotted attempt. Confirmed independently against HEAD's source in a scratch tree, and the image was checked not stale for `internal/link/`. A second verifier, working the design angle, established the correction that matters: **LinkCtrl never fetches a destination server-side** — the only two `http.Client`s in the tree are the opt-in feed and the container healthcheck — so this is a stored **open redirect**, not SSRF, and the attacker cannot read the response. That verifier widened the affected spellings to nine and validated a one-line fix (`strings.TrimSuffix` on the folded host at `destination.go:130`) that closes all three tiers with `go test ./internal/link/` green and no test edits — including `destination_test.go:133`, which requires `https://example.com./` to stay accepted. The DB tier already trims (`HostCandidates`, `blocking.go:298`) and `checkListEntry` already refuses a dotted *entry*, so the write side normalises and the read side does not | **High.** Falsifies m30.md's *"no configuration, list entry, or future review path can accept a metadata or private address"* and the embedded tier's *"editing the list and rebuilding"*. Not critical: no server-side fetch, so the victim's own browser makes the request and the attacker learns nothing. `localhost.` needs no WHATWG argument — glibc resolves it, and `curl http://localhost.:8081/healthz` returned 200 | Yes — owner approved 2026-08-01 on M32.9's triage; **reopens [M30](phase-details/m30.md)**. The fix must canonicalise the trailing dot, never reject it: `destination_test.go:133` asserts `https://example.com./` is accepted and is right to. | M30, 2026-08-02, reopened for it. Closed by folding the trailing dot away in `ValidateDestination` — one place, before the address, numeric and localhost checks — so that every tier reads a host that has already been canonicalized (D46). Canonicalized rather than refused, as this row's *Reviewed* cell required: `https://example.com./path` is accepted and **stored** as `https://example.com/path`, so the value a visitor is later handed is the value the tiers judged. This row's evidence is right on every point and is left as written, including its four `Where` sites. One correction, and it is the reason the fix is not the one-liner the row validated: `strings.TrimSuffix` trims a single dot, and `http://127.0.0.1../` has an empty last label after one trim just as `http://127.0.0.1./` does before any — measured, by running the row's proposed fix against the new test, which accepted it. `strings.TrimRight` is what closes the spelling as well as the character. `looksNumeric`'s empty-label branch, which this row correctly names as the mechanism at `destination.go:198`, now answers true rather than false; nothing reaches it once the fold is in, so it changes no behaviour and exists so the helper fails closed if a third caller appears. The test gap the row exposes is closed too: `TestUnappealableTierHasNoOverrideSwitch` guarded a promise about what may be *accepted* with a reflection walk over `DestinationPolicy`'s *fields*, so it never tried a host. `TestATrailingDotDoesNotDefeatAnyTier` (`internal/link/blocking_test.go`) feeds each dotted spelling against its dotless control through all four mechanisms and the embedded list, and `TestATrailingDotIsRefusedAndRecorded` (`test/integration/blocking_test.go`) holds the consequence this row found and the unit tests cannot see — the refusal now writes its `destination.blocked` row, on create and on update, where before there was no refusal and therefore no record. Both were shown red against the shipped behaviour first. |
| F28 | M32.9 | Deleting a workspace or an organization cascades away **trashed** links without writing their aliases to `reserved_aliases`, so a trafficked alias returns to the namespace for anyone on the instance to re-register | `internal/store/query/members.sql:140` and `organizations.sql:65` (both emptiness guards exclude `deleted_at IS NOT NULL`), `internal/team/workspace.go:213`, `internal/team/organization.go:271`, cascade at `00200_identity.sql:66` → `00300_links.sql:50` | Found independently by two readers that could not see each other's work — the M28/M28.5 claim audit and the cross-seam sweep — which is the same shape as Phase 1's headline finding (a missing purge job inverting the alias-reservation promise). Reproduced against :8081 inside `BEGIN … ROLLBACK`: a trashed link with `click_count = 4242`, both guards counting **0**, `DELETE FROM organizations` → `DELETE 1`, link row gone, **`reserved_aliases` rows written: 0**, and `IsAliasTaken` flipping **`t` → `f`**. The obvious counter is dead: `IsAliasTaken` (`redirect.sql:71-88`) deliberately has *no* `deleted_at` filter — *"a soft-deleted row holds its alias for the whole trash window"* — so the alias was not already free; the cascade is what releases it. Only `PurgeExpiredLinks` writes the reservation, and it never sees these rows. `DeleteWorkspace` does not refuse when trashed links exist | **Major.** Falsifies m28.5.md's *"There is no alias left to reserve: an organization that reaches this line holds no links … and the ones that had received traffic are already in `reserved_aliases` where the purge job put them."* Needs no attacker — trash a link that had traffic, tidy up the workspace within 30 days. Aliases are per-domain on the shared default domain, so the freed alias is instance-global, and `00300_links.sql:165` names the outcome: *"handing it to a different destination is a redirect hijack waiting to happen"*. The workspace half predates M28.5; M28.5 added a second door and asserted it was safe | Yes — owner approved 2026-08-01 on M32.9's triage; **reopens [M28.5](phase-details/m28.5.md)**. Must cover `DeleteWorkspace` as well as `DeleteOrganization`; the workspace half predates M28.5. | | **[M28.5](phase-details/m28.5.md)**, reopened for it. `ReserveWorkspaceTraffickedAliases` and `ReserveOrganizationTraffickedAliases` write the reservation inside each delete's transaction, at `PurgeExpiredLinks`' `click_count > 0`, locking the rows they are about; both doors are asserted by `TestDeletingAWorkspaceReservesItsTraffickedTrashedAliases` and `TestDeletingAnOrganizationReservesItsTraffickedTrashedAliases`. Reserve-rather-than-refuse is [D45](../../Plan.md#phase-2-decisions-taken-after-the-plan-was-finalised). The false paragraph is rewritten in all three places that carried it |
| F27 | M32.9 | M28's workspace-scoped memberships confer **organization-wide** authority: a workspace-scoped admin can grant themselves into any workspace, re-role and remove org-wide members, issue org-wide invitations, and — with no `Grant` at all — promote their own org-wide row to admin. A workspace-scoped **owner** additionally holds `org.delete` over the whole organization | `internal/team/member.go:262` and `:282` (`Grant`), `:111` (`ChangeRole`), `:187` (`Remove`), `:362` (`manageable`), `internal/team/team.go:156` (`mayManage`), `:191` (`resolveRole`), `internal/invite/invite.go:227` (`Create`), `internal/team/organization.go:226` (`org.delete`) | Reproduced end to end against the real services on a throwaway database. Controls first: acting in W1, Alice is **refused** `RenameWorkspace(W2)` and `DeleteWorkspace(W2)` by `writableWorkspace` — *"changing that workspace requires workspace.write in it"*. Three calls later, after `Grant(self, W2, admin)` succeeded, the same guard passes and she renames W2. Separately and needing no `Grant`: she holds org-wide `viewer` (rank 40) and workspace-scoped `admin` (rank 20); `GetUserRoleInWorkspace` takes the minimum, so `mayManage(20,40,10)` is true against **her own** org-wide row, which comes back `IsSelf: true` **and** `Manageable: true` — one dropdown on `/members` makes her an organization-wide admin. `refuseLastOwner` fires only on demotion, so promotion is unguarded; from there she can remove the real owner. The premise is a supported path: `resolveRole` refuses only `role.Rank < actor.RoleRank`, so `10 < 10` is false and an owner may grant **owner** scoped to one workspace, and `Roles` offers it. `internal/team/workspace.go` already does the right thing via `canInWorkspace`, with a comment naming this exact attacker — no member operation calls it. The UI does not limit it: `members.html:33-39` ranges over all of `.OrgWorkspaces` without filtering on `Manageable`. Every `Grant`/`ChangeRole`/`Remove` in `test/integration/team_test.go` uses `f.owner` as the actor; the one workspace-scoped identity the suite builds is only ever read | **Major.** Falsifies m28.md's *"An admin cannot demote themselves — self is not strictly below self"*, and contradicts `members.sql:69-71` (*"a workspace-scoped owner membership grants ownership of one workspace, not of the organization"*) and `team.go:236-238`. Reachable through the shipped UI in three dropdown picks, no forged request, no owner involvement after the initial grant. The UI describes the grant as *"It adds to whatever they already had"* | Yes — owner approved 2026-08-01 on M32.9's triage; **reopens [M28](phase-details/m28.md)**. It cannot be fixed at `Grant` alone — the self-promotion route uses no `Grant` at all. | M28, 2026-08-02. Closed by D44: a write is authorized by the membership whose **scope covers its target**, not by the identity's union. `auth.MembershipAuthority` (`internal/auth/authority.go`) over a new `ListMembershipAuthority` query answers, per scope, whether the actor holds the permission there and at which rank; `In(nil)` is the organization-wide scope, which only an organization-wide membership reaches. `manageable` — shared by `ChangeRole` and `Remove` — resolves it against the target membership's own scope before any rank is compared, and `resolveRole`'s ceiling now reads that rank rather than `Identity.RoleRank`, which closes the second route this row's evidence did not reach: an organization-wide **admin** who also held a workspace-scoped **owner** row would otherwise have carried rank 10 against organization-wide targets. `Grant` resolves the authority in the workspace being granted into, which is the same question `canInWorkspace` already asked for `workspace.write`. `invite.Create` and `DeleteOrganization` require the organization-wide scope, because what they produce and destroy is organization-wide. `refuseLastOwner` became `guardOwnerSet`, called on promotion as well as demotion so every change to the owner set takes the same lock — its promotion refusal is deliberate depth rather than the operative guard, since after D44 minting an organization-wide owner structurally requires being one; the decisions entry says so plainly. `members.html`'s workspace select is filtered where the page loads, and `Members` computes `Manageable` per row against the same authority. Five integration tests drive a workspace-scoped identity as the **actor** of a write — the gap this row named as the reason the suite was green — and all five were shown red first by neutralizing the scope filter, which reproduced the escalation including the rows it wrote. This row's evidence is right on every point and is left as written; its line numbers have since moved. Out of its scope and left open: [F31](#open), which shares the root but falsifies M21's claim, and [F63](#open), raised while building this. |
| F39 | M32.9 | `TestNoPageDataStructShadowsTheShell` never inspects **embedded** fields, so the exact F20 defect can be reintroduced by an ordinary refactor while the guard reports green | `internal/httpx/web_shell_test.go:126` — `readStruct` routes every anonymous field into `s.embedded` and never into `fieldOrder`, which the clash loop at `:54` is the only thing that iterates | Reproduced in a fresh scratch copy with three shadow shapes — a shared mixin naming `Path`, a local type named `Workspaces`, and an embedded `*auth.Identity`. **All three compile** (the attack's hypothesis that Go's ambiguity rules would reject them is false: `shell` *names* `Identity`, so an embedded one sits at depth 0 and wins), and the guard **passes**. Control with the named-field F20 shape fails correctly, so the test is not vacuous. Rendering the three through `text/template` reproduces F20's failure modes: two 500 on a plain field access, and the third renders an **empty workspace switcher silently**. No current page struct triggers it, but `struct{ shell; someMixin }` is the ordinary refactor that would — factoring `Notice`/`FieldErrors`/`Path` out of the dozen page structs that repeat them | **Moderate.** Falsifies m28.md's reopening, which asked for *"a test that fails if any page data struct in `internal/httpx` redeclares a field the shell already provides — the mechanism, not a comment"*: under Go's spec an embedded field **is** a field declaration. The test's own comment says *"Two limits, stated rather than hidden"*; this is an unstated third, which makes the comment worse than silence. Fix is one line in `readStruct` | No | M28, 2026-08-02. Closed under [phase-loop.md](phase-loop.md)'s *deferred overlap* rule rather than by owner approval — it falsifies M28's own first reopening, which asked for *a test that fails if any page data struct in `internal/httpx` redeclares a field the shell already provides*, and under Go's spec an embedded field **is** a field declaration. The exception is named in the commit message. `readStruct` now records every embedded field under the name Go gives it — the type identifier with any `*` and package qualifier dropped, so `*auth.Identity` is a declaration of `Identity` — and the clash check additionally walks the fields promoted from anything embedded that is *not* on the path to `shell`, which is this row's mixin shape: it does not shadow the shell's field, it makes the selector ambiguous, and a template resolving an ambiguous selector fails at render the way F20 did. Structs on the path to `shell` are skipped, because each is checked in its own right by the same loop. The test passed before the change, so it was shown red first against all three shapes recorded here — `sabotageOne` (mixin naming `Path`), `sabotageTwo` (embedded local `Workspaces`), `sabotageThree` (embedded `*auth.Identity`) — and the sabotage was removed by deleting the file that carried it. The *Two limits* comment this row called worse than silence now says one limit (methods) and names embedded fields explicitly. |
| F30 | M32.9 | The invalidation subscriber never detects a Redis that holds the connection open and stops answering: it blocks in `ReceiveMessage` with no deadline, never reconnects, never flushes, and `establish`'s liveness ping reports healthy | `internal/redirect/invalidation.go:207` (`ps.ReceiveMessage(ctx)`) and `:238` (the `ps.Ping(ctx)` health check) | Two go-redis facts verified at the installed v9.21.0 rather than taken on trust: `ReceiveMessage` is a bare loop over `Receive` → `ReceiveTimeout(ctx, 0)` → `deadline()` returning `noDeadline`, so `ReadTimeout` never applies; and `PubSub.Ping` is **write-only** — it writes the command and never reads a reply, so a nil return proves only that bytes entered the socket buffer. Reproduced on a purpose-built harness (not the finder's): real Redis behind a proxy discarding bytes both ways while holding sockets open, with a separate publisher. **Redis reported delivering the invalidation to 1 subscriber and it was silently lost**; `ReceiveMessage` stayed blocked 40s; `establish`'s ping returned nil in 0ms. Control with the socket *cut* — the only shape the existing tests cover — errored in 0.1s and recovered correctly. Keepalive measured off a real fd: `KEEPIDLE=300s INTVL=15s CNT=9` → death at **7m15s**, but only when probes go unanswered; an application-level stall (wedged Redis, proxy, sidecar) is ACKed by the peer kernel and the block is genuinely unbounded, proved by scaling keepalive to ~4s and still blocking at 30s. Note for whoever fixes it: go-redis's `Channel()` API would **not** help — its health check uses the same write-only `Ping` | **Major.** Falsifies m23.md's *"A dropped subscriber connection reconnects and degrades to TTL in the meantime — never to silence mistaken for freshness. Tested"* and `invalidation.go:238-240`'s claim that a successful ping means the subscription is *"genuinely live rather than merely not-yet-failed"*. Stale window is the remaining entry TTL, up to `REDIRECT_TTL` (24h default), because `mem.get` short-circuits before Redis is consulted; Redis itself stays correct, so it self-heals at expiry. Multi-replica only — the editing replica clears locally first. The test proxy already has a `stalled(true)` mode; it is pointed only at the writer | Yes — owner approved 2026-08-01 on M32.9's triage; **reopens [M23](phase-details/m23.md)**. Not via go-redis's `Channel()`, whose health check uses the same write-only `Ping`; the fix is a bounded read. | M23, 2026-08-01. Closed by bounding the subscriber's read with a new `REDIS_SUBSCRIBER_READ_TIMEOUT` (30s) and treating an expired read as a question rather than an answer: the subscriber pings and then waits for the *reply*, which is the one thing a stalled connection cannot produce, and only then concludes that the subscription has stopped delivering. Unanswered, it logs, drops both in-process tiers rather than serving them as current, closes the connection go-redis deliberately keeps after a mere timeout, and resubscribes. This row's evidence is right on every point and is left as written, including its note that `Channel()` would not help — that was checked again at v9.21.0 before the fix was written. Two things it did not reach. The flush moved from the reconnect to the failure, because against a Redis that never returns "flush when we reconnect" is a flush that never happens; and the fix had to be shown *not* to answer the finding by emptying the cache on every quiet interval, which is what the new test's first half asserts. `TestAStalledSubscriberStopsTrustingWhatItCannotHear` (`test/integration/invalidation_test.go`) was run red against the shipped tree first, timing out on the stale entry it went on to drop. D42. |
| F20 | M28 | `/workspaces` and `/members` return 500 on a plain GET once an organization has more than one workspace | `internal/httpx/web_team.go:40` and `:197` declare `Workspaces []team.Workspace`, shadowing the shell's `Workspaces []auth.Workspace` (`internal/httpx/web.go:90`); `internal/ui/templates/partials/nav.html:30` reads `.OrganizationName`, which `team.Workspace` does not have | Reproduced live on the test instance and confirmed by the server's own log: `render failed … page=workspaces error="ui: render page workspaces: template: nav.html:30:61: executing \"workspace_switch\" at <.OrganizationName>: can't evaluate field OrganizationName in type team.Workspace"`, and an identical line for `page=members`. Creation itself succeeds — the POST 303s and the row is in Postgres; the page you land on is what fails. **The trigger is >1 workspace in the CURRENT organization, not >1 overall**: an adversarial verifier held a user with six workspaces across two orgs and got 200 while acting in the one-workspace org, 500 on switching back, because `team.Service.Workspaces` filters on `actor.OrgID` (`internal/team/workspace.go:60`). A regression test written from the looser wording would pass without the fix. Two further symptoms: the switcher is silently **absent** from both pages rather than merely broken, so the two pages most about workspaces are the two you cannot switch from; and `renderWorkspacesError` re-renders the same broken page, so a duplicate workspace name answers 500 instead of 422 and validation messages on that page are unreachable. Every other authenticated GET route was swept and is unaffected. `/members` additionally needs `members.write`, so a read-only member sees no error at all | **Major.** Two shipped dashboard pages are unusable for any organization that has done the thing M28 exists to enable. It falsifies M28's own claims that member management and workspace management exist in the UI | Yes — owner reopened M28 on 2026-08-01 to fix it before [M32.9](phase-details/m32.9.md) completes | M28, 2026-08-01. Closed by giving the page field a name of its own: `membersPageData` and `workspacesPageData` now declare `OrgWorkspaces []team.Workspace`, and the templates and the render fixture follow it. The type stayed as it was — the two lists are genuinely different sets, the organization's workspaces with their per-row rights against every workspace the person may act in across organizations — so the name was the thing that was wrong, not the shape. This row's evidence is right on every point and is left as written; its `:40` and `:197` have since moved. The mechanism the reopening asked for is `TestNoPageDataStructShadowsTheShell` (`internal/httpx/web_shell_test.go`), which parses the package and fails on any struct embedding `shell` that redeclares one of its fields, so the next field to try this never reaches a browser. Two integration tests hold the behaviour, both written with two workspaces **in one organization** because the looser wording passes against the broken code. |
| F10 | M27 | `docs/usage.md` lists the never-grantable scopes and leaves out `audit.read` | `docs/usage.md`, the API keys section — "`apikeys.read`, `apikeys.write` and `org.delete` are never grantable to a key" | Noticed while correcting the sentence beneath it, which claimed `members.*` gates no endpoint. `auth.NonDelegableScopes` in `internal/auth/apikey.go` holds four entries: `apikeys.read`, `apikeys.write`, `org.delete` and `audit.read`. The prose names three. `docs/SECURITY.md` and `api/openapi.yaml` both name all four, so this is one document out of step with the other two rather than an ambiguity about the rule. Predates M27 — the omission dates from [M21](phase-details/m21.md), which added `audit.read` to the map and to the other two documents. | Cosmetic, documentation only. Minting a key with `audit.read` is refused whatever the guide says, so a reader who tries it gets a correct answer from the product; the cost is a reader who plans around a scope they cannot have. | No | [M28](phase-details/m28.md), incidentally, at `1d7c582` — not scheduled work. The scope documentation was rewritten while M28 documented `orgs.create`, and `docs/usage.md:171` now names all four never-grantable scopes including `audit.read`. Recorded rather than left open: the defect is gone, and an open row asserting a live defect that no longer exists is the same lie in the other direction. It was never owner-approved, which is worth seeing — an out-of-spec fix rode along inside an in-spec rewrite. |
| F2 | M23 | A stalled Redis stretches a link edit to about nine seconds | `internal/redirect/resolver.go`, `deleteFromRedis` | Measured while building M23, against a TCP proxy that accepts the connection and then never answers — which is what a stalled Redis looks like, as opposed to a refused connection, which fails in 264ms. `InvalidateAlias` took **9.07s** with the publish removed, so the time is the delete path alone: it makes three attempts, each given `RedisTimeout` (50ms in the test), and go-redis does not honour that context when it has to establish a connection to a server that never speaks. The measurement is in the transcript for M23; the same shape is reproducible with `redisProxy.blackholed(true)` in `test/integration/invalidation_test.go`. Predates M23 — the retry loop is M7's — and M23's own publish was made fire-and-forget so it adds nothing to this. | Moderate. Not data loss: the edit completes, the local tier is cleared, and correctness is unaffected. But `HTTP_REQUEST_TIMEOUT` is 15s, so an edit made while Redis is stalled comes within a few seconds of timing out, and a Redis stall is exactly when an operator is most likely to be changing things. | Yes — owner approved 2026-07-31, scheduled as [M26.6](phase-details/m26.6.md). That milestone corrects this row's attribution: the cause is two retry layers multiplying (the outer 3-attempt loop times go-redis's unset `MaxRetries` default of 3, over a 1s dial budget) rather than a context go-redis fails to honour. 3 × 3 × 1s matches the 9.07s measured. | M26.6, 2026-07-31. Closed by bounding the whole retry loop with one budget (`REDIS_INVALIDATE_BUDGET`, 250ms, D26) rather than by lowering a timeout. This row's evidence is right and is left as written: go-redis really does ignore the context on a stalled read, and the *Reviewed* cell's correction of it — two retry layers multiplying over a 1s dial budget — is wrong on both counts. Measured for M26.6: `MaxRetries` does not multiply a read that timed out, and `REDIS_DIAL_TIMEOUT` is not involved at all against a server that accepts the connection. The arithmetic is 3 outer attempts x the client's `ReadTimeout`, and the 3s in that product was go-redis's own default in `redisProxy.client`, which sets no timeouts. The shipped client sets 50ms from `REDIS_READ_TIMEOUT`, so the same stall cost a deployment **215ms, not 9.07s** — the severity above was measured through a client no deployment runs, and the edit was never near `HTTP_REQUEST_TIMEOUT`. What was genuinely broken is that a documented, operator-tunable knob was multiplied by three on a path an operator waits on. |
| F6 | M25 | Account and sign-out sit loose in the header instead of under the name they belong to | `internal/ui/templates/layout.html:21` (the `Account` navlink), `:28` (the email, rendered as inert text) and `:29`–`:31` (the sign-out form) | Owner report, 2026-07-31, verified against the tree. The header renders the signed-in address as a plain `<span>` that does nothing, with sign-out as a sibling button and Account as a fifth navlink competing with Dashboard, Links, API keys and Notifications. Nothing is broken — every destination is reachable — so this is design, not defect. The convention it departs from is the one every dashboard the reader has used follows: the name is the control, and identity-scoped actions hang off it. Same shape as F4, which this repo recorded as a finding rather than a feature and folded into a milestone: the function exists and the placement is wrong. Note that a dropdown has to work without JavaScript to satisfy the `ui` stays stdlib-only rule — `<details>`/`<summary>` is the only mechanism available, and CSP forbids the inline handler the obvious implementation would reach for. | Minor. No claim is false and nothing is unreachable. The cost is a top-level nav slot spent on Account and a person hunting for sign-out, which is the same discoverability cost F4 carried. | Yes — owner approved 2026-07-31, scheduled as [M26.5](phase-details/m26.5.md) | M26.5, 2026-07-31. The address became a `<button popovertarget>` opening a panel holding Account and Sign out, and both left the top-level nav. This row's evidence is wrong about the mechanism and is left as written: `<details>`/`<summary>` was not "the only mechanism available" — the Popover API is also declarative, also needs no script, also needs no CSP waiver, and unlike a disclosure it closes on Escape. D24 records the correction. |
| F7 | M25 | Notifications are a nav link with a badge, where the convention is a bell that previews | `internal/ui/templates/layout.html:22`–`:25` — the `/notifications` link and its `{{if .Unread}}` count badge | Owner report, 2026-07-31, verified against the tree. The unread count renders and the page at `/notifications` works ([M22](phase-details/m22.md)), so the function is whole; what is missing is the preview. Today the only way to learn what a notification *says* is to leave the page you are on. The note asks for a bell beside the name, a dropdown showing high-importance or recent items, and a "view all" to the existing full page — so the page stays and the dropdown is added in front of it. Partly new function rather than pure placement: the preview list needs a query the header does not make today, and it has the same no-JavaScript constraint as F6. Recorded as one row with F6 because both redesign the same header region and doing them separately would touch `layout.html` twice for one visual result. | Minor. Nothing is lost or unreachable; an unread count with no preview costs a page load to answer "what is it". | Yes — owner approved 2026-07-31, scheduled as [M26.5](phase-details/m26.5.md) | M26.5, 2026-07-31. The nav link became a bell whose panel previews the newest unread notifications and links on to `/notifications`; the count and the preview come back from one query, so the header still costs one notification round trip per page render. Built on the same popover mechanism as F6, for the reason under that row. |
| F5 | M26 | Two shipped documents still say the audit log has no behaviour | `docs/SECURITY.md`, the "What is not defended" list — "**The audit log has no behaviour.** The table is partitioned and maintained; nothing writes to it. Do not rely on it for forensics yet."; and `README.md`, the "Not built yet" list — "**No audit log behaviour**, no folders, no custom domains…" | Noticed while making the same two lists true for M26's mailer. Both claims were true when written and are false now: [M21](phase-details/m21.md) landed a writer (`internal/audit`, wired in `cmd/linkctrl/main.go` as `auditSvc` and passed into the link service), a read API (`GET /api/v1/audit`), a per-table retention window and a growth metric, and `test/integration/audit_test.go` exercises all of it. SECURITY.md's line is the more consequential of the two: it explicitly tells a reader not to rely on the log for forensics, which is advice against a defence that now exists. | Minor, and documentation only — nothing in the product misbehaves. The consequence is an operator who reads SECURITY.md and does not turn on a retention window, or does not know a trail exists, because the file told them there is nothing there. It makes M21's own claim false rather than M26's, so it reopens M21 rather than arriving as new scope — which is the owner's call to schedule, not this milestone's to take. | Yes — owner approved 2026-07-31 | | Not by reopening M21. `m21.md` enumerates the docs that milestone promised to update — the `partitions.go` comment and operations.md — and it updated both; every bullet it carries is satisfied, so an `in progress (reopened)` row would assert incompleteness that is not there. The fault was the per-commit Docs gate, which named only Plan.md and decisions.md and so never asked about README or SECURITY.md. Gate widened and both documents trued in the same commit, along with the M22, M25 and M26 omissions that share the cause. Fixing the audit sentences also surfaced two overclaims nobody had reported: SECURITY.md's claims table and the new README row both said administrative changes are recorded, when `internal/audit` defines exactly one action — `domain.root_redirect_changed`. Both now say so. |
| F3 | M24.5 | Dark mode has no effect on the UI: the light tokens are unlayered and beat every dark rule | `internal/ui/static/css/input.css` — light `:root` block at line 88, dark rules at lines 139–178 (`@media (prefers-color-scheme: dark)`) and 182 (`:root[data-theme="dark"]`) | Owner report, 2026-07-31, and confirmed by construction rather than inferred from the symptom. The light `:root` block sits at the top level of the file; both dark blocks sit inside `@layer base`. CSS cascade layers give **unlayered** normal declarations priority over layered ones regardless of specificity, so `:root { --t-surface: #f8fafc }` unconditionally beats `:root[data-theme="dark"] { --t-surface: #0f172a }`. Walking the built `app.css` top level confirms it survives the build: the dark override is inside the `@layer base { … }` construct, and `:root { color-scheme: light; … }` is emitted after `@layer utilities` closes, as its own unlayered top-level rule. Both dark paths are dead, the explicit override and the `prefers-color-scheme` one, which is why `color-scheme: dark` never takes either — matching "changing to Dark Mode does nothing". The server side is *correct* and is not implicated: the demo serves `data-theme="dark"`, `data-theme="light"` and no attribute across the three cookie states. **The milestone's own enforcement cannot catch this**: `TestTemplatesUseThemeTokensOnly` asserts templates name tokens, and the AA contrast figures are recorded in comments, so nothing tests that a token's *value* ever changes. A fix wants a test that fails on this specific cascade, not more contrast arithmetic. | **Critical.** [M24.5](phase-details/m24.5.md)'s headline claim — every dashboard page follows `prefers-color-scheme`, and an explicit override renders in the chosen theme — is false in the shipped build. The milestone was reopened rather than corrected by a successor, so the status row now reads `in progress (reopened)` and no longer asserts something untrue. The AA contrast claim is false by consequence: the dark figures describe values no browser applies. M24.5 was placed before M25, M31, M37, M38, M41, M43 and M44 precisely so those build inside a working theme; every UI milestone landing before this is fixed is written and reviewed against a dark theme that cannot be seen, which is the retrofit the ordering existed to avoid. | Yes — owner-reported; folded into reopened M24.5, 2026-07-31 | M24.5, 2026-07-31. Every `--t-*` block is now unlayered, so the dark selectors win on specificity and source order; `TestThemeTokensShareOneCascadeLayer` was shown failing against the stylesheet built from `b289a39` before it counted, and the live check now resolves the cascade against the composed stack rather than confirming the attribute. |
| F4 | M24.5 | The theme control sits in the page footer, where nobody looks for it | `internal/ui/templates/layout.html`, the `theme_switch` template near the footer | Owner report, 2026-07-31. Not a false claim: [m24.5.md](phase-details/m24.5.md) says the override is "settable from the dashboard" and never names a location, so the footer satisfies the milestone as written. It is a placement judgement the milestone did not make — appearance preferences are conventionally in account settings or the top-level chrome, and the footer is the one region of the page a person scanning for a setting will not read. Related to F3 only by file: the control works, it is merely hard to find, and moving it fixes nothing about F3. | Minor. No claim is false and nothing is broken. It is discoverability, and it costs a person one hunt through the page rather than any data or correctness. | Yes — owner-reported; folded into reopened M24.5, 2026-07-31 | M24.5, 2026-07-31. The control moved to `/account`, with the sign-in page rendering the same partial; `TestExactlyOneAppearanceControlPerPage` asserts the count on every page, so the layout's site cannot come back. |
| F76 | M44.9 | A domain verification can be made to verify a hostname the caller does not control — including the instance's **own** app or link host | `internal/link/verification.go:166` reads the row, `:175` resolves DNS (up to `DOMAIN_VERIFY_DNS_TIMEOUT`, 5s), `:201` writes `MarkDomainVerified(ctx, row.ID)`; `internal/store/query/domains.sql:162-170` is `WHERE id = $1 AND deleted_at IS NULL`. Same shape at `verification.go:301`/`:312` in `ReverifyDomains` | Read-check-write with an attacker-pausable network call in the middle, no transaction and no `FOR UPDATE` (`domainForWrite` is a bare `GetDomainByID`). `RenameDomain` is concurrently reachable at `PATCH /api/v1/domains/{domainID}` and clears `verified_at`; the late write then fills exactly that NULL through `COALESCE(verified_at, now())`. Nothing re-reads or re-compares the hostname, and `MarkDomainVerified`'s `RETURNING` row is consumed only to stamp the audit record at `:206-208` — **so the audit trail corroborates the hijack instead of catching it**. Adversarial verification established the impact is wider than a customer hostname: D69 and `hostname.go:39-96` refuse no self-host name, and `customHostRouter` sits in front of both router branches (`router.go:826`), so renaming the in-flight row to the instance's **app host** hides the dashboard and API behind the custom mux (ops + root + aliases only), and renaming it to the **link host** repoints every alias lookup at the attacker's `domain_id` via `redirect.go:177-179`. DNS and certificates for those names already exist | **Critical.** Falsifies m40.md's *"Only after `verified_at` is set does the host router resolve aliases on that hostname"* and `domains.sql:71-76`'s rename rationale. Reachable by any principal holding `domains.write` in any workspace — every organization owner and admin, and one registration on `LINKCTRL_SIGNUP_MODE=open`. The attacker owns the nameserver, so they choose the window length. Fix note: a transaction around read and write does **not** close it, because the rename commits in the gap between two separate transactions, and `FOR UPDATE` across the lookup holds a row lock for 5s inside a 500-row job. The shape that closes it is a conditional write (`WHERE id = $1 AND hostname = $2 AND verification_token = $3`) with a zero-rows-affected branch, applied at both sites | No | **[M40](phase-details/m40.md), reopened.** `MarkDomainVerified` is predicated on the hostname and the token the check was made against, with an explicit zero-rows branch, at both call sites; the audit record is stamped from the checked hostname. D92 records why a transaction and `FOR UPDATE` were both refused. `TestARenameCannotStealAVerificationInFlight` and `TestTheJobCannotVerifyANameItDidNotCheck` rename into the instance's own link and app hosts mid-lookup, and both fail on a build without the predicate. |
| F84 | M44.9 | Custom-domain re-verification can be starved indefinitely by one tenant, which silently disables the only mechanism that takes a lapsed or hijacked hostname out of service | `internal/link/domains.go:206-252` (`RegisterDomain`) has no per-workspace count check, unlike campaigns and folders; `internal/link/verification.go:292-302` walks one ordered batch serially; `internal/store/query/domains.sql:140-147` orders `verification_checked_at NULLS FIRST, id LIMIT $row_limit` (`DOMAIN_VERIFY_BATCH`, 500); `cmd/linkctrl/jobs.go:382` bounds the pass at ten minutes | Each row blocks up to `DOMAIN_VERIFY_DNS_TIMEOUT` (5s) against a nameserver the registrant controls, so 500 × 5s is 41 minutes against a 10-minute budget. Adversarial verification **corrected the mechanism**: rows the loop reaches do write `verification_checked_at`, so NULL rows drain — but `RenameDomain` resets that column to NULL (`domains.sql:82`), so one `PATCH` per row returns it to the head of `NULLS FIRST` indefinitely. Verified tenants carry non-NULL watermarks and therefore sort last, so `UnverifyDomain` (`verification.go:363`) never fires for them | **High**, and cross-tenant. What is starved is not throughput but the D70/D71 hard stop — the deleted-CNAME plan that m40.md calls *"the whole security story of this milestone"* — which stops running instance-wide while appearing healthy. Needs one workspace with `domains.write` and one zone delegated to a blackholing nameserver; the hostnames need not exist. Fix note: raising `DOMAIN_VERIFY_BATCH` makes it strictly worse, and adding lookup concurrency without a per-workspace domain cap turns the leader into an outbound DNS amplifier any tenant can aim | No | **[M40](phase-details/m40.md), reopened.** The pass is drawn in two classes and serving hostnames are checked first, inside the one configured batch — a rename un-verifies, so registration churn can crowd only the pending class. `RegisterDomain` caps a workspace at `domain.MaxDomainsPerWorkspace` (25). The batch was not raised and no lookup concurrency was added; nothing reaps a registration. `TestServingHostnamesAreCheckedBeforePendingOnes` and `TestAWorkspaceCannotRegisterUnboundedHostnames`. |
