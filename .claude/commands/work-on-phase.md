---
description: Build the current phase, one milestone at a time, until the phase ends
---

Read `docs/build-notes/phase-loop.md` and follow it exactly. It is the sequence;
`docs/build-notes/workflow.md` is the gates; `Plan.md` is the scope.

Start at step 0 (resume). Do not skip validation, do not bundle milestones, do
not start the next phase.
