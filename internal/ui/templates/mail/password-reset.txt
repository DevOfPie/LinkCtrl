Subject: Reset the password on your LinkCtrl account

Hello,

Somebody asked to reset the password for this address ({{.Email}}) on this
LinkCtrl instance.

Open this link to choose a new one:

    {{.URL}}

Nothing has changed yet. Your current password still works until you finish
that form.

The link works once and stops working on {{.Expires}}. Asking again replaces
it, so if this one has lapsed, start from the sign-in page.

When the new password is set, every session on the account is signed out —
including any this address did not open. API keys are not affected and keep
working.

If you did not ask for this, ignore it. Nobody can reset the password without
the link above, and it is in this mailbox only.

--
LinkCtrl
